# 软件升级迭代记录

> **项目**：USB 副屏工具（`MSU2_MINI_V2.py`）
> **当前版本**：v5.38.0（2026-09-22）—— **最终更新版**
> **维护说明**：本文件（原名《软件升级迭代过程和现在进行时》，2026-08-27 更名为《软件升级迭代记录》）随每次软件更新同步维护；历史版本源码保存在 `version/` 子目录，`MSU2_MINI_V2.py` 为最终更新版（最后编译也是使用这个文件编译的），因此本文档以「`version/` 全部历史版本 + 主程序」为依据，逐版本记录迭代内容，并在文末汇总「当前版本完整功能清单」，确保原有功能不遗漏、不丢失。
>
> **强制约定（每次更新必须遵守）**：①修改 `MSU2_MINI_V2.py` 后必须同步更新本文档（详细迭代记录 + 功能清单 + 代码地图三处）与主程序 `PROGRAM_VERSION`/`PROGRAM_CHANGELOG`；②每次更新版本号必须把主程序备份到 `version/` 子目录；③所有「设置」采用实时自动保存 + 持久保存，保证下次启动沿用上次设置；④脚手架/测试脚本一律放 `_scaffold\` 子目录。

---

## 一、版本总览（速查表）

| 版本 | 日期 | 一句话摘要 |
|---|---|---|
| 2.0.0 | 2026-08-11 | 整合两个开源项目，正式发布第一版 |
| 2.0.1 | 2026-08-11 | +页面下拉选择、上/下翻页 |
| 2.1.1 | 2026-08-11 | +显示方向选择（镜像/旋转） |
| 2.1.2 | 2026-08-11 | 修复时钟背景问题 |
| 2.1.3 | 2026-08-11 | 内部微调 |
| 2.1.4 | 2026-08-11 | 烧写区四区化 + 预设颜色下拉 |
| 2.1.5 | 2026-08-11 | +丰富色卡（Material/霓虹等色系） |
| 2.1.6 | 2026-08-11 | +防烧屏（像素微移） |
| 2.1.7 | 2026-08-11 | 烧写提示文案完善 |
| 2.1.8 | 2026-08-11 | 开始编译（打包） |
| 2.1.9 | 2026-08-11 | 尝试修复编译后斜45°问题 |
| 2.1.10 | 2026-08-11 | 截图防御增强 + mss 回退 + 安全发送 |
| 2.1.11 | 2026-08-11 | 串口缓冲细节修复 |
| 2.1.12 | 2026-08-11 | +LCD 看门狗（防硬件漂移） |
| 3.0.0 | 2026-08-11 | 多屏第一版（ScreenDevice 多设备架构） |
| 3.0.5 | 2026-08-11 | 修复 mss 线程本地化 bug |
| 3.0.6 | 2026-08-11 | +实时预览 |
| 3.0.7 | 2026-08-11 | 预览增强（时钟/系统状态软渲染） |
| 3.1.0 | 2026-08-11 | 预览图像 per-device |
| 3.1.1 | 2026-08-11 | +SN 检查（为按设备存储设置做准备） |
| 3.1.2 | 2026-08-13 | 继续修复倾斜（分块写入/排空缓冲） |
| 3.1.3 | 2026-08-13 | 修复 ADC 误判/串口带宽争抢 |
| 3.1.4* | 2026-08-14 | ⚠ 文件名误标，实为 4.5.4 副本（见注） |
| 3.1.5 | 2026-08-13 | 版本号回归 3.1.4，清理误存内容 |
| 3.1.6 | 2026-08-13 | +UI 消息队列、自定义显示可视化编辑器 |
| 4.0.1 | 2026-08-13 | 大版本：15 个新显示页面 + 大量通用功能 |
| 4.1.0 | 2026-08-13 | 内部微调 |
| 4.2.0 | 2026-08-13 | 跑马灯字体/字号/实时预览、天气中文城市 |
| 4.3.0 | 2026-08-13 | 页面内容分页设置 + 仪表盘初版 |
| 4.3.1 | 2026-08-13 | 仪表盘补全 + 硬件详情类型 + 按页面导航 |
| 4.3.2 | 2026-08-13 | 仪表盘补全（GPU温/风扇/网速）+ 串口渲染锁 |
| 4.3.3 | 2026-08-13 | 监控显示子分页 + 仪表盘行统一生成 |
| 4.4.1 | 2026-08-13 | 修复双屏问题 + 传感器自由选择 |
| 4.5.1 | 2026-08-13 | 多屏独立配置（按 SN 保存） |
| 4.5.2 | 2026-08-13 | 热搜独立设置 + 跑马灯速度 |
| 4.5.3 | 2026-08-13 | 设置更改实时生效 |
| 4.5.4 | 2026-08-14 | 热搜字体自适应 + 居中布局 |
| 4.5.5 | 2026-08-14 | 取消自定义字体（跑马灯固定黑体） |
| 4.5.6 | 2026-08-14 | +设备信息页（USB/SFR/Flash/系统） |
| 4.5.8 | 2026-08-15 | 磁盘读写三模式 + 网络流量颜色可配 |
| 4.5.9 | 2026-08-15 | +配色方案系统（内置 260+ 色系） |
| 4.5.10 | 2026-08-15 | 配色方案独立页 + 色块下拉 + 混搭 |
| 4.6.0 | 2026-08-16 | +本地 API 投屏服务器（HTTP+WebSocket） |
| 4.6.1 | 2026-08-16 | +多协议接入（TCP/UDP/管道/热文件夹等） |
| 4.6.2 | 2026-08-16 | +API 信息/控制接口（31 端点） |
| 4.6.4 | 2026-08-16 | +屏幕序号检测 + 强制投屏 |
| 4.7.0 | 2026-08-16 | 配置同步修复 + 后台化优化 |
| 4.7.1 | 2026-08-16 | 多屏页面串扰/API 重启等修复 |
| 5.0.0 | 2026-08-16 | 重大重构：Tkinter → PySide6 |
| 5.0.1 | 2026-08-16 | 多屏隔离修复 + 跨线程桥 + 预览优化 |
| 5.0.2 | 2026-08-16 | 完成 PySide6 迁移收尾 |
| 5.0.3 | 2026-08-16 | 下拉框/设备信息跨线程修复 |
| 5.1.0 | 2026-08-17 | +电视墙（显示墙 + 显示墙设置） |
| 5.1.1 | 2026-08-17 | 显示墙布局独立配置文件 |
| 5.1.2 | 2026-08-17 | 布局恢复时机修复（渐进连接） |
| 5.2.0 | 2026-08-17 | 显示墙性能优化（numpy 降采样） |
| 5.3.0 | 2026-08-18 | 画布显示墙 + UI 状态持久化 + 配色全覆盖 |
| 5.4.0 | 2026-08-18 | 配色方案对话框友好化 + 修复管理页多屏空白 |
| 5.5.0 | 2026-08-18 | 修复电视墙布局掉线/重连后自动重置为推荐值（2行×1列 → 保持） |
| 5.6.0 | 2026-08-19 | **主程序当前版**：修复编译(Nuitka)后 exe 启动崩溃（移除 ImageTk/tkinter 残留导入） |
| 5.7.0 | 2026-08-27 | **能效模式（1~5级）** + 静态页不重复刷新 + API 附加协议开关 + UI 显隐控制 + 硬件监控惰性加载 + 1级能效深度优化/降频 + 设置实时自动保存加固 |
| 5.8.0 | 2026-08-27 | **Webhook 对接**（Synology Chat 等聊天机器人）：接收（发出 Webhook 回调→屏幕叠加显示）+ 发送（传入 Webhook URL 推送）+ 设置实时自动保存 + OpenAPI/文档同步 |
| 5.9.0 | 2026-08-27 | **gRPC 全面接入**：附加接入协议 gRPC（端口+4，msu2.Msu2Api：Execute 投屏命令 + WatchFrame 流式帧）+ 设置勾选框 + 协议自动发现 + 文档同步 |
| 5.10.0 | 2026-08-27 | **MQTT 全面接入**：附加接入协议 MQTT（连接外部 Broker：命令/响应/帧主题）+ 设置勾选框与 Broker 配置 + 协议自动发现 + 文档同步 |
| 5.11.0 | 2026-08-28 | **硬件监控数据源可选 AIDA64**：设置→监控显示 切换 LibreHardwareMonitor / AIDA64（共享内存读取，需后台运行并开启共享内存） |
| 5.12.0 | 2026-08-28 | **硬件监控数据源新增「系统原生」**：无需任何后台程序（psutil/WMI/nvidia 程序内直读）也能获取硬件数据 |
| 5.13.0 | 2026-08-28 | **DeepSeek 余额显示页**：官方 /user/balance 接口 + 自定义显示项（余额/赠送/充值/币种/可用/累计消费估算）+ API Key 存 .env |
| 5.14.0 | 2026-08-28 | **DeepSeek 余额改为仅显示官方返回字段**（去掉累计消费估算）：总余额/赠送/充值/币种/可用 自定义勾选 |
| 5.15.0 | 2026-08-28 | **DeepSeek 余额字体设置**：字体自适应屏幕（默认）+ 手动字号 |
| 5.16.0 | 2026-08-28 | **设置持久化遗漏全面修复**：显示信息框（isHidden 误判）/设为自动连接/屏幕分辨率/API-MQTT 即时保存/DeepSeek Key 失焦保存 |
| 5.17.0 | 2026-08-28 | **主控页控件按设备配置恢复**：新增 _apply_main_ui_to_config，设备连接后自动回填 RGB/填充/FPS/相机/分辨率等，启动无需重选 |
| 5.18.0 | 2026-08-28 | **DeepSeek 余额对齐方式**：垂直居中（默认）/ 向上对齐 |
| 5.19.0 | 2026-08-28 | **DeepSeek 余额显示项自定义模板**：每项可自定义格式（%1=值 %2=币种，如「自定义 %1 元」），留空用默认 |
| 5.20.0 | 2026-08-28 | **修复 DeepSeek 自定义模板后字号自适应**：单行/少行字号上限 32→24，更协调 |
| 5.21.0 | 2026-08-28 | **DeepSeek 字号自适应改为基于内容宽高**：单行/少行字号上限 50（内容短用大号），长内容/多行自动缩小 |
| 5.22.0 | 2026-09-11 | **修复「显示信息框」设置不持久**（`or 1` 把保存的 0 吞掉，重启又自动显示）+ **修复两块小屏总有一块自动断开**（端口归属/主设备槽位被 UI 改写/单次通信失败误判断开） |
| 5.23.0 | 2026-09-13 | **编译脚本完善 + 修正「仅编译后才出现」的 3 处问题**：网络延迟/电池页子进程弹黑框（补 `CREATE_NO_WINDOW`）、开机自启动注册命令（Nuitka 不设 `sys.frozen`）、编译脚本（`call nuitka`/`cd /d %~dp0`/exe 版本信息/发布目录自动带 config 与 .env） |
| 5.24.0 | 2026-09-13 | **无控制台运行日志 + 串口连不上原因提示**（排查「编译后一块屏正常、另一块像没连接」）：无控制台时输出写 `MSU2_MINI_log.txt`（滚动 2MB）；串口打开失败/设备无响应/校验失败的原因汇总到信息框；并明确「本程序无单实例保护，跑两个实例会各占一块屏」 |
| 5.25.0 | 2026-09-13 | **新增单实例保护**（命名互斥体）：第二个实例启动时把已运行窗口切到前台/弹提示后退出，从根上消除「两个实例各抢一块屏 → 一块屏像没连接」；`--allow-multiple` 可多开 |
| 5.26.0 | 2026-09-19 | **修复挂机后「一块屏不显示，却显示已连接、预览也正常、切页也不恢复」**：daemon 改**逐屏 try/except**（单屏渲染异常不再拖死其它屏）+ 新增**画面刷新健康度**（状态栏/设备信息页显示「画面刷新正常 / 画面停滞 / 串口写入失败」）+ 新增**每屏通信心跳与线程看护**（非主屏掉线能被发现、截屏线程死了自动重启）+ 方向看门狗后**强制整帧重绘**（静态页不再黑） |
| 5.27.0 | 2026-09-19 | **新增「速度单位换色」**：网络流量 / 磁盘读写 的柱状图，某一路速度单位达到 MB/s（及以上）时换成「兆色」，单位是 KB/s 时保持原色——一眼区分兆与 KB；设置 → 监控显示 顶部新增开关 + 两个兆色（上行/读、下行/写），随设置实时保存（无需每次重设） |
| 5.28.0 | 2026-09-20 | **修复「屏幕黑着却一直显示已连接」的真正原因——设备复位后一直回上电横幅**：横幅 `\x00MSNxx` 不再被当成「通信正常」（旧逻辑会反复清零失败计数 → 永不判掉线 → 永不重连），而是**立即按掉线处理并自动重连重初始化**；响应校验/画面刷新健康度同步接入（能看出「通信异常 + 原因」）；错误日志统一限频（不再每秒刷几十行）；原有瞬时失败容错保持不变 |
| 5.29.0 | 2026-09-21 | **修复小屏画面偶尔倾斜/斜切（能自己恢复但更密集）——整帧「地址窗口 → 数据」之间有空档**：设备按字节流解析命令，窗口命令后必须紧跟像素数据，而旧代码「LCD_ADD →（渲染+编码几十毫秒）→ 发整帧」的空档里，按键 ADC 轮询/新增的 2 秒心跳/API 调用会插进命令 → 解析错位、像素高低字节错开 → 画面倾斜（预览却正常）；v5.26.0 方向看门狗每 15 秒强制 `state_change=1` 让这条路径变成周期性 → 倾斜变密集。现改为**窗口命令与整帧数据拼成同一条字节流、一次 `SER_rw` 发出（空档归零，任何线程插不进来）**，覆盖 `_safe_send_rgb888` 全部页面与屏幕镜像；并加「帧空档/空档内被插入命令」观测计数 |
| 5.30.0 | 2026-09-21 | **倾斜仍在 → 串口使用严格化为「事务模型」（帧前/帧后/方向重置后都不允许别的命令靠近）+ 帧自愈 + 看门狗收敛**：①帧发送期间与之后 150ms 静默期内，按键 ADC 轮询/通信心跳/API 一律避让（daemon 把「心跳 + 整轮渲染」当一个事务）；②`LCD_State` 方向重置后静默 200ms；③帧写失败/写不完整→**立刻整帧重发**（一帧内自愈）；④`SER_Write` 短写改为续写剩余（不再留下残缺帧）；⑤方向看门狗清屏后**只对静态页**强制整屏重绘（动态页不再每 15 秒多插一次「清屏+立刻整帧」）；⑥新增静默期违规计数与 5 个环境变量 A/B 开关 |
| 5.31.0~5.34.0 | 2026-09-22 | ⚠ **另一条并行改动线（非本文件）**：设备侧流控「等待传输完成」/串口单消费者队列/端口操作入工人线程与 NameError 修复/背压不重发与繁忙不判掉线。备份见 `version\MSU2_MINI_V2 - 5.31.0~5.34.0 *.py`；**未合入当前主文件**（当前主文件向下是 5.30.0，向前是 5.35.0）。注意 `_scaffold/smoke_test_multiscreen.py` 已被那条线改成引用 `_note_backpressure`，在未合入前它会失败（与本文件无关） |
| 5.35.0 | 2026-09-22 | **根治：行级地址重锚**——每行开头重新下发一次「第 row 行、行宽 W」的地址窗口，把写指针**每行重新锚定**；提交按行对齐切分。任何一次错位的后果被锁死在**一行以内**（下一行立即恢复），整幅斜切在结构上不再可能；量化：丢一个提交包时旧版错 4389 像素/40 行 → 本版 143 像素/1 行且之后每行像素级完美；体积 +8%；`MSU2_MINI_ROW_GUARD` 可 A/B |
| 5.36.0 | 2026-09-22 | **「速度单位换色」改为逐根柱子判断**：不再按瞬时速度给整条色带换色（一排柱子同色、看不出差别），而是**该柱自身数值 ≥ MB/s 就用「兆色」、其余保持原色**，于是**同一行里可以同时出现原色与兆色**；上下行/读写各有自己的兆色；设置文案同步；测试扩到 40 项（含像素级逐柱断言） |
| 5.37.0 | 2026-09-22 | **「速度单位换色」升级为「单位色 + 换色门槛」**：柱色改为逐柱按优先级求值——①**换色门槛**（自定义条件：大于/大于等于/小于/小于等于/等于/不等于，可选适用范围、数值+单位 B/KB/MB/GB/TB，**可随意增删任意多条，按顺序第一条命中即用**）→②**单位色**（<1K(B)/KB/MB/GB/TB 五档各自一色，留空=用原色）→③该行原色；设置页新增 **2×5 单位色网格**（色值框+取色+清空）与**换色门槛编辑器**（增/删/改 + 落盘）；测试扩到 **90 项** |
| 5.38.0 | 2026-09-22 | **单位色默认就是五种不同颜色（马卡龙色系）+ 设置里可直接选「色系」**：新增色系表 `SPEED_UNIT_SCHEMES`（7 套：马卡龙（默认）/糖果亮彩/暖色/冷色/高对比/单色·青绿/单色·橙金），每套给「上行/读」「下行/写」各 5 色，顺序 = <1K(B)/KB/MB/GB/TB；`sys_config` 十个单位色字段默认值由**马卡龙套派生**（开箱即五档五色、上下行不同色）；设置页加 **「色系:」下拉框**（选中一套即写入 10 色 + 落盘 + 表格同步；单格手改后显示「自定义」）；色值框改为显示配置真实值（不再回退成 MB 色）；测试扩到 **116 项** |

> **注（版本文件异常说明）**：
> - `version/MSU2_MINI_V2 - 3.1.4 深度使用...py` 文件内部 `PROGRAM_VERSION` 实为 **4.5.4**、构建日期 08-14，内容与 4.5.4 相同——它是开发过程中误以「3.1.4」命名的 4.5.4 副本，不构成独立的 3.1.4 版本。
> - `3.1.5 / 3.1.6 / 4.0.1 / 4.1.0 / 4.2.0 / 4.3.0` 这几个文件内部 `PROGRAM_VERSION` 仍为 3.1.4（开发期未及时改版本号），本文按文件名的名义版本记录。
> - `v4.3.2` 文件内部版本号为 4.3.1，`v4.5.2/4.5.3` 内部为 4.5.1，`v4.4.1` 内部为 4.3.1——同属「未及时改版本号」的情况。
> - 部分更新内容只体现在主程序 `PROGRAM_CHANGELOG` 中，如 v4.5.7、v4.6.3、v4.8.0（未单独立档文件，已分别并入 4.5.8、4.6.4、5.0.0 记录）。

---

## 二、详细迭代记录

### 阶段一：2.x —— 整合与打磨（Tkinter 时代，2026-08-11）

#### v2.0.0 整合后第一个版本
- 正式发布「USB 小屏幕助手」v2.0.0。
- 整合开源项目 `dimangopie/just-fun-for-MSU2_MINI`(MIT) 与 `dchg43/msu2_mini`(MIT)。
- 基础显示页面：动图、时钟、相册、屏幕镜像、相机视频、系统监控（CPU/内存/磁盘/电池使用率）、网络流量监控。
- 自定义显示内容（MiniMark 模板引擎）。
- LCD 屏幕分辨率自动检测（SFR 读取）。
- 托盘图标隐藏。
- 串口通信、RGB565 编码、mss/PrintWindow 截图等底层能力。

#### v2.0.1 +more1
- 新增「页面」下拉选择框 + ▲上翻/▼下翻 按钮（`Page_UP`/`Page_Down`、`sync_page_combobox`/`on_page_combobox_select`）。
- SFR 数据读取与设备 SFR 名称列表打印（`_read_sfr_item`）。
- 窗口标题栏显示程序版本信息。

#### v2.1.1 升级一些功能
- 新增「显示方向」下拉选择：水平/垂直/水平镜像/垂直镜像/镜像+90° 等（`LCD_Change`/`set_lcd_direction`/`sync_lcd_combobox`/`on_lcd_direction_select`）。
- 窗口枚举支持多显示器（`get_all_hwnd` 跳过完整桌面、逐个添加显示器）。

#### v2.1.2 时钟背景问题解决
- 修复时钟页面背景显示问题。

#### v2.1.3
- 内部微调（无显著代码结构变化）。

#### v2.1.4
- 烧写区改造为四区「选择+烧写」半宽按钮：闪存固件 / 相册图像 / 背景图像 / 动图文件。
- 新增预设颜色下拉（常用色 + 马卡龙 + 暖色系 + 冷色系 + 暗色系，`apply_color_preset`）。

#### v2.1.5 +色卡
- 颜色预设扩充：新增 Material Design 色系、霓虹色系等更丰富色卡。

#### v2.1.6 +防烧屏
- 新增「防烧屏」设置项（`anti_burn`，默认开启）：每 30 秒循环微调 1 像素显示位置，延缓 OLED 烧屏（`update_burn_offset`）。
- 切换页面时重置防烧屏偏移。

#### v2.1.7
- 烧写区按钮提示文案完善（选择闪存固件/相册图像/动图文件）。

#### v2.1.8 开始编译
- 开始打包/编译（编译脚本与产物流程建立）。

#### v2.1.9 尝试修复编译后显示斜45度问题
- `ContinuousCapture.get_actual_size`：修正编译后 DPI/窗口尺寸获取，尝试解决编译版斜 45° 问题。

#### v2.1.10
- 截图防御增强：`_mss_fallback_screenshot`（PrintWindow 失败自动回退 mss）、`_record_printwindow_fail`。
- 防御性安全发送 `_safe_send_rgb888`（编码/发送异常自动恢复 LCD 方向）。
- 串口写入字节数校验（`SER_Write` 防止部分写入错位）。
- 输入数据长度与 LCD 分辨率匹配校验；缩略尺寸校验 + 回退填充模式。
- 自定义渲染锁（`custom_render_lock`）：避免 UI 预览与 daemon 渲染并发冲突。
- LCD 定期看门狗（每 30 秒强制重置方向，防硬件状态漂移）。

#### v2.1.11
- 串口缓冲细节修复：`SER_Write` 不调用 `reset_output_buffer()`，避免 USB 适配器内部缓冲被打断导致命令流截断。

#### v2.1.12 修复倾斜，+看门狗
- 正式加入 LCD 看门狗机制：定期强制重置 LCD 方向，防止任何原因导致的硬件状态漂移。

### 阶段二：3.x —— 多屏时代（2026-08-11 ~ 08-13）

#### v3.0.0 多屏1版
- **多设备支持基础设施**：新增 `ScreenDevice` 类（状态/串口/线程/配置），`get_current_device`/`set_current_device`（线程本地活跃设备），`_init_single_device`（单屏兼容模式）。
- 每屏独立线程：截图线程、画面处理线程、FPS 控制线程（`screen_shot_task`/`screen_process_task`/`fps_control`）。
- UI 顶部新增「已连接设备」下拉框 + 设备计数 + 「刷新」按钮（`refresh_device_list`/`on_device_select`）。
- daemon 遍历所有已连接设备，各自运行状态机（多设备模式）。

#### v3.0.5 fix bug1
- 修复 mss GDI 句柄线程本地化问题：每个线程必须独立创建 mss 实例（截图线程/窗口截图各自独立实例）。

#### v3.0.6 +预览1
- 新增「实时预览」：实时显示小屏当前内容（`preview_enabled` 配置）。
- 保存屏幕镜像 RGB888 图像供 UI 刷新（`_preview_rgb` + `_preview_lock`），预览画布显示。

#### v3.0.7
- 预览增强：时钟/系统状态页面软件渲染预览（`_update_preview_clock`/`_update_preview_state`）。
- 预览画布自动缩放居中（宽度自适应最大 480，高度按比例）。

#### v3.1.0
- 预览图像改为 per-device（`last_preview_rgb` + `_preview_lock`），多屏各自独立。

#### v3.1.1 +SN检查
- 新增 USB 描述符诊断 `_dump_usb_descriptor`：读取端口序列号（SN），为后续「按 SN 独立存储设置」做准备。

#### v3.1.2 继续修复倾斜问题2
- 倾斜修复第二阶段：
  - 串口大块数据改为**分块写入并逐块校验完整性**，避免单次 write 超时导致命令流截断。
  - 大块数据发送后排空 USB 适配器缓冲，避免下一条命令与末尾字节交错。
  - 切换时彻底重置截图流水线（清空队列 + 丢弃在途帧），只发送最新帧。
  - LCD 方向重置清屏改用**设备实际分辨率**（修复 240×240 等设备清错区域）。

#### v3.1.3 依旧是倾斜问题
- 修复 ADC 心跳读取失败误判断开导致帧写入被截断 → 画面倾斜（`adc_fail_count` 连续失败计数容错）。
- `SER_Read` 改为定时等待，给设备渲染完成留出响应时间。
- 限制按键 ADC 轮询频率，避免与屏幕镜像帧发送争抢串口带宽。
- 串口写入前复查端口状态，防止并发关闭导致写入失败。
- 修复退出时线程未结束就关闭串口的竞态。

#### ⚠ v3.1.4（误标文件）
- 文件内部实为 **4.5.4** 构建（内容见 v4.5.4 记录），不构成独立版本。

#### v3.1.5
- 版本号回归 3.1.4，清理误存的 4.5.4 内容。
- 保留多屏基础 + 设置页基础（防烧屏/实时预览设置框）。
- 完善防御性发送：丢弃堆积旧帧只发最新帧、发送期间切换立即结束本轮由状态机统一重置方向。
- UI 错误弹窗（`manage_task` 中 `messagebox.showerror`）。

#### v3.1.6 +多线程（最后一个纯功能版本）
- **UI 消息队列**：工作线程→主线程安全更新界面（`_ui_msg_queue`/`_do_insert_text_message`/`_process_ui_msg_queue`，主线程 `after` 轮询）。
- **自定义显示「可视化命令编辑器」**：逐条选择命令类型（p 文本/a 锚点/m 移动/t 偏移/f 字体/c 颜色/i 图片/v 数值）、填入参数即可插入模板（`update_cmd_hint`/`browse_resource`/`insert_command`）。
- **自定义显示「图形化编辑」标签页**：命令列表（Listbox）+ 添加/编辑/删除/移动/清空 + 右侧实时预览，与「显示多项数值」页共享模板（`cmd_to_line`/`cmd_to_desc`/`parse_template_to_cmds`/`open_cmd_dialog`）。
- 窗口列表后台线程枚举（`load_windows_async`/`apply_windows`），避免启动卡顿。

### 阶段三：4.x —— 功能爆发 + API（2026-08-13 ~ 08-16）

#### v4.0.1 增加很多功能
- **新增大量显示页面**（PAGE_ID 10~25）：文字跑马灯、磁盘读写速率、网络延迟、进程 TOP、番茄钟、纪念日、待办事项、世界时钟、农历、仪表盘、硬件详情、天气、行情、热搜、电池、音乐。
- **自定义显示增强**：新增 矩形/线条/圆/动图 命令；模板预设库（大字时钟/监控面板等，`apply_preset`）；模板导入导出（`import_template`/`export_template`）。
- **按键自定义**：单击/双击/长按 动作映射（`key_single`/`key_double`/`key_long`，`do_key_action`）。
- **番茄钟**：开始/暂停/重置（`toggle_timer`/`reset_timer`）。
- **界面语言**：页面名称/方向名称 中英切换（`apply_language`）。
- **开机自启动**：写入 HKCU Run 注册表（`set_auto_start`）。
- **配置导入导出**（`export_config`/`import_config`）。
- **命令行参数**：`--page` 页面、`--com` 串口（`apply_command_line_args`）。
- **自动更新检查**：GitHub Release 后台检查（`check_update`/`check_update_async`）。
- **自动翻页轮播**（`page_cycle_enable`/`page_cycle_interval`，`_auto_cycle_tick`）。
- **息屏待机**：无操作超时息屏，按键唤醒（`screen_off_timeout`）。
- **镜像局部放大（跟随鼠标）**：`zoom_enable`/`zoom_scale`。
- **数据抓取**（全部后台线程 + 缓存）：天气（wttr.in 免 key）、行情（Binance 免 key）、微博热搜、电池健康度（powercfg）、当前播放音乐（winsdk）、网络延迟（ping 后台）。
- **设置页多标签**：按页面导航 / 通用 / 自动化 / 按键 / 页面内容 / 屏幕镜像 / 数据管理。
- 农历算法（`solar_to_lunar` 及辅助函数，1900~2100 数据表）。

#### v4.1.0
- 内部微调（无显著结构变化）。

#### v4.2.0
- 跑马灯新增**字体文件/字号**设置（`marquee_font`/`marquee_font_size`）+ **跑马灯实时预览**（所见即所得，`update_marquee_preview`）。
- 天气支持中文城市名（如 北京 或 Beijing）。

#### v4.3.0
- 「页面内容」设置改**分页标签**管理：文字跑马灯 / 天气与行情 / 时间 / 纪念日与待办。
- 跑马灯**字体颜色**设置（`marquee_color`，调色板）。
- 进程 TOP 显示数量（`proc_count`）；世界时钟时区配置（`clock_zones`）；硬件详情显示数量（`hwdetail_max`）。
- **仪表盘初版**：CPU/内存/磁盘 显示开关 + 颜色（`gauge_show_cpu/mem/disk`、`gauge_cpu/mem/disk_color`）。
- 新增「监控显示」设置子页。

#### v4.3.1
- **仪表盘补全**：CPU 温度、GPU（显示开关 + 颜色）。
- **硬件详情监控类型**：Temperature/Fan/Voltage/Load/Power 勾选（`hwdetail_types`）。
- 热搜刷新时间设置（`hotsearch_interval`）。
- **设置「按页面」导航**：选择页面查看设置项说明并可一键前往（`on_guide_select`/`goto_guide_setting`）。
- 传感器按类型自动识别（`_find_sensor_value`）。

#### v4.3.2
- **仪表盘补全**：GPU 温度、风扇、上传、下载（显示开关 + 颜色，`_make_gauge_row` 起雏形）。
- 网络速率采样（`get_net_speed`）。
- **串口渲染事务标志** `serial_busy`：帧/页面发送期间阻止按键 ADC 轮询插入，防命令流交错导致画面倾斜。

#### v4.3.3
- 监控显示内再**分子标签页**（进程 / 硬件详情 / 仪表盘），界面不再拥挤。
- 仪表盘用 `_make_gauge_row` 统一生成行（标签 + 勾选 + 颜色 + 调色板）。

#### v4.4.1 修复双屏问题
- **传感器自由选择**：硬件详情多选 / 仪表盘单项指定任意 LibreHardwareMonitor 传感器（`list_sensors`/`_open_sensor_picker`/`_resolve_gauge_sensor`），支持「自动检测」。
- 传感器按 **HardwareType 类型枚举**直接区分 CPU/GPU（`_hw_type_name`），无数据时显示 `--`（避免 0 误导）。
- **修复多屏只识别 1 个**：daemon 端口遍历时每处理一个端口前实时更新连接状态（第二端口误用主设备对象覆盖第一设备串口的问题）。
- 串口写入异常等待后重试一次。
- 提示：主板传感器（CPU 温度/风扇）需以管理员身份运行才能读取。

#### v4.5.1
- **多屏独立配置**：每个屏幕按 USB 序列号独立保存设置（`config/MSU2_MINI_<serial>.json`）。
- 多设备 SN 相同时按 COM 端口区分；SN 为空也按端口区分（`device_config_path`/`load_device_config`）。
- 配置文件集中到程序目录 `config/` 子目录，旧配置自动迁移（保留根目录原件备份，`migrate_old_config`）。
- **UI 锁定到当前设备**：`_ui_set_active`/`set_active_device_config`，所有 UI 操作锁定到当前选中设备，互不干扰。
- 页面/方向切换基于本设备自身配置（避免多屏联动）。
- `_apply_settings_ui_to_config`/`_apply_main_ui_to_config`：切换设备时批量刷新各页控件。
- 设备掉线自动重连（清除 known_com_ports）、LCD 指令失败容错重试。
- 修复多屏下网络速率/硬件详情等页面首次渲染崩溃。

#### v4.5.2
- **热搜独立设置页**：每页显示条数 / 抓取总条数（多于一页自动翻页）/ 字体自动适配屏幕 / 长文本自动滚动字幕 / 翻页间隔 / 自动刷新（`hotsearch_*` 系列配置）。
- 跑马灯新增**滚动速度**（`marquee_speed`）。

#### v4.5.3
- **设置更改实时生效**：天气城市/行情交易对/延迟目标改动后立即刷新数据并重绘，不再等待缓存刷新（`_refresh_page_now`）。

#### v4.5.4 基本修复大部分问题
- 热搜**字体自动适配屏幕大小**（按最长文本缩小字号，最小 8）；长文本滚动字幕、滚动速度可调。
- 热搜/跑马灯**居中布局**；字体/尺寸/速度等调整后小屏实时同步生效。

#### v4.5.5 取消自定义字体
- 跑马灯**字体固定为黑体**（移除字体选择，避免字体路径失效）；字号/颜色/速度/文本仍可调。
- 资源查找自动回退 `resource/` 子目录（`_get_resource`）。

#### v4.5.6 增加硬件信息
- 新增**「设备信息」标签页**：USB 连接信息 / 固件版本 / SFR 寄存器 / Flash 芯片与分区 / 本机系统信息（`refresh_hw_info`，含「刷新」按钮）。
- Flash 布局优先从 `device_protocol.json` 读取，失败用内置默认（`_load_flash_layout`）。
- 修复中文字体显示：`simhei.ttf` 自动回退到 `resource/`，跑马灯/时钟等中文不再变方块。
- 编译脚本加入 winsdk（播放音乐）与 device_protocol.json。

#### v4.5.8（含 v4.5.7 内容）
- **磁盘读写设置分页管理**：经典模式 / 经典2样式 / 网速样式 三模式，切换显示模式自动跳转对应标签（`diskio_mode`）。
- 经典模式：标题可选 + 字号自适应/手动 + 读/写行颜色可配。
- **经典2 样式**：布局与字体大小和网络流量一致（读/写标签），颜色跟随网络流量页面当前配色。
- 网速样式：读/写数值独立颜色 + 读/写柱状图颜色（`_show_diskio_netspeed`）。
- **网络流量监控**：上传/下载文字颜色、柱状图颜色全部可自定义；新增「经典/自定义」显示模式（`netspeed_mode`）。
- 通用两行速度渲染 `_render_two_line_bars`（网络流量与磁盘经典2 共用）。
- 修复：启动/切换设备后页面、显示方向下拉框自动恢复为该设备上次的选择；「按页面」导航记住上次选择的页面（`guide_last_page`）。

#### v4.5.9 +色系
- 新增**「配色方案」系统**：内置 260+ 个色系（马卡龙/莫兰迪/美拉德/赛博朋克/侘寂/大地/孟菲斯/极简/复古/霓虹/传统国色/艺术流派/色彩理论等）。
- 配色方案管理子页（「监控显示 → 配色方案」）：实时预览色块，可新增/编辑/删除自定义方案（按设备保存，`custom_color_schemes`）。
- **一键套用**：网络流量、磁盘读写（经典模式/网速样式）、仪表盘设置区新增「配色方案」下拉，选择后按顺序套用到各颜色（`_make_scheme_row`）。

#### v4.5.10
- **配色方案改为独立标签页**（设置 → 配色方案），放在「监控显示」之前，入口更显眼。
- 交互升级：选择方案**只提供候选色板**，不再自动套用；每个颜色位置后新增**「色块下拉」**（`_make_swatch_button`），可直接看到并挑选该方案的颜色。
- 支持**混合配色**：不同位置可分别从不同方案的色板选色，点「存为新方案」一键保存为自定义方案（`_save_current_scheme`）。

#### v4.6.0 —— 引入本地 API 服务器
- 新增「API投屏」页面与**本地 API 服务器**（HTTP + WebSocket，默认 `127.0.0.1:8632`），供其他程序自定义投屏内容。
- 投屏接口：整帧图像（image/rgb888/pixels/原始字节流）、文本、清屏、切页、窗口投屏；可配置端口与访问令牌。
- **OpenAPI 3.0 JSON 规范**（`/api/openapi.json`，别名 `/api.json`、`/api/openapi`）；启动自动生成 `api_openapi.json`，并自动校验端点与文档同步（`_check_openapi_sync`）。
- 网页投屏控制台（分页标签：投屏 / 选择程序 / 文档）：文本、图片（单张/多图轮播+间隔）、选择程序窗口投屏、停止投屏。
- 多屏投屏：各接口支持 `device` 参数指定目标屏，每屏独立显示；新增 `/api/devices` 列出多屏。
- 图片投屏显示方式：自适应（contain）/ 拉伸（stretch）/ 填充（cover）。

#### v4.6.1 —— 多协议接入
- 新增多种本地接入协议：TCP Socket（JSON 行，端口+1）、UDP（JSON 报，端口+2）、热文件夹（`hotfolder/` 放入图片/文本即投屏，文件名 `屏幕1_xxx` 指定目标屏）、Windows 命名管道、Unix Domain Socket、ZeroMQ（端口+3，需 pyzmq）、stdin/stdout 管道、SSE 事件流（`/api/events`）。
- **统一命令执行器** `api_execute_command`：所有协议命令格式与 HTTP/WebSocket 完全一致（type 为 screen/text/clear/slideshow/stop/page/mirror，支持 device 指定目标屏）。
- 设置页「API接入」显示全部协议地址；网页控制台文档、/docs 说明、OpenAPI 描述同步更新。

#### v4.6.2 —— API 信息/控制接口
- **信息/查询接口**：`/api/health` 健康检查、`/api/version` 版本、`/api/protocols` 列出全部接入协议与地址（自动发现）、`/api/status` 综合运行状态、`/api/config` 读取配置、`/api/screenshot` 获取当前屏画面（PNG+RGB888）。
- **控制/配置接口**：`/api/page/next`、`/api/page/prev` 翻页；`/api/key` 模拟按键；`/api/orientation` 设置 LCD 方向；`/api/marquee` 跑马灯投屏；`/api/config/set` 白名单改配置（40+ 字段，立即保存）；`/api/device/select` 切换活跃屏；`/api/device/refresh` 刷新设备；`/api/notify` 状态栏通知；`/api/quit` 退出程序（需 force）。
- 全部新命令同步加入统一命令 type 分发，TCP/UDP/WebSocket/命名管道/热文件夹/stdin 等所有协议均可用；OpenAPI 增至 31 个端点，文档同步。

#### v4.6.4（含 v4.6.3 内容）
- **「屏幕序号检测」**：触发后所有副屏同时显示各自屏号（屏幕1/屏幕2…），显示时长后自动恢复原画面（如热搜）。
  - 触发方式：设置 → API接入「检测屏幕」按钮、网页控制台「🔢 屏幕序号检测」按钮、API `/api/screen/id` 及统一命令 `screen_id`。
  - 显示时长可配置（1~300 秒，默认 5 秒）；检测期间不改变页面状态（`_api_try_screen_id`）。
- **「强制投屏」**：设置 → API接入 可勾选（`api_overlay`），未选择「API投屏」页也可投屏（任何页面都会被投屏覆盖）；停止投屏后自动返回投屏前的页面；按设备保存，多屏各自独立（`_api_try_overlay`）。
- `/api/config` 与 `/api/config/set` 白名单新增 `api_overlay` 字段；文档与 OpenAPI 同步更新。

#### v4.7.0
- 修复：设备连接后设置页/主控页控件**自动同步为设备已保存的配置**（此前强制投屏等设置可能显示为默认值，造成「设置没保存」的错觉）。
- 修复：退出程序时**等待配置写盘完成**再结束，避免修改后立刻关闭导致设置未写入文件。
- 优化：摄像头列表改为**后台枚举**（启动不再卡顿）；摄像头/窗口下拉框点击后台刷新（`load_cameras_async`/`apply_cameras`）；「检测屏幕」与设备信息「刷新」改为后台线程执行串口读取，界面不卡顿（`_collect`/`_apply`）。

#### v4.7.1 未分页管理屏幕前的备份
- 修复：切换屏幕时 **API 服务器被反复重启**（端口/令牌控件刷新误触发重启）——现仅同步配置、不重启服务器。
- 修复：切换设备时批量刷新控件**不再重复写盘**，消除切换卡顿；刷新操作锁定到 UI 当前选中屏。
- 修复：**多屏页面串扰**（如 屏幕1流量监控 → 切到屏幕2后屏幕2也变成流量监控）——渲染按各屏自己的运行时页面显示，切换设备时页面同步不再经过全局配置对象。
- 修复：切换设备后相机下拉框/屏幕镜像窗口下拉框不再残留上一屏幕的值（找不到时显示为空）。

### 阶段四：5.x —— PySide6 重构 + 电视墙（2026-08-16 ~ 08-18）

#### v5.0.0（含 v4.8.0 内容）—— Tkinter → PySide6 重大重构
- **重大重构**：主界面由 Tkinter 迁移到 **PySide6 (Qt 6.11)**，界面更现代、布局更规范；业务逻辑层（串口/渲染/配置/API）全部复用。
- **新界面结构**：第一层「主控 | 关于」；主控页内按屏幕分标签（屏幕1/屏幕2…）；每屏标签内再嵌套「主控 | 设置 | 设备信息」三个子页，每屏一套独立控件。
- **主控子页**（`_build_main_tab`，精确复刻老版 4.7.1 布局）：设为自动连接、烧写区 4 行（闪存固件/相册/背景/动图 + 选择/烧写）、文字颜色 R/G/B 滑块 + 色块 + 预设颜色下拉、填充/适应单选、动图间隔、最大 FPS、自定义内容按钮、▲上翻/▼下翻、方向/页面下拉、相机下拉（后台枚举）、屏幕镜像窗口下拉、屏幕分辨率 + 检测屏幕、实时预览。
- **每屏一套主控控件**（v4.8.0 引入，PySide6 保留）：多屏时主控页按屏分标签，彻底隔离各屏状态；主控标签、设置标签、设备信息标签、顶部设备下拉框四处双向联动（`_activate_by_name`）。
- 自定义显示（Qt 化完整版）：显示多项数值 / 显示两项图表 + 模板编辑 + 命令插入 + 实时预览 + 示例模板 + 查看错误；图形化编辑（命令列表 + 添加/编辑/删除/上移/下移 + 每种命令的 QDialog 表单）。
- 设置页（`_build_settings_tab`）：通用（防烧屏/实时预览/开机自启/界面语言）、自动化（自动翻页/息屏超时）、按键（单击/双击/长按）、API 接入（启用/端口/令牌/强制投屏 + 应用并重启）、数据管理（导出/导入配置）、页面内容（跑马灯/天气行情/热搜/时间/纪念日待办）、监控显示（进程/硬件详情/仪表盘/磁盘读写/网络流量）、屏幕镜像（放大镜）、配色方案（方案选择/预览/增删改）。
- 设备信息页（`_build_hw_tab`）：连接信息 / SFR 寄存器 / Flash 芯片 / Flash 分区 / 系统信息。
- 托盘（隐藏/显示/退出）、状态栏（设为自动连接 + 设备状态 + 隐藏按钮）、关于页。
- 原 Tkinter 版保留为 `MSU2_MINI_V2 - 4.7.1 未分页管理屏幕前的备份.py` 可回退。

#### v5.0.1
- **多屏隔离修复**：截图/处理/FPS 线程改用本设备最新配置（`cfg = dev.config` 移入循环内，设备重连替换 dev.config 后立即用上）。
- `SER_Read` 底层瞬时错误（串口被并发关闭/句柄异常）容错返回 0。
- **相机/窗口下拉跨线程桥** `_ComboBridge`：后台线程经信号回主线程填充（`QTimer.singleShot(0,...)` 在后台线程不触发导致下拉框一直为空的问题）。
- **预览性能优化**：numpy 直接构造 QImage（跳过 PIL），实测 1600×900 全尺寸 44ms→19ms；刷新 200→500ms 降频，主线程负担大幅降低（修复「控件点不开」）。
- **设置/设备信息懒加载**：首次点开标签才构建（`_on_inner_tab_changed`），启动更快。
- 后台线程（daemon/load/ping/manager）启动顺序与老版 4.7.1 一致。

#### v5.0.2 完全升级到pyside6
- 完成 PySide6 迁移收尾（版本号 5.0.2，彻底脱离 Tkinter 依赖）。

#### v5.0.3
- `sync_page_combobox` 优化：仅当下拉项集合变化（如切换语言）才重建 items，否则只更新选中项——避免下拉框频繁刷新/打断选择。
- **设备信息跨线程桥** `_HwBridge` + 防 GC 容器 `_keepalive`（修复「设备信息页空白」）。
- 设备连接继承页面改用本设备自身配置（修复多屏「串台」+ 下拉频繁刷新）。

#### v5.1.0 +电视墙
- 新增**「电视墙」标签页**（第一层：中控 | 电视墙 | 关于）；第一层原「主控」更名「中控」，避免与第三层「主控」子页同名混淆。
- 电视墙包含**「显示墙」**（按设置的行×列网格实时显示所有小屏预览）与**「显示墙设置」**（根据已连接屏幕数量动态提供快速布局——横向/纵向排布，行/列可手动微调，布局可保存）。

#### v5.1.1
- 修复：显示墙设置无法自动保存/恢复——原写入全局配置会被多屏渲染线程切换导致落错文件，现改为**独立配置文件 `config/MSU2_MINI_wall.json`**（原子写入），下次启动自动恢复。

#### v5.1.2
- 修复：显示墙布局仍无法在下次启动恢复——启动时屏幕尚未连接，保存的布局会被立即重置为默认并被微调框范围钳制。现改为将保存值作为**期望布局缓存**，待已连接屏数足以容纳时自动恢复（屏幕渐进连接时生效）。

#### v5.2.0 优化显示墙
- 显示墙性能优化：每帧先 **numpy 降采样**到格子尺寸再显示（单屏处理约 19ms→1~2ms）；**仅在显示墙激活且窗口可见时渲染**（切走/最小化零开销）；刷新间隔降为 1 秒——小屏数量多时不再卡顿。

#### v5.3.0 —— 主程序当前最终版（2026-08-18）
- 显示墙新增**画布渲染方式**（`_WallCanvas`，QPainter 按行列统一绘制，比 N 个 QLabel 控件更省资源、更流畅）；显示墙设置新增「显示方式」：控件 widget / 画布 canvas。
- **所有设置持久化**：窗口大小/位置/最大化状态与上次停留的第一层标签在退出时保存、启动自动恢复（`config/MSU2_MINI_ui.json`）；退出时强制保存显示墙设置。
- **（主程序最终版新增，未单独编号）** 配色方案全面覆盖所有改颜色处：
  - 监控显示各页（进程、硬件详情、仪表盘、磁盘读写经典/网速样式、网络流量）每个颜色位置后加「色块下拉」+「配色方案行」+「存为新方案」。
  - 主控页文字颜色（RGB 滑块）加入色块按钮 + 方案下拉 + 存为新方案。
  - 页面内容（跑马灯、天气与行情、热搜、时间、纪念日待办）全部支持可配置背景色 + 字体色（新增 20 个颜色配置字段：`marquee_bg_color`/`weather_bg_color`/`weather_text_color`/`crypto_text_color`/`hotsearch_bg_color`/`hotsearch_text_color`/`time_bg_color`/`timer_text_color`/`clock_text_color`/`memo_bg_color`/`memo_text_color`/`todo_text_color`/`proc_bg_color`/`proc_text_color`/`hwdetail_bg_color`/`hwdetail_text_color`/`gauge_bg_color`/`gauge_label_color`/`diskio_bg_color`/`netspeed_bg_color`）。
  - 渲染侧统一使用 `_hex2rgb()` 把配置颜色转为 RGB，Image.new 用背景色、draw.text 用前景色。
  - 「经典方案」色块：方案下拉初始选中后候选色立即载入色块菜单；配色方案管理页渐变预览下新增「色块:」行（点击复制 hex 到剪贴板）。

#### v5.4.0 —— 配色方案对话框友好化 + 修复多屏空白（2026-08-18）
- **新增：配色方案编辑对话框友好化**（`_scheme_dialog` 重写）：已选颜色以色块列表展示（点击移除，带计数/「调色盘选色...」/清空）、常用色板点选、从其他配色方案（下拉选方案）点选添加颜色。
- **修复**：新增/自定义的配色方案，在「设置 → 配色方案」管理页再次选择时预览渐变与色块**空白**的问题。
  - **根因**：管理页的 `_draw_preview`/`_refresh_schemes` 及新增/编辑/删除（`add_custom_scheme`/`edit_custom_scheme`/`del_custom_scheme`）都直接用**全局 `config_obj`** 读写 `custom_color_schemes`；而 daemon 渲染线程会在**多屏之间切换全局 `config_obj`**。当用户选择方案时若 daemon 恰好把 `config_obj` 切到另一台设备（该设备配置里没有这个自定义方案），就读不到 → 预览/色块空白（增删改还可能作用/保存到错误屏）。
  - **修复**：管理页统一改用绑定设备的 `_cfg()`（`dev.config if dev.config is not None else config_obj`）读写自定义方案（与监控设置区 `_on_select`、主控页 `_on_main_scheme_select` 一致）；监控设置区「存为新方案」`_save_current_scheme` 由 `_ui_set_active()`（锁 UI 活跃屏）改为 `set_active_device_config(dev)`（锁绑定屏），保证保存写对本屏配置文件。
  - 已离屏验证根因：全局 `config_obj` 切到无该方案的屏时 `get_all_color_schemes` 读不到（→空白）；改用绑定设备配置后能读到。

#### v5.5.0 —— 修复电视墙布局被自动重置为推荐值（版本号正式升级到 5.5.0）
- **修复**：显示墙设置布局在程序运行期间被自动重置为推荐值（如 2行×1列 → 2行×2列）。典型场景：设好布局后最小化程序，较长时间后恢复，发现布局已变。
  - **根因**：`_wall_tick` 每秒比对设备集合变化。程序最小化期间某台屏短暂掉线重连（USB 挂起/重扫），设备集合 2→1→2 翻转两次，各触发一次 `_wall_refresh_options()`。掉线时（n=1）`wall_rows_sb.setRange(1,1)` 把当前值强制 clamp 成 `1×1`；重连后（n=2）`_wall_refresh_options` 读到被 clamp 后的 `(1,1)`，因不满足「行×列≥屏数」而被重置为推荐值——2 台屏按「最接近方形」排序，推荐值恰为 **2×2**。且 `setRange` clamp 会触发 `valueChanged`，误触发 `_on_wall_rc_changed` 把掉线时的临时值（1×1）写进 `MSU2_MINI_wall.json`，进一步破坏配置。
  - **修复**：新增 `_wall_layout` 状态（用户当前布局，与 spinbox 范围 clamp 解耦）——手动调整（快速布局/行列微调）时更新；`_wall_refresh_options` 决定布局时依次按 `_wall_expected`（启动恢复）→ `_wall_layout`（运行中保留）→ 推荐值（仅屏数不足时的临时值，**不覆盖** `_wall_layout`/`_wall_expected`），屏数足够后自动恢复用户布局。同时把行/列 spinbox 的 `blockSignals` 提前到 `setRange` 之前，杜绝 clamp 误触发保存。

#### v5.6.0 —— 修复编译后 exe 启动崩溃（2026-08-19）
- **修复**：Nuitka 编译生成的 exe 启动即崩溃（退出码 1，且 `crash_log.txt` 无记录——崩溃发生在主流程 try 之外）。
  - **根因**：主文件残留 `from PIL import Image, ImageDraw, ImageFont, ImageOps, ImageTk`，其中 `PIL.ImageTk` 内部会 `import tkinter`；Nuitka 编译启用 PySide6 插件后会**主动排除 tkinter**，导致 exe 启动加载 `PIL.ImageTk` 时报 `ImportError: Module 'tkinter' was actively excluded from Nuitka compilation`。py 直接运行不受影响（开发机装有 tkinter），因此表现为「调试正常、exe 运行不了」。
  - **修复**：从导入中移除 `ImageTk`（代码中无任何 `ImageTk.*` 实际调用，纯残留），并在导入行加注释防止回归。已用重定向 stderr 确认根因、重新编译并离屏验证新 exe 可正常启动。
  - **提醒**：Nuitka 编译时同一 `build_output` 目录**不要并发运行两个编译进程**（会触发 `AssertionError: ...module.HardwareMonitor.c` 冲突崩溃）；编译命令需在项目目录下执行（否则相对路径 `resource\icon.ico` 找不到）。

#### v5.38.0 —— 单位色默认五种不同颜色（马卡龙色系）+ 设置里可直接选「色系」（2026-09-22）

**用户需求（原话）**：「默认这五项就应该不同的颜色，默认采用马卡龙色系，也可以直接选择色系，不同色系就直接设置默认的 5 种颜色。」

**问题**：v5.37.0 只有 `MB` 档有颜色、其余四档留空（留空 = 用该行原色），所以设置页打开就是「五格全是同一颜色」（空值回退显示成该行 MB 色），想逐柱变色得手动一个个填。

**做法**：
1. 新增 **色系表** `SPEED_UNIT_SCHEMES`（8114，**7 套**）：马卡龙（默认）/ 糖果亮彩 / 暖色（红橙黄）/ 冷色（青蓝紫）/ 高对比（暗底最醒目）/ 单色·青绿深浅 / 单色·橙金深浅。每套 = `(名称, 上行/读五色, 下行/写五色)`，五色顺序与 `SPEED_UNIT_LABELS` 一致 = `<1K(B)`/`KB`/`MB`/`GB`/`TB`。马卡龙默认：上行 = `#f7a1b5`/`#ffc09f`/`#ffe08a`/`#9fe2bf`/`#a4d8f0`，下行 = `#d3bce8`/`#7ec8e3`/`#a8e6a1`/`#f6a192`/`#c8a2c8`。
2. **默认值由色系表派生**：`sys_config.__init__`（10104 区）用 `SPEED_UNIT_SCHEMES[0]` 循环写入 10 个字段 → 开箱即「五档五色 + 上行与下行同单位也不同色」；`SPEED_UNIT_BAR1_DEFAULT`/`SPEED_UNIT_BAR2_DEFAULT`（8138/8139）也改由色系表派生（名字保留，门槛默认颜色/兼容用）。**想改默认观感只改色系表即可。**
3. 新增 `_speed_scheme_colors(name)`（8143，按名取色）与 `_speed_current_scheme()`（8151，按当前 10 色**反查**色系名，对不上任何一套 → `SPEED_UNIT_SCHEME_CUSTOM`「自定义（手动调整）」）。
4. 设置页（设置 → 监控显示）新增 **「色系:」下拉框**（13186）：项目 = 7 套预设 + 「自定义（手动调整）」；选中某套 → `_apply_scheme`（13205）把 10 个颜色一次性写入 + 同步刷新表格色值框 + `save_config()` + 信息框提示；单格手改（`_commit`）后 `_refresh_scheme_combo`（13195）自动切回「自定义」。**先 `setCurrentIndex` 再连信号**，避免构建时误写配置。
5. 修掉一个小别扭：色值框改为**显示配置里的真实值**（原来空值会回退成该行 MB 色，看着像没清掉）；「×」清空仍 = 该单位用该行原色；老配置文件里已存的颜色不受影响。
6. 验证：`_scaffold/smoke_test_speed_unit_color.py` 扩到 **116 项全过**（新增：默认 5 色互不相同、上下行同单位不同色、色系反查（含「自定义」）、逐档默认取色、默认色下的像素级断言、磁盘读写·网速样式同样生效、UI 色系下拉框（初始回显 / 选中即写入 10 色 / 落盘 / 手改后变「自定义」/ 切回马卡龙））。

#### v5.37.0 —— 速度换色升级为「单位色 + 换色门槛」（不同单位不同色；门槛可随意增删，大于/小于/等于多少是什么颜色）（2026-09-22）

**用户需求（原话）**：「KB 是默认色，少于 1K 的一个色，兆的一个色，G 的一个色，反正就是不同的单位采用不同的颜色。还有设置里面再增加一个换色门槛的，就是大于多少的时候是什么颜色，小于多少的时候是什么颜色，并且可以设置多个条件，就是大于多少多一个色，大于多少多少一个色，小于多少多少一个色，小于多少多少一个色，就是大于小于这种条件可以随意增加删除，可以很多条这种类似的条件，还有等于多少的时候是什么颜色等等。」

**做法**：把「一根柱子什么颜色」从写死的「MB 判定」升级为一个**逐柱求值的优先级链**，并在设置里给出完整的编辑界面。

1. **取色优先级（逐根柱子、每个采样值各算一次）**：
   ① **换色门槛**（用户自定义条件，按列表**从上到下**匹配，**第一条命中即用**）
   → ② **单位色**（`<1K(B)` / `KB` / `MB` / `GB` / `TB` 五档，**留空 = 用该行原色**）
   → ③ 该行**原始柱色**。上行/读（`bar1`）与下行/写（`bar2`）各自独立一套。
2. **门槛条件**：运算符 `大于 / 大于等于 / 小于 / 小于等于 / 等于 / 不等于`；数值 + 单位（`B/KB/MB/GB/TB`，**1024 进制**，与文字显示单位同口径）；适用范围可选「两行都适用 / 仅上行·读 / 仅下行·写」；颜色。条数**不限**，可随意增删（设置里「＋ 添加条件」/「删除」）。
   - 「等于」按 **±0.5% 容差**（目标为 0 时容差自动为 0 → 精确比较），因为流量是浮点采样值、很少严格等于。
   - 容错：**颜色留空**、运算符非法、数值非法、非字典条目、`speed_color_rules` 不是列表 —— 一律**安静忽略**（不会崩、也不会误染色）。
3. **代码实现**（`MSU2_MINI_V2.py`）：
   - 删除 v5.36.0 的 `_speed_unit_palette()` / `_speed_unit_bar_color()`，改为：
     `SPEED_UNIT_KEYS`(8081) / `SPEED_UNIT_LABELS`(8082) / `SPEED_UNIT_SCALE`(8083) / `SPEED_RULE_OPS`(8084) / `SPEED_RULE_ROWS`(8086) / `SPEED_RULE_EQ_TOL`(8087)、
     `_speed_unit_key(value)`(8090，与 `sizeof_fmt(base=1024)` 的切换点一致)、`_speed_unit_field(row,unit)`(8107，MB 沿用老字段名 → **老配置继续生效**)、
     `_speed_rule_hit(op,value,target)`(8114)、`_speed_rule_unit_scale(unit)`(8137)、`_speed_rule_list()`(8142)、`_speed_parse_color(raw)`(8154，空→None、支持 `#rgb` 简写、非法回退白)、
     **`_speed_bar_color_resolver(row_key, base_color)`(8165)**：每帧每行只构造一次（预解析规则与单位色），返回一个 `f(value)` 供柱循环逐柱调用 —— **不逐柱读配置**；开关关闭时直接返回「恒原色」的函数（零开销）。
   - 两个渲染点改接解析器：`_render_two_line_bars`(8214，网络流量 / 磁盘读写经典2 共用) 与 `_show_diskio_netspeed`(15478，磁盘读写网速样式)。
   - `sys_config` 新增字段（`__dict__` 自动落盘/恢复）：`speed_unit_bar{1,2}_{b,kb,gb,tb}_color`（8 个，默认 `""`，见 10029~10036）+ **`speed_color_rules = []`**(10040)；原有 `speed_unit_color_enable`/`speed_unit_bar1_color`/`speed_unit_bar2_color`(10025~10027) 保留不动。
4. **设置界面**（设置 → 监控显示）：
   - 开关文案改为「**开启逐柱换色**（按「换色门槛 → 单位色」给每根柱子上色，未命中的用该行原色）」。
   - 新增 **2×5 单位色网格**：行 = 上行/读、下行/写；列 = `<1K(B)`/`KB`/`MB`/`GB`/`TB`；每格 = 色值输入框 + 「…」取色（`QColorDialog`）+ 「×」清空（清空即该单位用原色）；改动即 `_lock()` → 写配置 → `save_config()`。
   - 新增 **换色门槛编辑器**（`QGroupBox` + `QScrollArea`，最多 170px 高）：每条 = 序号 + 适用范围下拉 + 条件下拉 + 数值 + 单位下拉 + 「时 →」 + 颜色框 + 「…」取色 + 「删除」；底部「＋ 添加条件」。新增/删除即时写配置并**重建列表**（`setParent(None)` + `deleteLater` 断亲，避免残留控件）；下拉框/文本框改完（`currentIndexChanged`/`editingFinished`）即存，**不逐字符写盘、不弹提示刷屏**。
   - **本页套了一层滚动容器**（`QScrollArea` + `setWidgetResizable(True)` + 无边框；`_build_monitor_settings` 13036）：内容变多后小窗口可滚动，不会把下面的子页签（进程/硬件详情/…）裁掉；窗口够大时看不出差别（`_scaffold/measure_monitor_page.py` 用于量高度）。
5. **验证**（`_scaffold/smoke_test_speed_unit_color.py`，**90 项全过 / 0 失败**）：
   - 单位划分边界（b/kb/mb/gb/tb）、`_speed_unit_field` 向后兼容、颜色解析容错；
   - 单位色 5 档逐档断言 + 「只配上行色时下行不受影响」+ 「清空后回落原色」；
   - **像素级**：`[500]*20 + [200K]*20 + [5M]*20 + [1G]*20` 四段，第 5/25/45/65 根柱子分别是 B 色/KB 色/MB 色/GB 色（同一行 4 种颜色同屏）；
   - 6 种运算符（含 `>=`/`<=` 边界、`==` 容差内外、`!=`）、**顺序第一条命中即用**、行范围（仅上行/仅下行）、单位换算（1MB ≡ 1024KB）、颜色留空/非法运算符/非法数值/坏列表容错、**规则优先于单位色**；
   - **像素级**：门槛颜色真的画到柱子上（`#123456`）；磁盘读写·网速样式走同一条规则链（`#654321`）；
   - UI：开关文案、2×5 网格存在（表头 + 行名 + MB 默认色）、「＋ 添加条件」→ 新门槛字段完整 + 出现「删除」+ **落盘**（配置 JSON 里 `speed_color_rules` 长度 1）→ 「删除」后配置清空、按钮消失。
   - 其余 20 个冒烟测试无回归（`smoke_test_multiscreen.py` 仍因另一条并行线引用 `_note_backpressure` 而失败，与本版无关）。

#### v5.36.0 —— 速度单位换色改为「逐根柱子判断」（同一行里只有 MB 的那几根换色）（2026-09-22）

**用户反馈**：「柱状图的柱子现在都是统一颜色，可以不同单位的时候的那条柱子采不同颜色吗？……
我要求的变色不是全部柱状图的柱子都一个颜色，是**发生不同单位的那一条柱子**才用不同的颜色。」

**旧行为（v5.27.0）为什么看着“都是统一颜色”**：当时是拿**当前瞬时速度**去给**整条色带**选色——
只要这一路当前是 MB/s，这一行**所有**柱子（含历史上那些 KB 的采样）全部变成兆色，
所以一眼看去“整行同色”，看不出哪几根是兆。

**新行为（v5.36.0）：逐根柱子判断**
- 该柱**自身数值** ≥ 1MB/s（`SPEED_UNIT_MB_BYTES`，与文字单位口径一致）→ 用该行的**兆色**；否则用**原色**；
- 上行/读 与 下行/写 各有自己的兆色；开关关闭时兆色==原色（等于完全不换色）；
- 效果：**同一行里可以同时出现原色与兆色**——历史采样里哪几根冲到了兆，一目了然。

**实现**：
- `_speed_unit_palette(c1, c2)`（新）→ 返回 `(原色1, 兆色1, 原色2, 兆色2)`（开关关/配置异常时兆色==原色）；
- `_speed_unit_bar_color(value, 原色, 兆色)`（新）→ **逐柱**判定（单根柱子调用一次）；
- `_render_two_line_bars`（网络流量 / 磁盘读写经典2 共用）与 `_show_diskio_netspeed`（磁盘读写网速样式）
  两处柱循环改为逐柱取色；旧的 `_speed_unit_bar_colors()`（整条换色）已删除。
- 设置文案同步（设置 → 监控显示）：勾选框 →「柱子的数值达到 MB/s（及以上）时，该根柱子改用下面的
  「兆色」（逐根判断，其余保持原色）」；提示 →「逐根柱子判断——哪一根柱子的数值达到 MB/s 就换成该行的兆色，
  同一行其余柱子保持原色；上行/读 与 下行/写 各有自己的兆色」。开关与两个兆色仍随设置实时保存（按屏）。

**验证**：`_scaffold/smoke_test_speed_unit_color.py` 扩到 **40 项全过**，新增的像素级逐柱断言：
混值序列（前 40 根 KB / 后 40 根 5MB）下——第 5 根=原色(235,139,139)、第 39 根=原色、第 45 根=兆色(255,159,28)、
最后一根=兆色；全程 KB 时整行原色；开关关时即使混值也不换色；边界 1MB-1 不换、1MB 换；
磁盘读写「网速样式」同样逐柱生效（同一行同时出现原色与兆色）；设置页文案/勾选落盘不变。

#### v5.35.0 —— 根治：行级地址重锚（一次错位最多毁 1 行，整幅斜切在结构上不再可能）（2026-09-22）

**用户追问**：「能不能标本兼治、根除倾斜问题？」

**为什么 v5.30.0 还不够**：v5.30.0 把「帧前后不许有别的命令」做严后，干扰已经很少，**但只要发生一次，
后果仍然是整幅斜切** —— 因为旧版整帧**只在开头下一次地址窗口**，之后全靠设备自己的线性指针推进：

```
[窗口 0,0,160x80]  page0 page1 page2 …… page99        ← 指针一路推进 12800 像素
                      ↑ 这里丢/多几个字节（或被吞进一条命令）
                        → 后面所有页面全部错位 → 画面「从某行起整幅斜切」，只能等下一次整帧重绘
```

**根治思路：让画面的正确性变成「局部」的**（不再依赖长距离指针推进）：

```
每行开头重新锚定：  [窗口 第r行, 行宽160]  row_r 的 128px+32px 两次提交   ← 每行都独立定位
                     ↑ 这一行出问题，下一行的锚立刻把它拉回来
```

**实现（`MSU2_MINI_V2.py`）**
- 新增 `_lcd_row_anchor_bytes(row, width)`：构造「第 row 行第 0 列、行宽 width」的地址窗口字节
  （`LCD_Set_XY` + `LCD_Set_Size(width,1)` + `[2,3,7]` 载入地址，含防烧屏偏移），把写指针**每行重新锚定**；
  窗口宽度固定 = 图像行宽，所以不论设备以「窗口宽度」还是「面板宽度」当步长，结果都一致。
- 重写 `Screen_Date_Process(Photo_data, row_width=None)`：每 `ROW_GUARD_ROWS` 行先下一遍行锚；
  缓冲提交按行对齐切分（行宽 160 → 128 px + 32 px 两次提交），**提交不再跨行**；
  每 128 像素仍保留「背景色 + 只发差异像素」压缩，**画面内容与旧版逐像素一致**。
- 两个调用点都传入行宽：`_safe_send_rgb888`（`row_width=SHOW_WIDTH`）、镜像帧（`row_width=LCD_MAX_X`）。
- 开关：环境变量 `MSU2_MINI_ROW_GUARD=K`（每 K 行重锚一次，默认 1=每行；0=关闭 → 回旧行为，供 A/B）。

**量化证明（`_scaffold/smoke_test_row_anchor.py`，用「虚拟小屏」逐包还原帧缓冲）**

| 场景 | 旧版（整帧一个窗口） | v5.35.0（行级重锚） |
|---|---|---|
| 无干扰 | 0 像素错 | 0 像素错（内容不变） |
| 丢一个「提交」包（设备少收一条「写 N 字节」） | **4389 像素错 / 40 行**（整幅斜切） | **143 像素错 / 仅 1 行**，且**之后每行像素级完美** |
| 丢一个「缓冲写入」包`[4,i,…]` | 2 像素错 | 2 像素错 |
| 插队一条 6 字节命令（按键 ADC 读） | 0 像素错 | 0 像素错 |
| 帧字节流体积 | 18132 字节 | 19572 字节（**+8%**，仍是一次 `SER_rw`） |

**与 v5.30.0 的配合**：v5.30.0 让干扰**很少发生**（事务/静默期/自愈），本版让干扰即使发生也**不会累积**；
两者叠加即「少发生 + 不累积」。剩余不可控项（设备端解析整体错位）需要设备侧流控——见下注。

> **注（并行改动线）**：`version\` 下另有 **5.31.0~5.34.0** 备份，属于**同一天另一条并行改动线**
> （设备侧流控「等待传输完成」/串口单消费者队列/端口操作入工人线程与 NameError 修复/背压不重发与繁忙不判掉线），
> **未合入当前主文件**（当前主文件为 5.30.0 → 5.35.0）。那条线里的「串口单消费者队列」与
> 「设备侧流控」正好补上「让干扰根本不产生 / 让设备告诉我们它忙」，与行级重锚互补；
> 若要合并，需先把它们对 `SER_rw`/帧发送路径的改动与 v5.30.0 的串口事务、v5.35.0 的行级重锚一起核对。
> 另：`_scaffold/smoke_test_multiscreen.py` 已被那条线改成引用 `_note_backpressure`（其 5.34.0 的 API），
> 在未合入前该测试会失败，与 v5.35.0 无关。

**验证**：新增 `_scaffold/smoke_test_row_anchor.py`（**19 项全过**）；`smoke_test_frame_gap`（26）/`smoke_test_serial_transaction`（44）/
`comm_health`/`device_health`/`speed_unit_color`/`static_api`/`power_opt`/`settings_persist`/`ui_persist`/`ui_visibility`/`single_instance`
全部通过；`py_compile` 通过。

#### v5.30.0 —— 倾斜仍在：把串口使用严格化为「事务模型」+ 帧自愈 + 看门狗收敛（2026-09-21）

**用户反馈**：v5.29.0（整帧自带窗口）之后**画面依旧偶尔倾斜**，而且控制台里
「整屏窗口与帧数据之间被插入命令」还在计数（插入的是 `Read_ADC_CH(9)` 按键/心跳读）

```
屏幕1 整屏窗口与帧数据之间被插入命令 b'\x08\t\x00\x00\x00\x00'（累计 1 次…）
校正按键检测阈值为：3741
屏幕1 …被插入命令 …（累计 2/3/4 次…）
```

**结论：v5.29.0 修掉的只是「窗口与数据被拆开」这一条**（现在数据自带窗口，插队不再破坏它），
但**「串口上还有别的命令贴近帧数据」本身就是倾斜的成因**——设备是小缓冲的字节流解析器，
帧是一大串（160x80 ≈ 26KB）字节，帧前/帧后/方向重置前后只要被别的命令打扰（哪怕是 6 字节的 ADC 读），
它就可能把帧解析错位 → 画面倾斜（预览正常），且**残缺的帧会一直留在设备上，直到下一次整屏重绘才恢复**。

**修复（`MSU2_MINI_V2.py`，六条）**
1. **串口事务**：新增 `_serial_begin()`/`_serial_end()`/`_serial_transaction_busy()`
   （`ScreenDevice.serial_busy` + `serial_busy_since` + `serial_quiet_until`）。
   帧发送期间 + 发送后**静默期**（默认 150 ms）内，按键 ADC 轮询、通信心跳、API 一律避让；
   daemon 把「健康心跳 + 整轮页面渲染」整体作为一个事务；
   `_serial_transaction_busy()` 带**陈旧保护**（标志卡死超过 1.5 s 自动忽略，防按键彻底失灵）；
   按键轮询另有 `_key_poll_should_wait()` 的**饿死保护**（1 秒都没轮到就放行一次）。
2. **方向重置后静默**（200 ms）：`LCD_State` 成功后设置 `serial_quiet_until`——设备应用方向需要时间，
   旧流程是「方向重置 → 清屏 → **立刻**写整帧」，这正是最易错位的时刻。
3. **帧自愈**：`_send_frame_with_window()` 在写失败/写不完整时**立刻整帧重发一次**（`frame_resend_count`），
   画面在一帧内自愈，而不是把残缺帧留在设备上「斜到下一次整屏重绘」（失败的尝试不留静默期，
   重发自身也不算静默期违规）。
4. **短写续写**：`SER_Write()` 遇短写不再直接抛异常（旧行为 → 整帧被截断 → 残缺帧），
   改为在 0.6 s 时限内继续写剩余部分；写异常（拔线等）仍立即上抛，保持快速失败。
5. **方向看门狗收敛**：清屏后**只对静态页**（照片/关于/纪念日/待办/农历）强制 `state_change = 1`；
   动态页（网络流量/磁盘读写/系统监控…）每轮本来就整帧重画，不需要——旧代码对所有页面都强制，
   等于每 15 秒额外插一次「清屏 + 立刻整帧」（既黑闪，又正好落在最易错位的时刻），
   这是 v5.26.0 之后倾斜变密集的直接来源。同时周期改由 `LCD_WATCHDOG_SECONDS` 统一（可用环境变量调/关）。
6. **观测**：新增 `serial_quiet_violations`（静默期内仍有命令发出 → 计数 + 限频日志，正常应为 0，
   不为 0 就说明还有绕过避让的发送路径）；设备信息 → 连接 页同时显示「帧空档」与静默违规。

**现场 A/B 开关（环境变量，不改代码即可逐项定位；不设即上述默认行为）**

| 环境变量 | 作用 | 典型用途 |
|---|---|---|
| `MSU2_MINI_NO_LCD_WATCHDOG=1` | 关闭周期方向重置 | 判断「每 15 秒方向重置」是不是元凶 |
| `MSU2_MINI_LCD_WATCHDOG_S=30` | 方向重置周期（默认 15 秒） | 降低重置频率 |
| `MSU2_MINI_NO_KEY_POLL=1` | 关闭按键 ADC 轮询（按键失效） | 判断「串口另有命令」是否主因 |
| `MSU2_MINI_FRAME_QUIET_MS=400` | 帧后静默期（默认 150 ms） | 加大避让窗口 |
| `MSU2_MINI_WRITE_PACE_MS=2` | 大块写限速（默认 0） | 怀疑设备接收缓冲小/解析慢时用 |

**验证**：新增 `_scaffold/smoke_test_serial_transaction.py`（**40 项全过**）——事务语义（期间 busy、
结束后静默、过期自动失效）/静默违规统计/帧写异常→整帧重发/短写续写且内容一致/各路径接线
（按键轮询、通信心跳、daemon、镜像、LCD_State、SER_rw）与看门狗收敛（只对静态页）的源码级断言；
`smoke_test_frame_gap.py`（26 项）与既有 10 个冒烟测试全部通过（无回归）。

#### v5.29.0 —— 画面偶尔倾斜/斜切：整帧「地址窗口 → 数据」之间的空档被别的命令插了队（2026-09-21）

**用户反馈**：小屏画面偶尔出现倾斜（斜切），虽然能恢复，但比以前密集了，问是不是哪里逻辑有问题。

**根因（程序逻辑问题，不是屏坏了、也不是线材问题）**
- 设备是**按字节流解析命令**的：`LCD_Set_XY` + `LCD_Set_Size` + `[2,3,7]`（=「载入地址窗口」）之后，
  必须**紧跟**对应区域的像素数据。
- 旧代码的整帧发送是两段：
  ```
  LCD_ADD(0,0,160,80)      # ① 发窗口命令（SER_rw）
  …画图/取字体/RGB565/Screen_Date_Process…   # ② 几十毫秒的渲染+编码（什么都不发）
  SER_rw(整帧数据, read=False)              # ③ 发像素数据（SER_rw）
  ```
  ①和③之间那段空档，其它线程随时会往同一个串口塞命令：
  **按键/ADC 轮询 `manage_task`（约 20Hz）**、**v5.26.0 新增的每 2 秒通信心跳 `_device_health_tick`**、
  API 投屏/屏号检测/Webhook 叠加等。
- 插入的这条命令打乱了设备的解析对齐 → 帧数据与窗口/分组边界错开 →
  **画面倾斜、斜切**（像素高低字节错开的典型表现，而软件里的实时预览完全正常，因为预览不经过串口）。
  下一次「整屏重绘」（重新 `LCD_ADD` + 整帧）又把对齐拉回来 → 于是表现成「斜一下、又自己好了」。
- **为什么变密集**：v5.26.0 的方向看门狗每 15 秒强制 `state_change = 1`（原本是为了让被清屏的静态页重绘），
  于是「`state_change == 1` → `LCD_ADD` + 整帧」这条**带空档的路径从「只在切页时走」变成「每 15 秒走一次」**；
  v5.26.0 新增的 2 秒心跳又让空档期被插入的概率更高。
- 历史同源：v3.1.2「频繁切换页面/窗口/方向后倾斜（分块写入/排空缓冲）」、
  v3.1.3「ADC 心跳失败误判掉线导致帧写入被截断」、镜像页的 `serial_busy` 注释
  「LCD_ADD与帧数据之间…阻止按键ADC插入，防命令流交错导致画面倾斜」——都是同一个病根，
  之前是「缩小窗口/降频规避」，这次从结构上消除。

**修复（`MSU2_MINI_V2.py`）**
- **`_lcd_window_bytes(x, y, w, h)`（新增）**：只**构造**「地址窗口」命令字节、不发出去
  （含防烧屏偏移 `update_burn_offset()`，与原 `LCD_ADD` 逐字节一致）；`LCD_ADD()` 改为调用它。
- **`_send_frame_with_window(dev, hexstream, w, h)`（新增，核心修复）**：把窗口命令**拼到整帧数据前面**，
  用**同一次 `SER_rw`** 发出——`SER_lock` 内整段写是原子的，任何线程都插不进来，**空档归零**。
- **接入全部整帧发送**：`_safe_send_rgb888()`（网络流量/磁盘读写/系统监控/自定义显示/关于/农历/待办/
  世界时钟/纪念日/硬件详情/仪表盘/DeepSeek 余额/API 投屏/屏号检测/Webhook 叠加…共几十个页面共用）
  与屏幕镜像 `show_PC_Screen()`（按设备实际分辨率 `LCD_MAX_X/Y`）。
- **空档观测（保留诊断能力）**：新增 `ScreenDevice.frame_window_time / frame_gap_count / frame_gap_max /
  frame_gap_intrusions`；`LCD_ADD` 成功后标记「窗口已开、等帧数据」，`SER_rw` 在空档期内发现别的命令要发
  就计数 + 限频日志（10 秒一条，直接打出插入命令的头几字节）；整帧发送前由
  `_close_frame_window_gap()` 统计空档时长（>50ms 记一次、>500ms 限频提示）并关闭标记。
  设备信息 → 连接 页新增「帧空档 N 次（最长 x ms）· 空档内被插入命令 M 次」。
- **原有保护全部保留**：`serial_busy` 串口渲染事务标志、`LCD_ADD` 回显校验与失败判掉线、
  `COMM_FAIL_LIMIT` 容错、防烧屏偏移、逐屏 try/except、方向看门狗与静态页重绘、
  v5.28.0 复位横幅识别、v5.26.0 画面刷新健康度均未改动。

**验证**：新增 `_scaffold/smoke_test_frame_gap.py`（**26 项全过**）——
①窗口字节与旧 `LCD_ADD` 逐字节一致 + 防烧屏偏移仍生效；
②整帧发送时「窗口 + 数据」在**同一条字节流**且中间 **0 字节空档**（整条流 = 窗口 + 完整帧，无截断）；
③空档期内插入命令会被计数、整帧发完后不再计；④发送后 `serial_busy` 复位、画面刷新健康度正常更新；
⑤镜像帧同样自带窗口（按 240×240 等实际分辨率）；⑥空帧/无设备保护、`LCD_ADD` 行为不变；
⑦源码级断言（整帧发送统一出口、两个发送函数内不再有裸的整帧 `SER_rw`）。
回归：`smoke_test_multiscreen`（27）/`smoke_test_ui_persist`（19）/`smoke_test_comm_health`（33）/
`smoke_test_device_health`/`smoke_test_speed_unit_color`/`smoke_test_static_api`/`smoke_test_power_opt`/
`smoke_test_settings_persist`/`smoke_test_ui_visibility`/`smoke_test_single_instance` 全部通过。

#### v5.28.0 —— 屏幕黑了却一直显示已连接：设备复位一直回上电横幅（2026-09-20）

**用户反馈**：屏幕2 又没显示了，控制台一直刷（片段）：
```
Read_ADC_CH failed (第1次，忽略): bytearray(b'\x00MSN01')
LCD towards change failed: bytearray(b'\x00MSN01')
屏幕2 通信失败（第 1/3 次，忽略）
LCD_ADD failed: bytearray(b'\x00MSN01')
Read_ADC_CH 连续失败10次，触发重连
……（以下同样两种错误反复循环，持续几分钟）
```

**根因（两个 bug 叠加）**
1. **设备侧**：COM34 那块屏的固件复位了，之后对每条命令都只回它的**上电横幅** `\x00MSN01`
   （就是开机时主动上报的那串），于是 LCD_ADD / 方向重置 / ADC 读取全部失败，画面不再更新。
2. **程序侧（关键）**：`SER_Read` 里「**只要收到任何字节就把 comm_fail_count 清零**」——
   横幅恰好一直在收 → 失败计数永远回到 0 → **永远到不了 `COMM_FAIL_LIMIT`(3) → 永不判掉线、永不重连**。
   日志里那串永远「第 1/3 次，忽略」就是铁证（永远不会变 1/3 → 3/3）。
   再加上「实时预览是在发送前保存的」，于是用户看到的就是「屏幕黑着 + 显示已连接 + 预览正常」。

**修复（`MSU2_MINI_V2.py`）**
- **`_force_device_reset(device, banner)`（新增）**：上电横幅 `\x00MSNxx` 是设备复位的铁证 →
  **立即** `device.set_device_state(0)`（关串口→daemon 自动重连→重新握手/初始化 LCD），
  不经过 3 次容错；并写信息框「检测到设备复位（上电横幅 …），已按掉线处理，正在自动重连并重新初始化…」。
  保护：只在「`device_state==1` + 非烧写中（`write_path_index==0`）+ 连接稳定 3 秒后（`connected_time`）」才生效，
  避开连接握手阶段的残留横幅与「烧写后设备本来就要重启」的情况。
- **`SER_Read`**：识别横幅 → `_force_device_reset`；其余情况**保留 v5.22.0 的容错**（收到响应即清零），
  不把瞬时失败累积成误判掉线（上一版尝试收紧后，高负载下会把正常屏误判掉线，已在开发中抓出并改正）。
- **`SER_rw` 响应校验**：设备回复的是「**最后一个子命令**」的回显（拼接命令 `LCD_ADD` 回的是末尾的 `2,3`，
  不是整条命令的头两字节——这一点由 `_scaffold/smoke_test_comm_health.py` 抳出）→ 回显正确则 `_note_comm_ok`；
  收到横幅→ `_force_device_reset`；其它无效响应仅限频记日志。
- **`_note_comm_ok` / `_note_comm_fail` / `_looks_like_boot_banner`（新增）**：
  前者清零 `comm_fail_count/adc_fail_count` 并记 `comm_ok_time`；后者把原因写入 `last_comm_note`
  并调 `_note_write_error`（因此这类帧不再被计为「刷新成功」，`device_frame_status` 会显示
  「串口通信异常(N 次)：收到设备复位横幅…」）。
- **日志降噪 `_throttled_print` / `_throttled_dev_print`（新增）**：同类错误（按「设备+错误类型」分 key）
  每 5~10 秒最多一条，替换了 `SER_Read timeout` / `串口读写异常` / SFR 读写失败 / ADC 失败 /
  `LCD_ADD`/方向/`LCD_Color_set`/`LCD_ASCII`/`LCD_GB2312`/`LCD_Photo_wb_MIX` 失败 / 系统监控页写入失败 /
  镜像页写地址窗口失败 / 「通信失败（第 N/3 次，忽略）」等十余处打印——
  设备异常时旧版每秒刷几十行，把控制台与日志刷爆、真正有用的信息反而被淹没。
- **`ScreenDevice` 新字段**：`comm_ok_time` / `last_comm_note` / `comm_note_time` / `reset_banner_count` /
  `connected_time`（`set_device_state(1)` 时记录）。
- **状态文案**：`device_frame_status` 的「串口写入失败」改为「**串口通信异常**」，并优先显示可读原因。

**修复后的行为**：设备复位不再被「默默忽略」——信息框明确提示 + 自动重连重初始化；
若设备一直不恢复（持续回横幅），则一直显示「未连接」并提示「请拔插该屏的 USB 线（设备可能已复位/死机）」，
拔插/设备恢复后自动重新连接并恢复显示。

**验证**：新增 `_scaffold/smoke_test_comm_health.py`（**33 项全过**）——用假串口模拟
`echo`（正常回显，回显末尾子命令头）/ `banner`（只回上电横幅）/ `garbage`（无效数据）三种设备：
①横幅识别（含 `\x00MSNCN`、LCD 回显、超短/空数据的反向用例）；
②正常设备：LCD_ADD 成功、计数清零、连续多次不被误判为通信异常；
③横幅 = 立即掉线（串口关闭可重连）、次数/原因/健康度/状态文本均可读；
④**容错不回归**：非横幅无效响应仍清零、瞬时失败仍要连续 3 次才判掉线；
⑤连接稳定 3 秒内的横幅不当故障，过稳定期则判掉线；⑥信息框提示与限频；⑦日志限频。
回归：`smoke_test_multiscreen`（27，真实 `daemon_task` 双假屏连接/掉线重连）/`smoke_test_device_health`（30）/
`smoke_test_speed_unit_color`（30）/_settings_save（12）/_settings_persist（41）/_power_mode（35）/_power_opt（16）/
_ui_visibility（9）/_ui_persist（19）/_static_api（28）全部通过；主程序 `py_compile` 通过。

#### v5.27.0 —— 速度单位换色：柱状图按兆/KB 自动换色（2026-09-19）

**需求（用户原话）**：「现在都是一个颜色……当速度单位为兆的时候，条是其他颜色的，当单位是 KB 的时候，
条是原来这种颜色的，就是直观一看，就自动，不同的条形颜色就是兆和 KB 的区别，然后在设置增加这个功能的
开关，这个开关也是持续保存的，不用每次开程序都设置一次。」

**实现（`MSU2_MINI_V2.py`）**
- **配置字段（3 个，写在本屏配置文件即自动持久化）**：`speed_unit_color_enable`（0/1，默认 **0=关**，
  不影响现有观感）、`speed_unit_bar1_color`（上行/读 兆色，默认亮橙 `#ff9f1c`）、
  `speed_unit_bar2_color`（下行/写 兆色，默认青绿 `#2ec4b6`）；已加入 `_API_CONFIG_WRITABLE`（可用 API 改）。
- **判定口径与文字完全一致**：`SPEED_UNIT_MB_BYTES = 1024*1024`，`_speed_unit_is_mb(value)`（≥1MB/s 即文字
  显示的 "MB/s"）；**上下行（读写）各自独立判断**（下载 5MB/s、上传 200KB/s 时只有下载柱换色）。
- **配色选择 `_speed_unit_bar_colors(v1, v2, c1, c2)`**：开关关 / 配置异常 / 颜色非法时原样返回（绝不报错、
  不影响原观感）；配置取「当前渲染设备」自己的（`_render_cfg()`，多屏不串）。
- **两个画柱点接入**：共用函数 `_render_two_line_bars`（网络流量全部样式 + 磁盘读写**经典2**样式）
  与 `_show_diskio_netspeed`（磁盘读写**网速样式**）；磁盘读写「经典」模式无柱状图，不受影响。
  *与文字颜色无关：只换柱子颜色，文字仍按原有上/下、读/写配色（一眼看柱子即可分辨单位）。*
- **设置 UI**：设置 → 监控显示 **顶部新增分组**「速度单位换色（网络流量 / 磁盘读写 的柱状图）」——
  勾选框 + 「上行/读 兆色」「下行/写 兆色」两个颜色行（自带调色板与配色方案色块下拉）+ 说明提示。
  ★ **先 `setChecked` 再连 `toggled` 信号**（否则构建时就会用默认值触发一次保存，把用户存的选择冲掉）。
- **保存**：勾选/改色立即 `save_config()`（5 秒防抖 + 快照原子写盘），重启自动沿用；
  （与其他显示颜色一样是**按屏独立**的，两块屏各勾一次即可永久生效。）

**验证**
- 新增 `_scaffold/smoke_test_speed_unit_color.py`（**30 项全过**）：
  ①配置默认值 + 阈值边界（999KB / 1MB-1 字节 / 1MB / 9.5MB / 2GB / None / 字符串）；
  ②配色组合（关、单路兆、双路兆、非法颜色回退白）；
  ③**真实渲染像素级**（桩住 `_safe_send_rgb888` 抓 RGB 图统计颜色）：`_render_two_line_bars` 与
    `_show_diskio_netspeed` 两种画柱路径都确认「KB=原色、兆=兆色、单路各自独立」；
  ④设置页 UI：离屏构建整个 UI（懒加载页签逐一切换）→ 找到开关与两个兆色输入框、初值与配置一致、
    勾选即写入配置并**落盘**（读到 json 里为 1）、取消勾选回 0。
- 另附预览图脚本 `_scaffold/preview_speed_unit_color.py` → `_scaffold/_speed_unit_color_preview.png`
  （5 个场景对比：KB 开关关/开、单路上兆、双路上兆、开关关；像素统计已校：KB=粉/浅蓝各 30240 像素，
  兆=亮橙/青绿）。

#### v5.26.0 —— 修复「一块屏不显示，但显示已连接、预览正常、切页不恢复」（2026-09-19）

**用户现象**：挂机一段时间后屏幕2 像没连接一样不显示；软件里屏幕1/屏幕2 **都显示「已连接」**，主控页**实时预览也正常**；
把屏幕2 的页面切走再切回**也不能恢复**。（历史背景：该屏曾出现 45° 倾斜，后来靠看门狗刷正常了。）

**根因（多条并存，全部修掉）**
1. **daemon 渲染循环一个 try 包住整个 for（致命）**：`daemon_task` 的 `for dev_id, device in list(all_devices.items())`
   里没有逐屏 try/except，任何一块屏的渲染异常都会直接中断整轮循环、被外层 `except` 接走 —— 屏幕1 排在 dict 前面
   已渲染完，所以**只有屏幕2 黑**；旧代码还把该异常只打印在控制台，界面上**毫无提示**。
2. **预览是在发送之前保存的**：`screen_process_task` 入队前、`_safe_send_rgb888` 发送前就写了 `last_preview_rgb` ——
   「预览正常」只能证明截图+编码在跑，**证明不了画面真的发到屏上**。
3. **「已连接」不代表在刷新**：整帧页发送走 `SER_rw(..., read=False)` **不回读响应**，写失败被 `SER_rw` 内部吞掉（只 print），
   而 `set_device_state(0)` 还要连续失败 `COMM_FAIL_LIMIT`(3) 次才生效（前 2 次故意忽略）；更关键的是**按键/ADC 心跳
   `manage_task` 只绑定了主设备一块屏**（全程序只创建一个管理线程）→ **第二块屏没有任何活性检测**。
4. **方向看门狗会清屏且不触发重绘**：状态机里每 15 秒的 `force_lcd_reset` 会重发 `LCD_State`，它成功后
   `LCD_Color_set(0,0,w,h,黑)` **把屏清黑**；而这一步**不设置 `state_change`**，于是静态页
   （照片/关于/农历/纪念日/待办，见 `_static_page_loop` 的内容指纹判断）指纹没变就不再重绘 → **一直黑着**。
5. **截屏/处理线程死了没人管**：`screen_shot_task`/`screen_process_task` 异常退出后没有任何重启机制
   （旧函数 `screenshot_panic` 全工程从未被调用 = 死代码），屏会永久停在最后一帧。

**修复（`MSU2_MINI_V2.py`）**
- **①daemon 逐屏异常隔离**（`daemon_task`，15788）：循环体改为
  `for …: if device_state != 1: continue; try: …健康检查… MSN_Device_1_State_machine() except Exception:`，
  单屏异常只累计该屏 `render_error_count`/`last_render_error`，并把「渲染异常（第 N 次，已隔离）+ 异常末行」
  写入信息框（第 1/10 次与之后每 100 次提示，防刷屏），**绝不影响其它屏**。
- **②画面刷新健康度**（`FRAME_STALL_SECONDS`/`_note_picture_ok`(3934)/`_mark_frame_sent`(3943)/
  `device_frame_status`(3956)，`ScreenDevice` 新字段 1093 起）：`_mark_frame_sent(dev, err_seq)` 比较
  「发送前后的串口写失败序号」，**只有这次真的写成功**才刷新 `last_frame_ok_time`，否则计入 `frame_fail_count`；
  `_note_picture_ok()` 给「成功写明地址窗口（LCD_ADD）/ 图库照片（LCD_Photo）」记账；
  `SER_rw` 失败经 `_note_write_error()`（2014）登记 `write_error_seq/last_write_error/last_write_error_time`。
  状态文本：`画面刷新正常（x.x 秒前）` / `画面停滞 N 秒（无成功刷新）` / `串口写入失败(N 次)：原因` / `尚无画面送出`。
  展示位置：**状态栏**（`_periodic_refresh` 13728，`设备已连接: 屏幕2 · 画面刷新正常（0.3 秒前）`）
  与**设备信息 → 连接**页新增行「画面刷新」「渲染异常」（`_build_hw_tab` 12622，点「刷新」重新读取）。
  注：`LCD_Color_set`（纯清屏）**不**记账 —— 否则「被看门狗清屏后一直没重绘」会被掩盖。
- **③每屏独立健康检查 `_device_health_tick()`**（15730，渲染前调用，保证心跳命令不会插进页面命令序列中）：
  · **通信心跳**：每 `DEVICE_HEARTBEAT_SECONDS`(2s) 对该屏做一次 `Read_ADC_CH(9)`（有问有答），失败累计交给
    `Read_ADC_CH` 的 10 次机制判掉线 → daemon 自动重连 —— **这是非主屏唯一能发现「掉线/无响应」的途径**；
  · **线程看护**：每 `THREAD_WATCH_SECONDS`(5s) 检查截屏/处理线程，为 None 或已退出即 `start_threads()`（幂等）
    并写信息框；
  · **画面停滞提示**：超过 `FRAME_STALL_SECONDS`(20s，按能效等级放大避免静态页低频刷新误报) 没有成功送出画面 →
    信息框提示一次（含状态文本与「切页重绘/重新插拔 USB/重启程序」建议），恢复后自动复位。
    ★ 从未送出过画面时以「健康检查首次看到该屏」为计时基准（`_frame_baseline_time`）——
    避免设备刚连接/刚切页时“还没有首帧”就被误报成停滞（开发中由 `smoke_test_multiscreen` 抳出）。
- **④方向看门狗后强制重绘**（`MSN_Device_1_State_machine` 14091）：`LCD_State` 重置后补置 `device.state_change = 1`，
  让页面重走「`state_change` → `LCD_ADD` + 整帧重绘」分支（与 API `screen_id` 的做法一致），静态页不再黑。
- **⑤息屏互不干扰**：息屏分支里把 `last_frame_ok_time` 跟着推进并复位停滞标志，避免唤醒后误报「画面停滞」。

**排查工具**
- 新增 `_scaffold/run_dev_with_log.bat`：开发运行并把全部输出落到 `_scaffold/run_log.txt`（同时程序目录 `MSU2_MINI_log.txt`），
  挂机复现后按关键字回溯：`Exception in daemon_task` / `渲染异常` / `串口读写异常` / `LCD_ADD failed` / `画面停滞` / `线程已退出`。
- 排查结论：**「显示已连接 + 预览正常」都不能证明画面真的发出去了**；以后遇到同类现象先看状态栏/设备信息页的「画面刷新」。

**验证**：新增 `_scaffold/smoke_test_device_health.py`（**30 项全过**）——
①健康度：写成功才记时间/写失败不得记成功/状态文本四态/`device_frame_status(None)`；
②健康检查：心跳限频、线程为 None 或「对象在但已退出」都触发重启、停滞只提示一次且恢复复位、
**刚连接无首帧不误报但超时后会提示**；
③daemon 逐屏隔离：桩住状态机让屏幕2 每轮抛异常，断言**屏幕1 仍持续渲染、屏幕2 异常被计数、每轮都跑到屏幕2**（
即 v5.26.0 修复的核心，旧代码只渲染一轮就整轮中断）。
**回归**：`smoke_test_multiscreen`（27，含真实 `daemon_task` 双假屏连接/掉线重连）/
`smoke_test_settings_save`（12）/`smoke_test_settings_persist`（41）/`smoke_test_power_mode`（35）/
`smoke_test_power_opt`（16）/`smoke_test_ui_visibility`（9）/`smoke_test_ui_persist`（19）/
`smoke_test_static_api`（28）全部通过；主程序 `py_compile` 通过。

#### v5.25.0 单实例保护（2026-09-13）

**背景**：v5.24.0 已查明「编译后一块屏正常、另一块像没连接」的头号成因是 **两个实例各抢一块屏**
（程序对两块小屏的串口没有互斥，先打开哪个 COM 口就拥有哪块屏）。本版从根上堵住：

**实现（`MSU2_MINI_V2.py`）**
- `_SINGLE_INSTANCE_NAME`（8367）= `MSU2_MINI_V2_single_instance`（**不带 `Global\`**：普通用户也能创建；
  进程退出自动释放，不会留死锁）。
- `acquire_single_instance()`（8371）：`CreateMutexW` + `ERROR_ALREADY_EXISTS`(183) 判定。
  ★ **必须用 `ctypes.WinDLL("kernel32", use_last_error=True)` + `ctypes.get_last_error()`**——
  直接用 `windll.kernel32.GetLastError()` 会读到被覆盖的错误码，检测静默失效（开发中真实踩到过）。
  非 Windows 不做限制；创建失败时放行（宁可多开也不因保护而打不开）。
- `_find_running_main_window()`（8409）：`EnumWindows` 按标题前缀（`PROGRAM_TITLE`）找**可见**主窗口
  （托盘/隐藏状态不强拉，避免与托盘逻辑状态不同步）。
- `notify_already_running()`（8438）：找到窗口→ `ShowWindow(SW_RESTORE)` + `SetForegroundWindow` 切到前台；
  找不到→弹 `QMessageBox` 说明“程序已经在运行”与原因。
- **调用位置（关键）**：`__main__`（15992）里 **最前面**，在 `_init_single_device()` 之前——
  否则第二个实例已经抢占了某块屏的串口，保护就失去意义。
- 例外：`--allow-multiple` 参数 / 环境变量 `MSU2_MINI_ALLOW_MULTIPLE=1` 跳过检查（会提示“多实例会各抢一块屏”）；
  `--quiet` / `MSU2_MINI_QUIET` 静默退出不弹窗。
- 附带修复：新增**模块级 `import ctypes`**（第 41 行）——此前 `ctypes` 只在个别函数内部局部 import，
  模块级并无该名字（开发中导致 `NameError` 被 `except` 吞掉、保护静默失效）；同时把
  `acquire_single_instance` 的捕获改为**打印异常**（不再静默）。

**新增冒烟测试**：`_scaffold/smoke_test_single_instance.py`（9 项全过）——实现/顺序检查、本进程重复获取、
真实子进程获取=False、子进程 `--allow-multiple`/环境变量=True；若本机已有实例在运行则整段跳过（不误报）。

#### v5.24.0 无控制台运行日志 + 串口连不上原因提示（2026-09-13）

**背景**：用户反馈「**编译后，一个屏正常，一个屏好像没有连接一样**」。查代码发现两件事：
1. 程序**没有任何单实例保护**（无 mutex/QLocalSocket/QSharedMemory）：同时跑两个程序
   （源码版 + exe，或两个 exe、自动启动 + 手动启动）会对同一批串口竞争，
   **先抢到哪个串口就只拥有哪块屏**，另一个实例里那块屏就永远“好像没连接”——
   这是「一个正常、一个像没连接」最典型的表现（源码单跑时正常，所以“编译前没问题”）。
2. 编译成无控制台 exe（`--windows-console-mode=disable`）后 **print 全部进 NUL**，
   而串口打不开/设备无响应的原因 **只 print、不提示**→ 用户只能看到“没连接”，无从判断。

**新增 1：运行日志文件（无控制台时自动开）**
- 模块级 `_TeeWriter`（8270）/`_setup_log_file`（8314，启动即调用 8341）：
  检测到 **没有真实控制台**（编译后的 exe、pythonw 运行）时，把 `sys.stdout`/`sys.stderr`
  重定向为「原流 + 日志文件」双写；有真实控制台（源码运行、`attach` 模式、重定向测试）不写文件；
  环境变量 `MSU2_MINI_NO_LOG=1` 可完全关闭。
- 日志位置：**程序目录（exe 同目录）`MSU2_MINI_log.txt`**；滚动：超过 2MB 自动把旧文件滚为 `MSU2_MINI_log.txt.1`；
  每次启动写一行分隔（时间/版本/PID/程序目录），用锁保证多线程写安全。

**新增 2：串口连不上原因提示（信息框可见）**
- `_connect_failures` / `_note_connect_failure`（13654）/`_clear_connect_failure`/`_report_connect_failures`（13664）：
  `Get_MSN_Device`（13686）在三种失败处登记原因——串口打不开（“可能被其他程序/另一个实例占用”）、
  设备无响应、设备校验失败；连接成功后清除。
- daemon 每轮扫描（15603）调 `_report_connect_failures`：把“有串口但没连上”的原因汇总发到信息框
  （限频 30 秒，避免刷屏），例如：`以下串口未连接：COM4：串口打不开（可能被其他程序/另一个实例占用）：...`。

**使用方法**：换新 exe 后若再出现「一块屏像没连接」，直接看 ①信息框里的「以下串口未连接」原因；
②exe 同目录 `MSU2_MINI_log.txt` 里的 `USB设备描述符诊断` / `连接成功：COMx@位置` / `设备校验失败` 等记录；
③先在任务管理器确认只有**一个**实例在跑（本程序不会阻止第二个实例启动）。

#### v5.23.0 编译脚本完善 + 修正「仅编译后才出现」的 3 处问题（2026-09-13）

**背景**：用户反馈「源码运行正常、编译（Nuitka）后的 exe 好像有点问题」。用 Nuitka 4.0.7 实测 + 编译产物字符串比对逐项定位，结论如下。

**一、实测结论：Nuitka 编译后与源码运行的差异**

| 项 | 源码运行 | Nuitka 编译后（实测） |
|---|---|---|
| `sys.frozen` | 不存在 | **仍然不存在**（Nuitka 只注入 `__compiled__`） |
| `__file__` | 源码绝对路径 | exe 目录：`dirname(sys.argv[0]) + 模块相对路径`（故 `HardwareMonitor` 找 LibreHardwareMonitor DLL、`_get_resource` 找 resource/ 都正常） |
| `sys.stdout/stderr` | 控制台 | `--windows-console-mode=disable` 时是 `NUL:` 包装器（`print` 不报错，但日志全丢） |
| 子进程（ping/powercfg） | 继承父控制台，无窗口 | 无控制台 → Windows 为子进程**新建控制台窗口（黑框一闪而过）** |
| 程序目录 | 源码目录 | **exe 所在目录**（`config/`、`.env`、`hotfolder/` 全部跟 exe 走） |

**二、修复 1：网络延迟页 / 电池页弹黑框（最容易被感知为「编译后有问题」）**
- `ping_worker`（网络延迟页每秒 ping 一次）与 `fetch_battery`（`powercfg /batteryreport`）调用 `subprocess` 时未带 `CREATE_NO_WINDOW`。源码用 python.exe 跑时子进程继承父控制台，看不到黑框；编译成无控制台 exe 后每个子进程都会新建控制台窗口 → 显示「网络延迟」页时每秒闪一个黑框。
- 新增模块常量 `_NO_WINDOW_FLAGS`（第 44 行，`0x08000000` = `CREATE_NO_WINDOW`），上述两处子进程调用补齐 `creationflags`。

**三、修复 2：开机自启动注册命令错误**
- `set_auto_start` 只判 `getattr(sys, "frozen", False)`，而 **Nuitka 不设 `sys.frozen`**（实测 MISSING）→ 编译后走的是「源码分支」，把 HKCU Run 项注册成 `"<exe>" "<exe路径>"`（把 exe 自己当参数传给自己）。
- 改为 `getattr(sys, "frozen", False) or "__compiled__" in globals()`。

**四、修复 3：编译脚本 `0_nuitka_build.bat` 完善（发布目录可直接拷走）**
- **`call nuitka ...`（关键）**：本机 `nuitka` 实为 `Scripts\nuitka.cmd`（批处理），批处理里**不加 `call` 调用另一个批处理会被「接管」并终止当前脚本** → 旧脚本 `nuitka` 之后的 `echo Build finished!` / `pause` 从来没执行过，也没法加后处理。改为 `call`。
- 开头 `cd /d "%~dp0"` + 校验主程序存在：不再要求「必须在项目目录运行」，避免 `resource\icon.ico` 等相对路径找不到。
- 开头 `chcp 65001`（正确读中文注释/参数）+ 结束恢复原代码页。
- 新增可调变量：`APP_VER`（与 `PROGRAM_VERSION` 同步，用于 exe 属性）、`CONSOLE_MODE`（默认 `disable` 正式发布；改 `attach` 后从 cmd 运行 exe 即可看到日志、双击仍无控制台——排查「编译后才有」的问题用）、`SYNC_CONFIG`（默认 1）。
- 新增 exe 版本信息（`--company-name/--product-name/--file-description/--file-version/--product-version`），exe 属性页不再空白。
- 补打包 `LibreHardwareMonitor.NET.10`（约 18MB，供缺少 .NET Framework/8 的机器回退）。
- 新增 `--assume-yes-for-downloads`（避免编译中因下载确认而卡住）。
- **编译后自动整理发布目录**：创建 `hotfolder`；把本机 `config\*.json`（每屏设置/电视墙/界面状态）按较新原则同步进发布目录的 `config\`；`.env`（DeepSeek 等密钥）缺失时复制。
- 结束提示改为「发布要拷贝**整个 `build_output\MSU2_MINI_V2.dist\` 目录**，只拷 exe 无法运行；设置在同目录 config、密钥在同一目录 .env」。

**五、说明**：源码运行时程序目录 = 源码目录，编译后 = exe 目录，所以「编译后设置像被重置 / 密钥没了」的根因是 **config 与 .env 不在 exe 目录**；新编译脚本已自动把两者带过去。

#### v5.22.0 —— 修复「显示信息框」设置不持久 + 两块小屏总有一块自动断开（2026-09-11）

**一、「显示信息框」设置没有持续保存（用户反馈：关闭后每次都要重新设置）**
- **根因**：`_ui_load_state()` 读取 UI 状态时写的是 `show_info = int(data.get("show_info", 1) or 1)`。
  `config/MSU2_MINI_ui.json` 里存的是 `show_info: 0`（已关闭），但 `0 or 1` 在 Python 里会回退成 `1`，
  于是勾选框被重新勾上、底部信息框又被显示出来——“设置不持久”其实是加载侧把 0 吞掉了。
- **修复**：改为 `_show_info_state = 1 if int(data.get("show_info", 1)) else 0`；新增
  `_apply_show_info_state()`（配模块级注册表 `_show_info_widgets` 信息框控件 / `_show_info_cbs` 各屏设置页勾选框）
  统一同步「信息框显隐 + 所有屏勾选框状态」，避免多屏设置页里同一个程序级开关表现不一致。
  勾选/取消勾选仍实时 `_ui_save_state()` 写入 `config/MSU2_MINI_ui.json`（设置实时自动保存约定）。
- **同类缺陷一并修复**：电视墙「在显示器标签内显示实时内容」`wall_show_live` 原为
  `bool(int(data.get("wall_show_live", 1) or 1))` —— 关闭后重启同样会被强制打开，已改为
  `bool(int(data.get("wall_show_live", 1)))`。

**二、两块小屏总有一块自动断开（用户反馈：设备本身没问题，怀疑程序逻辑）**
- **根因 ①（关键）端口归属被主设备槽位污染**：daemon 扫描连接时用 `_primary_device`（主设备槽位）
  判断“这个端口要不要用主设备对象接”，而 UI 切屏（顶部设备下拉 `on_device_select`、屏幕标签
  `_activate_by_name`、API `/api/device/select`）会把 `_primary_device` 改写为用户当前选中的屏。
  于是当**两块屏同时掉线再重连**时：daemon 把第一个扫到的端口（如 COM11，本属屏幕1）连进了
  「屏幕2 的设备对象」——设备身份/名称/配置文件全部错乱；原屏幕1 对象因自己登记过的 COM11
  已被 `known_com_ports` 占用而**永远不会再被扫描重连** → 表现为“总有一块屏自动断开（永久离线）”，
  运气差时还会多出一个「屏幕3」。
  **修复**：扫描改为**端口归属优先**——先找「已登记该端口」的设备对象（无论当前是否在线）并复用，
  只有“端口无归属且当前无任何设备在线”才用主设备槽位；新设备索引改取 `max(all_devices.keys())+1`
  （避免删除后索引冲突）；连接失败的回退删除改为按 `target.index` 删除。
- **根因 ② 主设备槽位不该被 UI 改写**：新增 `set_default_device()`（默认活跃屏）与 `_default_device`，
  `get_current_device()` 的兜底改为 `_default_device or _primary_device`；UI/API 的三处切屏一律改用
  `set_default_device()`，`_primary_device` 只由 daemon 扫描/连接维护（端口扫描、断线重连依赖它稳定）。
- **根因 ③ 单次通信失败就判掉线**：LCD/SFR 命令（`LCD_ADD`/`LCD_State`/`LCD_Photo`/`LCD_Color_set`/
  `Read_M_u8` … `show_PC_state` 等十余处）任一失败都会调用 `set_device_state(0)`，而
  `ScreenDevice.set_device_state(0)` 会**直接关闭串口** → 设备掉线 → daemon 重连初始化（屏幕白一下/消息刷屏）。
  设备瞬时繁忙（正在渲染帧、复位握手残留）就会触发，与既有注释「不 ser.close()/set_device_state(0)：
  瞬时异常会误判掉线」的设计相反。
  **修复**：`set_device_state(0)` 改为**连续失败累计**——新增 `COMM_FAIL_LIMIT = 3` 与 `device.comm_fail_count`，
  累计 3 次才真正判掉线（并提示“设备掉线：屏幕X（连续 3 次通信失败，正在自动重连）”）；
  `SER_Read()` 只要收到任何响应就清零计数（一次成功通信即证明链路正常）。真正拔线时所有命令都会失败，
  连续 3 次后仍能正常判掉线并自动重连。
- **根因 ④ `manage_task` 线程设备绑定不一致**：该线程启动时把 `dev` 抓在局部变量里，但线程本身没有绑定
  活跃设备，`Read_ADC_CH`/`SER_rw` 会回退到「当前默认屏」。一旦 UI 切屏，就变成「检查 A 屏状态、
  却读写 B 屏串口」，可能把 B 屏判掉线/把按键动作打到 B 屏。
  **修复**：`manage_task()` 启动时 `set_current_device(dev)`（线程本地绑定），检查与读写始终同一块屏。
- **验证**：新增 `_scaffold/smoke_test_multiscreen.py`——用假串口（FakeSerial/FakePort 仿真 MSN 协议）
  模拟两块小屏（COM11/COM12），离屏跑真实 `daemon_task`：双屏自动连接、UI/API 切屏不改写主设备槽位、
  双屏同时掉线后重连仍是同一对设备对象（不产生「屏幕3」、无永久离线遗留对象）、单屏掉线重连回到原对象、
  连续 3 次失败才判掉线、成功通信清零计数，共 27 项全过。
- **其他**：`_scaffold/smoke_test_ui_visibility.py` 增加配置目录隔离（脚本运行时程序目录 = `_scaffold\`，
  原先会读/写 `_scaffold\config`，上次运行留下的 `wall_show_live=0` 会干扰断言）；
  `smoke_test_settings_persist.py`/`smoke_test_aida64.py`/`smoke_test_system_monitor.py`/`smoke_test_deepseek.py`
  增加 `sys.stdout.reconfigure(encoding="utf-8")`（控制台 GBK 时 ✓/✗ 会 UnicodeEncodeError 而中断整份测试）。
  全部 14 个冒烟测试退出码均为 0。

#### v5.21.0 —— DeepSeek 字号自适应改为基于内容宽高（2026-08-28 正式发布）
- **优化**：DeepSeek 余额字号自适应改为**基于内容实际宽高**——开启自适应后：①高度：行数*(字号+2) <= 屏高 → 最大字号 = 屏高//行数-2；②宽度：最长行 <= 屏宽-4，从①的上限往下找能放下的最大字号；③单行/少行时字号上限 **50**（内容短可用大号，如 `130.31` 可到 40+），内容长/多行自动缩小（4 行 18 / 5 行 14）。
- **背景**：此前按行数固定上限（先 32 后 24）导致单行短内容字号偏小、显得“没自适应”（用户反馈：应到 50）。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（45 项全过：新增高度/宽度/上限 50 断言）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.20.0 —— 修复 DeepSeek 自定义模板后字号“不自适应”（2026-08-28 正式发布）
- **修复**：自定义模板后若只显示少量项（如仅「总余额 %1」一行），自适应字号冲到上限 32（行高 34 几乎占满 80px 整屏，看起来像没自适应）。
- **调整**：自适应字号上限由 32 降至 **24**——单行/少行时更协调，多行仍按行数自动缩小（4 行 18 / 5 行 14 不受影响）；长文本自动缩小逻辑对自定义模板同样生效（如「自定义 %1 元」这类长模板会自动缩小）。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（43 项全过：新增渲染字号上限与长文本缩小断言）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.19.0 —— DeepSeek 余额显示项自定义模板（2026-08-28 正式发布）
- **新增**：DeepSeek 余额显示页每个显示项（总余额/赠送/充值/币种/可用）后新增**自定义模板输入框**——设置 → 页面内容 → DeepSeek余额，每行「勾选 + 模板输入」。模板支持 `%1`=该显示项值（数值或文本）、`%2`=币种，如「自定义 %1 元」→「自定义 80.00 元」；**留空用默认格式**（余额/赠送/充值/币种/可用）。
- **存储**：新增 `deepseek_custom_templates`（dict，{item: 模板}，按设备保存）；因 API 白名单仅支持 int/str，该 dict 字段**不入** `_API_CONFIG_WRITABLE`。
- **渲染**：`_deepseek_build_lines` 增加 `custom_templates` 参数——有模板用模板（`%1`/`%2` 替换），无/空模板回退默认格式。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（41 项全过：新增自定义模板/混合模板+默认/空模板回退/默认字段）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.18.0 —— DeepSeek 余额对齐方式（2026-08-28 正式发布）
- **新增**：DeepSeek 余额显示页新增「对齐方式」——设置 → 页面内容 → DeepSeek余额 下拉：垂直居中（默认）/ 向上对齐（`deepseek_align`：center/top）。
- **渲染**：按对齐方式计算起始行——居中 `start_y = max(2, (SHOW_HEIGHT - 行数*行高)//2)`，向上 `start_y = 2`。
- **API 白名单**：`_API_CONFIG_WRITABLE` 新增 `deepseek_align`（现 8 个 deepseek_* 字段）。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（35 项全过：新增对齐默认字段断言）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.17.0 —— 主控页控件按设备配置恢复（2026-08-28 正式发布）
- **修复**：主控页（主设备 index0）控件启动时显示全局默认值、用户需重新选择——根因：index0 主控页在设备连接前用全局默认配置构建（`_build_main_tab` 的 `_cfg()` 解析为全局 config），连接后 `_rebuild_main_tabs` 因 index 已存在而跳过重建，且无任何按设备配置刷新路径（此前维护记忆中的 `_apply_main_ui_to_config` 实际不存在，本次补齐）。
- **新增 `_apply_main_ui_to_config(dev)`**（UI_Page 内）：设备配置就绪/变化时把配置刷回主控页控件——RGB 文字颜色滑块 / 填充适应单选 / 动图间隔 / 最大 FPS / 相机下拉 / 镜像窗口下拉 / 屏幕分辨率下拉，全部 `blockSignals` 防误触发保存。
- **`_periodic_refresh` 触发**：检测设备配置签名（`id(dev.config)`）变化即调用刷新（修复 index0 及多屏切换后的控件显示）。
- **屏幕分辨率下拉**：构建时直接读 `config.lcd_size` 匹配选项回填（此前用全局 `LCD_MAX_X/Y` 拼字符串，恒显示 160x80 (默认)）。
- **ctx 补充**：`_sliders`/`_radio_fill`/`_refresh_cameras`/`_refresh_windows` 控件引用。
- **验证**：更新 `_scaffold/smoke_test_settings_persist.py`（31 项全过：含主控页恢复源码断言）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.16.0 —— 设置持久化遗漏全面修复（2026-08-28 正式发布）
- **修复「显示信息框」不保存**：根因——`_ui_save_state` 用 `Text1.isHidden()` 读取信息框状态，而 Qt 的 `isHidden()` 在主窗口最小化到托盘/隐藏时**恒为 True**，导致程序在后台退出时把「关闭信息框」误存为「显示」，下次启动信息框又出现（看起来每次都要重设）。修复：新增独立状态变量 `_show_info_state`（模块级，随 `_ui_save_state`/`_ui_load_state` 读写，`_chg_show_info` 更新），不再依赖控件可见性。
- **设为自动连接（auto_connect）**：此前仅改内存全局、无持久化，重启必回默认。现程序级持久化到 `MSU2_MINI_ui.json`（切换即 `_ui_save_state()`，启动 `_ui_load_state()` 恢复并同步勾选框，blockSignals 防误触发）。
- **屏幕分辨率（lcd_size）**：手动选的分辨率此前不保存，重启被自动检测覆盖。现 `sys_config.lcd_size` 持久化，`Set_LCD_Size_Manual` 保存，`Detect_LCD_Size` 启动/连接时优先应用已保存值（不再自动检测覆盖）。
- **API/MQTT 字段即时自动保存**：`api_enable`/`api_port`/`api_token`/`api_overlay`/`api_protocols`/`mqtt_*` 此前仅靠「应用并重启」按钮落盘，不点按钮直接退出即丢。新增 `_autosave_api()`（改动即保存到配置，不重启服务器），各控件接 `.toggled`/`.editingFinished`；「应用并重启」按钮复用 `_autosave_api()` 后重启（仍负责即时生效）。
- **DeepSeek API Key 失焦自动保存**：输入框 `editingFinished` 自动写入 `.env`（静默），按钮仍可手动保存（带提示）。
- **验证**：新增 `_scaffold/smoke_test_settings_persist.py`（24 项全过：状态变量 / 分辨率保存与恢复 / API 自动保存源码断言 / 默认字段）。既有 11 个冒烟测试全部通过（无回归）。

#### v5.15.0 —— DeepSeek 余额字体大小设置（2026-08-28 正式发布）
- **新增**：DeepSeek 余额显示页新增字体设置——设置 → 页面内容 → DeepSeek余额 新增「字体自适应屏幕」开关（`deepseek_font_auto`，默认开启）与「手动字号」输入（`deepseek_font_size`，默认 13）。
- **自适应（默认）**：按显示行数自动推算字号（`SHOW_HEIGHT // 行数 - 2`，8~32），并长文本自动缩小字号保证一屏放得下；行高随字号自适应（font_size+2）。
- **手动**：关闭自适应后用手动字号（8~72）。
- **API 白名单**：`_API_CONFIG_WRITABLE` 新增 `deepseek_font_auto`/`deepseek_font_size`（现 7 个 deepseek_* 字段）。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（34 项全过：新增字体默认字段断言）。既有 10 个冒烟测试全部通过（无回归）。

#### v5.14.0 —— DeepSeek 余额仅显示官方字段（2026-08-28 正式发布）
- **调整**：DeepSeek 余额显示页改为**仅显示官方接口真实返回的字段**——官方 `GET /user/balance` 返回 `is_available` + `balance_infos[]`（`currency`/`total_balance`/`granted_balance`/`topped_up_balance`），按需求去掉「累计消费（估算）」项（官方不返回累计消费，不做任何估算）。
- **显示项固定为官方字段**：总余额 / 赠送余额 / 充值余额 / 币种 / 账户是否可用，可多选勾选实时保存（`deepseek_show_items`）。
- **移除估算相关**：删除 `deepseek_topup_total` 配置字段与设置项（累计充值总额输入框）；`_parse_deepseek_balance` 不再接收 `topup_total` 参数、不再计算 `used_estimate`；`_deepseek_build_lines` 去掉 used_estimate 分支；`_API_CONFIG_WRITABLE` 同步去掉该字段（现 5 个 deepseek_* 字段）。
- **验证**：更新 `_scaffold/smoke_test_deepseek.py`（32 项全过：仅官方字段解析 / 显示项组合 / 打桩抓取无 used_estimate / 未配置 Key / 网络兜底 / .env 往返 / 默认字段）。既有 10 个冒烟测试全部通过（无回归）。

#### v5.13.0 —— DeepSeek 余额显示页（2026-08-28 正式发布）
- **新增**：第 28 个显示页面「DeepSeek余额」（`DEEPSEEK_PAGE_ID=27`）——查询 DeepSeek API 账户余额并显示到小屏，显示内容可自定义勾选。
- **官方接口**：`GET https://api.deepseek.com/user/balance`（`Authorization: Bearer <key>`），返回 `is_available`（账户是否可用）+ `balance_infos[]`（`currency` / `total_balance` 总余额 / `granted_balance` 赠送余额 / `topped_up_balance` 充值余额）。
- **自定义显示项**：设置 → 页面内容 → DeepSeek余额 勾选显示 总余额 / 赠送余额 / 充值余额 / 币种 / 账户是否可用 / 累计消费（估算），可多选，实时保存（`deepseek_show_items`）。
- **累计消费为估算**：官方余额接口**不返回累计消费**，用「累计充值总额 - 当前总余额」估算（`deepseek_topup_total`，设置里填写，留空则不显示）。
- **API Key 安全（维护约定第 8 条）**：存程序目录 `.env` 的 `DEEPSEEK_API_KEY`（设置页可填写并「保存到 .env」，密钥不入源码/配置 JSON）；新增 `_load_env_file`（8098，启动加载 .env 到 os.environ）`/_save_env_key`（8119，写 .env）。
- **抓取/渲染**：`fetch_deepseek_balance`（14782，后台线程 + TTL 自动刷新，`deepseek_refresh_interval` 默认 300s 可配置/关闭）→ `_parse_deepseek_balance`（14727，解析官方响应）→ `_deepseek_build_lines`（14753，按显示项组行）→ `show_deepseek_balance`（14809，渲染多行，背景/字体颜色可配含配色方案）。
- **API 白名单**：`_API_CONFIG_WRITABLE` 新增 6 个 deepseek_* 字段，`/api/config/set` 可远程改。
- **验证**：新增 `_scaffold/smoke_test_deepseek.py`（35 项全过：解析 / 估算 / 显示项组合 / 打桩抓取 / 未配置 Key / 网络兜底 / .env 往返 / 默认字段 / 页面注册）。既有 10 个冒烟测试全部通过（无回归）。

#### v5.12.0 —— 硬件监控数据源「系统原生」（零后台，2026-08-28 正式发布）
- **新增**：设置 → 监控显示 → 硬件监控数据源 新增第三项「系统原生（无需任何后台程序，覆盖有限）」（`sys_config.hardware_source`：libre / aida64 / system）——**彻底不依赖 AIDA64 或任何后台进程**，程序内直接读取硬件数据，回应「不想开 AIDA64 也能获取数据」的需求。
- **覆盖**：CPU 使用率/频率、内存、磁盘、电池（psutil）；CPU 温度（WMI 热区 `MSAcpi_ThermalZoneTemperature`，视主板支持，5 秒缓存避免 COM 查询卡顿）；NVIDIA GPU 温度/负载/显存/功耗（pynvml，可选，无则跳过）；无 wmi/pynvml 时优雅降级（其余项正常）。
- **实现**：新增 `load_system_monitor`（7607）/`SystemMonitorManager`——与 `HardwareMonitorManager` 接口鸭子兼容（sensors/get_value/get_value_formatted/list_sensors/update_hardwares），硬件详情/仪表盘/传感器选择框全部复用；`update_hardwares` 每次刷新读数（psutil 非阻塞 CPU 采样、WMI 5 秒缓存、pynvml 直读）。
- **惰性加载分流**：`_ensure_hardware_monitor_async`（14613）新增 `system` 分支（无 pythonnet/.NET 加载，秒级就绪）。
- **三数据源取舍**：LibreHardwareMonitor（默认，无后台程序，覆盖广）/ AIDA64（需后台运行并开启共享内存，覆盖最全）/ 系统原生（零后台，覆盖有限）。
- **验证**：新增 `_scaffold/smoke_test_system_monitor.py`（29 项全过：打桩 psutil/wmi/pynvml 的解析 / 类型与硬件分类 / 接口兼容 / `_find_sensor_value` 自动识别 / 刷新 / 无 wmi/pynvml 优雅降级）。既有 10 个冒烟测试全部通过（无回归）。

#### v5.11.0 —— 硬件监控数据源可选 AIDA64（2026-08-28 正式发布）
- **新增**：设置 → 监控显示 → 顶部「硬件监控数据源」下拉（`sys_config.hardware_source`，libre/aida64，按设备独立保存），可切换 **LibreHardwareMonitor**（默认，推荐）或 **AIDA64**。
- **AIDA64 方式说明**：需 AIDA64 在后台运行（可最小化到系统托盘，占用资源小），并在 AIDA64 → 文件 → 设置 → 硬件监视 → LCD 中勾选「启用共享内存(Shared Memory)」。**无独立 DLL 可直接调用**——AIDA64 通过 Windows 共享内存（memory-mapped file）`AIDA64_SensorValues` 实时暴露全部传感器（CPU/GPU/主板/内存/风扇/电压/功率/电流/时钟等），本程序用 ctypes 打开并读取（无 pythonnet/.NET 依赖）。
- **实现**：新增 `load_aida64_monitor`（7418）/`Aida64MonitorManager`——读取共享内存（偏移0=dwSize、4~16=产品类型/版本/构建号/实例数、20 起=“名称:值,名称:值,...”传感器串 NUL 结尾），从值字符串解析数值并按单位推断传感器类型（°C/°F→Temperature、%→Load、RPM→Fan、V→Voltage、W→Power、A→Current、MHz/GHz→Clock、/s→Throughput、GB/MB→Data），按传感器名推断硬件类别（含 CPU→Cpu / GPU→Gpu / 内存 / 主板 / 风扇），接口与 `HardwareMonitorManager` 鸭子类型兼容（sensors / get_hardware / get_value / get_value_formatted / list_sensors / update_hardwares）。
- **复用**：硬件详情、仪表盘（温度/风扇/负载）、自定义显示（多项数值/两项图表）、传感器选择框全部复用——`_find_sensor_value` / `_open_sensor_picker` / `_resolve_gauge_sensor` / `show_hwdetail` / `show_custom_two_rows` / `get_full_custom_im` 无需改动即可工作；CPU/GPU 自动识别（硬件类型 Cpu 命中 `_HW_CPU_TYPES`，GPU 走名称关键词回退）。
- **惰性加载分流**：`_ensure_hardware_monitor_async`（14423）按 `config_obj.hardware_source` 选择加载 LHM（pythonnet/.NET）或 AIDA64（共享内存）；切换数据源自动重置已加载管理器（`hardware_monitor_manager=None`、`_hw_monitor_loading=False`），下次进入硬件页面按新数据源重新加载。
- **未运行/未开启提示**：AIDA64 未运行或未开启共享内存时 `OpenFileMappingW` 失败 → `last_error` 提示「AIDA64→文件→设置→硬件监视→LCD→勾选启用共享内存」，页面显示“未找到传感器”。
- **验证**：新增 `_scaffold/smoke_test_aida64.py`（28 项全过：假共享内存解析 / 类型推断 / CPU-GPU 硬件分类 / 接口兼容 / `_find_sensor_value` 自动识别 / 未运行提示 / update_hardwares 刷新）。既有 9 个冒烟测试全部通过（无回归）。

#### v5.10.0 —— MQTT 全面接入（2026-08-27 正式发布）
- **新增**：附加接入协议 MQTT（设置 → API接入 → 附加接入协议 勾选「MQTT 客户端…」后启用，默认关闭）。以 paho-mqtt 客户端**连接外部 Broker**（设置新增「MQTT 接入」分组：`mqtt_host`/`mqtt_port`/`mqtt_username`/`mqtt_password`/`mqtt_command_topic`/`mqtt_response_topic`/`mqtt_frame_topic`，全部实时自动保存）。
- **命令通道**：订阅命令主题（默认 `msu2/command`）执行投屏命令（`Command.json` 与 HTTP POST /api/* 完全一致，复用 `api_execute_command`（4507），响应 JSON 发回响应主题 `msu2/response`）；连接成功后向响应主题发布 `mqtt_status/connected` 状态消息。
- **帧通道**：`start_api_mqtt`（6428）启动帧发布线程（`_api_mqtt_thread`）低频推送当前活跃屏外部投屏帧到帧主题 `msu2/frame`（原始 RGB888，**有变化才发**、≥0.5s 节流），与 gRPC WatchFrame / SSE 类似。
- **未装 paho-mqtt 自动跳过**（打印提示，与 pyzmq/grpcio 一致）；`stop_api_extra`（6561）统一停止（loop_stop + disconnect + 停帧线程）。兼容 paho 1.x/2.x（`CallbackAPIVersion.VERSION2`，无则旧式 Client）。
- **协议自动发现**：`GET /api/protocols`（`api_get_protocols`（4154））新增 MQTT 条目（type=mqtt，enabled 按 paho-mqtt 是否安装，地址 mqtt://host:port）。
- **文档同步（维护约定）**：`_build_openapi_doc()`（4944）info 描述补 MQTT；`_API_DOC_HTML`（5643）第 8 节补 MQTT 接入与客户端示例；Web 控制台文档标签补 MQTT。
- **验证**：新增 `_scaffold/smoke_test_mqtt.py`（27 项全过：配置默认值/持久化/amqtt 本地 broker 端到端（health/version/未知命令）/帧推送（长度/首像素）/protocols 自动发现/停止后清空/设置勾选框含 mqtt）。既有 8 个冒烟测试全部通过（无回归）。

#### v5.9.0 —— gRPC 全面接入（2026-08-27 正式发布）
- **新增**：附加接入协议 gRPC（设置 → API接入 → 附加接入协议 勾选「gRPC（端口+4，msu2.Msu2Api，需 pip install grpcio）」，默认关闭，端口 = api_port+4 即默认 8636）。基于 grpcio 动态构建服务描述（`_api_grpc_build_messages`，用 `google.protobuf` 的 descriptor_pool + message_factory，仅需 grpcio 无需 grpcio-tools），提供 `msu2.Msu2Api` 服务（`_GRPC_PROTO` 常量即 .proto 源文本，启动时自动导出到程序目录 `msu2_api.proto` 供外部生成客户端）。
- **Execute RPC**：执行一条投屏命令（`Command.json` 与 HTTP POST /api/* 完全一致：text/screen/clear/page/mirror/quit/health/version 等全部命令，复用 `api_execute_command`（4490）），返回 `Response.json`（JSON 响应）+ `code`（0=成功 非0=失败）。
- **WatchFrame RPC（服务器端流式）**：订阅指定屏幕的最新外部投屏帧（`FrameRequest.device` 留空=当前活跃屏），`FrameData` 携带 device/width/height/rgb888（原始 RGB888，W*H*3）/version；低频轮询（0.2s）**有变化才推送**，不占用 CPU。
- **未装 grpcio 自动跳过**（打印提示，与 pyzmq 处理一致）；`stop_api_extra`（6424）统一停止（`server.stop(grace=1)` + wait）。
- **协议自动发现**：`GET /api/protocols`（`api_get_protocols`（4152））新增 gRPC 条目（type=grpc，enabled 按 grpcio 是否安装，地址 127.0.0.1:端口+4）。
- **文档同步（维护约定）**：`_build_openapi_doc()`（4927）info 描述补 gRPC；`_API_DOC_HTML`（5626）第 8 节补 gRPC 接入与客户端示例（grpc_tools 生成 + WatchFrame 订阅）；Web 控制台文档标签补 gRPC；`_send_plain_doc` 补 port_plus_4 格式化。
- **验证**：新增 `_scaffold/smoke_test_grpc.py`（27 项全过：动态描述符/启动停止/幂等/Execute（health/version/未知命令/text）/WatchFrame 流式帧（宽高/字节数/首像素）/protocols 自动发现/停止后连接失败/设置持久化（api_protocols 含 grpc）/设置勾选框含 grpc）。既有 7 个冒烟测试全部通过（无回归）。

#### v5.8.0 —— Webhook 对接（Synology Chat 等聊天机器人）（2026-08-27 正式发布）
- **新增**：设置 → Webhook（`sys_config` 新增 6 个字段：`webhook_enable`/`webhook_port`/`webhook_urls`/`webhook_auto_display`/`webhook_display_seconds`/`webhook_receive_token`）。与聊天机器人（Synology Chat / Discord / 钉钉等）双向对接，为后续更多收发功能打基础。
- **接收（发出的 Webhook）**：独立 Webhook 接收服务器（`start_webhook_server`，仅监听 127.0.0.1，默认端口 8633），外部聊天服务把消息 POST 到 `http://127.0.0.1:<端口>/webhook/incoming`。解析器 `_webhook_extract_text` 兼容 Synology Chat / Discord / 钉钉 / 通用 JSON / 表单 / 纯文本（自动提取 text/content/message 与 attachments/embeds 拼接）；可选接收令牌（`webhook_receive_token`，请求头 `X-Webhook-Token` 或 `?token=`，不匹配返回 401）。收到消息后：记录到底部信息框（`insert_text_message`）+ 接收日志（`_webhook_receive_log`）+ 按 `webhook_auto_display` 配置在**屏幕叠加显示**（`_api_try_webhook_overlay`，叠加渲染挂接在 `MSN_Device_1_State_machine` 渲染分发链，先于普通页面渲染；`webhook_display_seconds` 秒后自动恢复原页面，多屏都会显示）。
- **发送（传入的 Webhook）**：`webhook_send(text, urls=None, data=None)` 把文本 POST 到配置的 `webhook_urls`（每行一个，逗号/换行分隔；如 Synology Chat「传入的 Webhook」URL / Discord / 钉钉）。后台线程 `_webhook_send_worker` 发送不卡 UI，发送日志 `_webhook_send_log` 可查。四种触发方式：①设置页「测试发送」按钮；②API `POST /api/webhook/send`（`api_apply_webhook_send`，需已启用 Webhook）；③`api_execute_command` 的 `type=webhook_send`（TCP/UDP/WebSocket 复用）；④Webhook 服务器 `POST /webhook/send`（便于脚本/局域网触发）。
- **API/文档同步（维护约定）**：`ApiHandler` 新增 `GET /api/webhook/status`（`api_get_webhook_status`：enabled/port/receive_url/send_targets/server_running/收发日志）与 `POST /api/webhook/send`；`_build_openapi_doc()` 新增两个 path + Webhook tag；`_API_DOC_HTML` 新增第 12 节「Webhook 对接」说明（含 Synology Chat 双向对接说明与 curl 示例）。`_check_openapi_sync` 启动校验无告警。
- **设置实时自动保存（★）**：全部 6 个字段走 `save_config()`（5 秒防抖 → 快照 + 原子写盘），下次启动自动恢复并**自动启动接收服务器**（`main` 启动块新增 `start_webhook_server` 分支）；勾选「启用」即时启停服务器（`webhook_apply_server_state`），端口/令牌等修改后点「应用并重启」生效。退出清理：`closeEvent`/`on_closing`/`api_apply_quit`/`finally` 均补 `stop_webhook_server()`。
- **验证**：新增 `_scaffold/smoke_test_webhook.py`（57 项全过：配置默认值/持久化写盘与恢复/消息提取各格式/接收核心/屏幕叠加渲染/发送到本地回环服务器/接收服务器端到端（JSON+表单+健康检查+服务器转发）/令牌校验/启停/状态接口/OpenAPI 同步/命令发送）。既有 6 个冒烟测试全部通过（无回归）。

#### v5.7.0 —— 能效模式（1~5 级）+ 静态页不刷新 + API 协议开关 + UI 显隐控制（2026-08-27 正式发布）
- **新增**：设置 → 通用 →「能效模式」下拉框（`sys_config.power_mode`，0=均衡默认 / 1~5=五级能效，按设备独立保存）。
- **等级设计**：1 级最省资源（适合长期挂机）→ 5 级最接近均衡。各级放大系数（刷新/轮询间隔倍数）：1 级=4.0、2 级=3.0、3 级=2.0、4 级=1.5、5 级=1.2、均衡(0)=1.0。
- **各热点按等级自动缩放**（`_power_factor`）：
  - 屏幕镜像/相机：帧率按系数降低（fps // factor，最低 1fps）；截图/处理队列让步 sleep 同步放大。
  - 按键检测：ADC 轮询 20Hz → 1级约 5Hz（`manage_task`，0.05×factor，串口读 + CPU 双降）。
  - 各显示页面刷新间隔按系数放大（`_power_wait`）：系统监控/网络流量/自定义显示/仪表盘/硬件详情/天气/行情/热搜/电池/跑马灯/静态页等全部接入。
  - UI 定时器按系数放大：设备刷新 2s（1级 8s）、显示墙 1s（1级 4s）、主控实时预览 500ms（1级 2000ms）。
  - ping 后台线程 1s（1级 4s）。
- **实现**：新增模块级辅助函数 `_power_level(dev)`（0~5 等级，越界钳制）、`_power_factor(dev)`（等级→系数）、`_power_active(dev)`（是否启用）、`_power_wait(dev, base)`（按系数放大间隔）；所有页面 wait 点、fps_control、manage_task、UI 定时器统一接入。
- **设置持久化加固（★）**：修复多屏下设置可能「下次启动丢失」的隐患——`save_config`（6753）记录本次修改对应的目标文件与配置快照（`_pending_config_file`/`_pending_config_snapshot`，6748），`save_config_thread`（6774）写盘用快照而非全局 `config_file`/`config_obj`；否则 daemon 渲染线程切换全局配置时，5 秒延迟写盘可能把 A 屏设置写到 B 屏文件。能效模式等**所有设置项**均经 `save_config` 持久化（设置页各子页 + 主控页全部接入），重启自动恢复。
- **UI 显隐控制（★）**：
  - 设置 → 通用 →「显示信息框」：关闭后底部信息框（「信息:」+ 消息记录）隐藏，状态存 `MSU2_MINI_ui.json`（`show_info`，程序级）重启恢复。
  - 实时预览：关闭「开启实时预览」后，主控页「实时预览:」标题与预览图控件**整块隐藏**（原仅停止刷新、控件仍占位）。
  - 电视墙 → 显示墙设置 → 显示方式 →「在显示器标签内显示实时内容」：关闭后显示墙格子仅显示设备名占位、不渲染实时预览（控件保留），状态存 `MSU2_MINI_wall.json`（`wall_show_live`）。
- **1 级能效深度降频（★）**：补齐未接入能效等级的高频轮询/等待点（1 级 factor=4.0 时全部降到原 1/4 频率）：
  - 屏幕镜像页 daemon 轮询：`show_PC_Screen` 队列 `get(timeout=0.3)` → `0.3×factor`（1级 1.2s，原 0.3s 未降频）。
  - 非镜像/相机页：截图线程与处理线程轮询 `sleep(0.5)` → `0.5×factor`（1级 2s，2 台屏共 4 线程显著降频）。
  - daemon 设备扫描：5s → `5×factor`（1级 20s，减少 comports() 枚举）。
  - 主线程 UI 消息队列 `_process_ui_msg_queue`：100ms → `100×factor`（1级 400ms）。
  - 自动翻页轮播 `_auto_cycle_tick`：1s → `1×factor`（1级 4s）。
  - 按键检测设备断开轮询 `sleep(0.3)` → `0.3×factor`；截图失败/相机待选等恢复等待同样按系数放大。
  - 效果：镜像轮询、截图/处理、扫描、主线程定时器等全部降频（镜像帧率仍保底 1fps），进一步降低 CPU 占用。
- **验证**：更新 `_scaffold/smoke_test_power_mode.py`（35 项全过：等级判定/系数映射/间隔放大/帧率缩放）；新增 `_scaffold/smoke_test_settings_save.py`（12 项全过：能效模式等设置保存/加载 + 多屏写盘快照）、`_scaffold/smoke_test_ui_visibility.py`（9 项全过：UI 启动/信息框/电视墙开关及持久化/预览显隐联动）。
- **1 级能效深度优化（★ 2026-08-27）**：进一步降低 1 级能效下 CPU/内存占用：
  - 修复**息屏空转烧 CPU**：`daemon_task` 息屏分支原直接 `continue` 空转死循环（screen_off 后无任何等待），现按能效等级放宽等待 `_power_wait(device, 1)`（1级 4s，可被按键唤醒 `sleep_event` 立即中断）。
  - 新增**进程空闲优先级** `_apply_process_priority()`（3425）：能效模式启用时整个进程设为 `BELOW_NORMAL_PRIORITY_CLASS`（低于正常，不用 IDLE 以免抢占串口/渲染时序），让系统调度优先保证前台程序，挂机时“感知占用”更低；均衡模式恢复普通优先级。启动时按配置文件生效（13525）、设置里切换能效等级实时同步（9050）。
  - 串口忙轮询降频：`manage_task` 镜像期间 50Hz 的 `serial_busy` 轮询 `sleep(0.02)` 乘系数（1级≈12Hz）；按键阈值校正等待 `sleep(0.1)` 同乘系数。
  - 系统监控页 CPU 采样改**非阻塞**：`show_PC_state` 原 `psutil.cpu_percent(interval=0.5)` 每次刷新阻塞 0.5s，改 `interval=None`（启动时预热基准，13454）。
  - 天气/行情/热搜/电池 TTL 按能效放大：`_refresh_cache_if_needed` 内 `ttl × factor`（1级 天气60s→240s、电池300s→20min），减少后台网络抓取线程。
  - **硬件监控（LibreHardwareMonitor）改惰性加载（内存大省）**：原启动 `load_task` 无条件加载 pythonnet/.NET 运行时（几十 MB），现 `load_task` 只做 PAGE_ID 标签排序；仅当首次进入需要硬件传感器的页面（仪表盘/硬件详情/自定义显示）才在后台线程加载（`_ensure_hardware_monitor_async`，13080），加载期间页面显示“加载中…”下轮自动恢复；设置对话框/传感器选择同步接入。未用硬件页面时启动省下整个 pythonnet 加载开销。
- **静态页不重复刷新（★ 2026-08-27）**：显示静态/极低频页面（照片/关于/纪念日/待办/农历）时，内容无变化则跳过重绘与串口发送，仅低频等待（`_static_page_loop`，11658）。内容指纹机制：指纹变化（配置修改/跨天）或状态变更时自动重绘——照片/关于用常量指纹（仅切页/唤醒时渲染一次）、待办用列表指纹、纪念日用「列表+日期」、农历用日期（跨天自动重绘）。显著降低静态页长期挂机时的 CPU 与串口占用。
- **API 附加协议常驻开关（★ 2026-08-27）**：设置 → API接入 → 附加接入协议（`sys_config.api_protocols`，逗号分隔）：TCP Socket（端口+1）与热文件夹默认推荐常驻，UDP（端口+2）/Windows 命名管道/Unix Domain Socket/ZeroMQ（端口+3）可关闭省内存与线程。HTTP+WebSocket（网页控制台/SSE）随「启用 API 投屏服务器」总开关始终常驻。`start_api_server` 按 `_api_enabled_protocols()`（6171）按需启动各协议；旧配置（无 api_protocols 键）迁移为全部开启保持旧行为，可在设置里按需关闭。

---

## 三、当前版本（v5.38.0 主程序）完整功能清单

> 目的：确保迭代过程中积累的功能全部保留、不遗漏。以下为 `MSU2_MINI_V2.py`（最终编译文件）当前包含的全部功能模块。

### 1. 设备与连接
- 多屏支持：按 USB 序号自动扫描/连接/掉线自动重连，每屏独立线程与配置。
- **端口归属与主设备槽位分离（★ v5.22.0）**：daemon 扫描按 COM 端口复用「已登记该端口」的设备对象（不依赖 `_primary_device`），
  新设备索引取 `max(keys)+1`；UI/API 切屏改用 `set_default_device()`（默认活跃屏），`_primary_device` 仅由 daemon 维护。
  杜绝「两块屏同时掉线重连后端口串台 → 其中一块永久离线/多出屏幕3」。
- **通信失败容错（★ v5.22.0）**：LCD/SFR 命令连续失败 `COMM_FAIL_LIMIT`(3) 次才判掉线，任意一次成功通信清零计数
  （修复“单次瞬时失败就关串口→频繁掉线重连/画面重置”）；真正拔线仍能自动判掉线并重连。
- **按键线程设备绑定（★ v5.22.0）**：`manage_task` 启动时 `set_current_device(dev)`，避免“检查 A 屏、读写 B 屏串口”。
- 多屏独立配置：按 SN（冲突/为空按 COM 端口）独立保存到 `config/MSU2_MINI_<SN>.json`。
- LCD 分辨率自动检测 + 手动设置（支持 240×240 等非 160×80 设备）。
- 显示方向控制（水平/垂直/镜像/旋转，下拉选择 + 按键循环切换）。
- 每 30 秒 LCD 看门狗 + 强制方向重置，防画面倾斜/漂移。
- 防烧屏（像素微移）、实时预览（多屏独立）、设备掉线自动重连。
- **通信有效性 + 设备复位自恢复（★ v5.28.0）**：设备复位后会持续回它的**上电横幅** `\x00MSNxx`，
  此时所有命令失效、画面不再更新；程序不再“假装正常”（旧逻辑会把失败计数反复清零 → 永不判掉线 →
  永远显示「已连接」），而是：横幅=铁证 → **立即判掉线并自动重连、重新初始化 LCD**，
  并在信息框提示；响应校验（回显=最后一个子命令头）与「画面刷新健康度」联动，
  状态栏/设备信息页会显示「串口通信异常(N 次)：收到设备复位横幅…」；
  设备异常时的错误日志统一限频（每 5~10 秒一条），不再刷爆控制台/日志；
  瞬时失败的原有容错（v5.22.0：收到响应即清零、连续 3 次才掉线）保持不变。
- **整帧发送「窗口 + 数据」一次发出（★ v5.29.0，画面倾斜/斜切的根治）**：所有整帧页面
  （网络流量、磁盘读写、系统监控、自定义显示、关于、农历、待办、世界时钟、纪念日、硬件详情、仪表盘、
  DeepSeek 余额、API 投屏、屏号检测、Webhook 叠加）与**屏幕镜像**发送帧时，
  把「LCD 地址窗口」命令与整帧数据**拼成同一条字节流**、用一次 `SER_rw` 发出去——
  不再有「窗口→（渲染+编码几十毫秒）→数据」的空档，按键 ADC 轮询/通信心跳/API 调用再也插不进去
  （它们就是历史「倾斜/斜切」的成因）。同时保留观测：设备信息 → 连接 页会显示
  **「帧空档 N 次（最长 x ms）· 空档内被插入命令 M 次」**，空档 >500ms 时限频提示一次——
  一旦这行数字在涨，说明串口上又有东西在窗口与数据之间插队（排查提醒，不是故障）。
- **串口事务模型（★ v5.30.0，帧前后不允许其它命令靠近）**：每块屏的串口有「事务」概念
  （`_serial_begin/_serial_end/_serial_transaction_busy`）——帧发送期间 + 发送后静默期（默认 150 ms）内，
  按键 ADC 轮询 / 通信心跳 / API 一律避让；方向重置后静默 200 ms；帧写失败/写不完整 →
  **立刻整帧重发**（一帧内自愈）；`SER_Write` 短写会续写剩余（不再留下残缺帧）；
  方向看门狗清屏后只让**静态页**重绘（动态页不再每 15 秒多插一次「清屏 + 立刻整帧」）。
  设备信息 → 连接 页显示「帧空档 / 静默违规」计数；5 个环境变量可现场 A/B（见 iterate 5.30.0）。
- **行级地址重锚（★ v5.35.0，画面倾斜的结构性根治）**：每帧不再只在下开头下一次地址窗口，而是**每行开头
  重新锚定一次**（`_lcd_row_anchor_bytes`：第 row 行 / 行宽 W / 载入地址），缓冲提交按行对齐切分
  （行宽 160 → 128px + 32px）。于是任何一次错位（丢包/杂散字节/命令被吞）的后果**最多是当前这一行**，
  下一行立即回到正确位置——整幅斜切在结构上不再可能。画面内容与旧版逐像素一致，帧体积仅 +8%。
  开关：环境变量 `MSU2_MINI_ROW_GUARD=K`（默认 1=每行；0=关闭回旧行为）。
  量化对比（虚拟小屏逐包还原，`_scaffold/smoke_test_row_anchor.py`）：丢一个提交包时
  旧版错 **4389 像素 / 40 行**，本版错 **143 像素 / 1 行**且之后每行像素级完美。
- 设备信息页：USB 连接信息 / 固件版本 / SFR 寄存器 / Flash 芯片与分区 / 本机系统信息。
- **画面刷新健康度 + 每屏健康检查（★ v5.26.0）**：每屏记录「最近一次真的把画面写进串口的时间」与「串口写失败次数」
  （`_mark_frame_sent`/`_note_picture_ok`/`_note_write_error`/`device_frame_status`，3934~3971），
  **状态栏**与**设备信息 → 连接**页显示「画面刷新正常（x.x 秒前）/ 画面停滞 N 秒 / 串口写入失败(N 次)：原因」，
  把「已连接」与「真的在刷新」分开；daemon 每屏渲染前调 `_device_health_tick`（15730）：
  通信心跳（每 2 秒一次 ADC 读，**非主屏掉线从此能被发现**）、线程看护（每 5 秒检查截屏/处理线程，死亡自动重启）、
  画面停滞提示（>20s（按能效放大）无成功送帧 → 信息框提示一次，恢复自动复位）；
  daemon 渲染循环改为**逐屏 try/except**（单屏异常只计数并写信息框，不再拖死其它屏），
  方向看门狗重置后补置 `state_change` 强制整帧重绘（静态页不再黑）。
- **速度单位换色（★ v5.27.0 新增；★ v5.36.0 改为逐柱判断；★ v5.37.0 升级为「单位色 + 换色门槛」；★ v5.38.0 默认马卡龙五色 + 色系）**：网络流量 / 磁盘读写 的柱状图，**每根柱子按自身数值决定颜色**，优先级为
  ①**换色门槛**（用户自定义条件：大于 / 大于等于 / 小于 / 小于等于 / 等于 / 不等于，数值 + 单位 `B/KB/MB/GB/TB`，适用范围「两行都适用 / 仅上行·读 / 仅下行·写」，**可增删任意多条、按列表顺序第一条命中即用**；「等于」按 ±0.5% 容差，0 为精确比较）
  → ②**单位色**（`<1K(B)` / `KB` / `MB` / `GB` / `TB` 五档各自一色；**默认就是五种不同的马卡龙色**，留空 = 用该行原色）
  → ③该行**原始柱色**；上行/读（`bar1`）与 下行/写（`bar2`）各自独立一套。
  设置 → 监控显示：勾选框「开启逐柱换色」（`speed_unit_color_enable`，默认关）+ **「色系:」下拉框**（★ v5.38.0，选中一套即把 10 个单位色改成那套：马卡龙（默认）/糖果亮彩/暖色/冷色/高对比/单色·青绿/单色·橙金；单格手改后显示「自定义」）+ **2×5 单位色网格**（`speed_unit_bar1_color`/`speed_unit_bar2_color` 为 MB 档；其余 8 个 `speed_unit_bar{1,2}_{b,kb,gb,tb}_color`；默认值由色系表派生）+ **换色门槛编辑器**（列表存 `speed_color_rules`，「＋ 添加条件」/「删除」，改动即存）。
  只换柱子颜色（文字配色不变）；随设置实时保存、重启沿用（按屏独立）；判定口径与 `sizeof_fmt` 一致（1024 进制）；MB 档仍可用 `/api/config/set` 修改。

### 2. 显示页面（共 28 页）
- 基础：动图、时钟、相册、屏幕镜像、相机视频、关于。
- 监控：CPU/内存/磁盘/电池使用率、网络流量、磁盘读写速率、网络延迟、进程 TOP、仪表盘、硬件详情、**DeepSeek 余额**。
- 内容：文字跑马灯、番茄钟、纪念日、待办事项、世界时钟、农历、天气、行情、热搜、电池、音乐。
- 其他：自定义显示（多项数值/两项图表/完全自定义 MiniMark 模板 + 图形化编辑）、API 投屏页。

### 3. 屏幕镜像
- PrintWindow + mss 双截图通道（失败自动回退）；多显示器窗口枚举。
- 显示方式：填充/适应；局部放大（跟随鼠标，1~8 倍）。
- 多屏隔离：每屏独立截图/处理/FPS 线程，用本设备配置。

### 4. 自定义显示（MiniMark）
- 命令：文本/数值/图片/位置/偏移/锚点/颜色/字体/矩形/线条/圆/动图。
- 图形化编辑器 + 模板预设库 + 模板导入/导出 + 实时预览 + 查看错误。

### 5. 配色方案系统
- 内置 260+ 色系 + 用户自定义方案（增/删/改，按设备保存）。
- 各颜色位置色块下拉、方案下拉（提供候选色板）、混合配色、存为新方案。
- 覆盖：监控显示各页、主控页文字颜色、页面内容（背景+字体色）。

### 6. 设置中心
- 通用：防烧屏、**显示信息框（关闭后底部消息记录隐藏，程序级持久化，★ v5.16.0 修复 isHidden 误判，★ v5.22.0 修复「保存的 0 被 `or 1` 吞掉导致重启又自动显示」，并由 `_apply_show_info_state()` 同步各屏设置页勾选框）**、**设为自动连接（程序级持久化到 ui.json，★ v5.16.0）**、实时预览（关闭后主控页预览控件整块隐藏）、**能效模式（1~5级，1级最省资源长期挂机推荐，降低刷新率/轮询频率；1级下含息屏空转修复、进程空闲优先级、串口忙轮询降频、非阻塞CPU采样、网络TTL放大、硬件监控惰性加载等深度优化）**、开机自启动、界面语言（中/英）。
- 自动化：自动翻页轮播、息屏待机。
- 按键：单击/双击/长按 动作映射（下翻页/上翻页/切换方向/无）。
- 页面内容：跑马灯（文本/字号/速度/颜色）、天气与行情（城市/交易对/延迟目标）、热搜（条数/字体自适应/滚动/翻页/刷新）、时间（番茄钟/世界时钟）、纪念日与待办、**DeepSeek 余额（API Key 存 .env / 显示项勾选（仅官方字段：总余额/赠送/充值/币种/可用）/ 自动刷新 / 字体自适应+手动字号 / 颜色）**。
- 监控显示：**硬件监控数据源（LibreHardwareMonitor / AIDA64 共享内存 / 系统原生零后台，★ v5.12.0）**、进程数量、硬件详情（类型+数量+自由选传感器）、仪表盘（项目+颜色+传感器）、磁盘读写（三模式+颜色）、网络流量（经典/自定义+颜色）。
- 屏幕镜像：放大镜。配色方案：管理。数据管理：导出/导入配置。
- API 接入：启用/端口/令牌/强制投屏/屏幕序号检测时长 + **附加接入协议开关（TCP/热文件夹推荐常驻，UDP/命名管道/Unix Socket/ZeroMQ/gRPC/MQTT 可关省内存与线程）** + 应用并重启。
- **Webhook（对接聊天机器人）**：启用开关 / 接收端口 / 接收令牌 / 收到消息自动显示到屏幕（时长可调）/ 发送目标 URL（每行一个）/ 测试发送 / 应用并重启。**所有设置实时自动保存，下次启动沿用并自动启动接收服务器**。

### 7. 本地 API 服务器（HTTP + WebSocket + 多协议）
- HTTP REST / WebSocket / SSE / TCP(端口+1) / UDP(端口+2) / ZeroMQ(端口+3) / gRPC(端口+4，msu2.Msu2Api：Execute 投屏命令 + WatchFrame 流式帧，需 grpcio) / MQTT(连接外部 Broker：命令/响应/帧主题，需 paho-mqtt) / Windows 命名管道 / Unix Domain Socket / 热文件夹 / stdin 管道；HTTP+WebSocket 随总开关常驻，其余附加协议可按需开关（TCP/热文件夹推荐常驻）。
- 投屏命令：screen/text/clear/slideshow/stop/page/mirror（统一命令，device 指定目标屏）。
- 信息接口：health/version/protocols/status/config/screenshot。
- 控制接口：page_next/page_prev/key/orientation/marquee/config_set/device_select/device_refresh/notify/quit/screen_id。
- OpenAPI 3.0 文档（`/api/openapi.json`，自动生成 + 同步校验）；网页投屏控制台；强制投屏；屏幕序号检测。
- **gRPC 接入（★ v5.9.0）**：附加接入协议 gRPC（端口+4，`msu2.Msu2Api`）——`Execute` 投屏命令（JSON 与 HTTP/TCP/UDP 一致）+ `WatchFrame` 服务器端流式推送屏幕帧（RGB888，有变化才推）；启动时导出 `msu2_api.proto` 到程序目录供外部生成客户端；`/api/protocols` 自动发现；未装 grpcio 自动跳过。
- **MQTT 接入（★ v5.10.0）**：附加接入协议 MQTT——以 paho-mqtt 客户端连接外部 Broker（设置 → API接入 → MQTT 接入 配置地址/端口/用户名/密码/命令/响应/帧主题），订阅命令主题执行投屏命令（JSON 与 HTTP/TCP/UDP 一致，响应发回响应主题），并低频推送屏幕帧（RGB888 到帧主题，有变化才发、≥0.5s 节流）；未装 paho-mqtt 自动跳过。

### 8. Webhook 对接（聊天机器人，Synology Chat / Discord / 钉钉）
- **接收（发出的 Webhook）**：独立接收服务器（默认端口 8633）`POST /webhook/incoming`，外部聊天服务回调 → 消息显示到屏幕（叠加显示，超时自动恢复原页面，多屏都会显示）+ 底部信息框 + 接收日志；可选接收令牌（`X-Webhook-Token`/`?token=`）；兼容 Synology Chat / Discord / 钉钉 / 通用 JSON / 表单 / 纯文本。
- **发送（传入的 Webhook）**：`webhook_send` 后台线程 POST 到配置的 URL 列表（Synology Chat「传入的 Webhook」/ Discord / 钉钉），四种触发：设置「测试发送」/ `POST /api/webhook/send` / `type=webhook_send`（TCP/UDP/WS）/ 服务器 `POST /webhook/send`；发送日志可查。
- **查询**：`GET /api/webhook/status`（enabled/port/receive_url/send_targets/server_running/收发日志）；`GET /webhook/health` 健康检查。
- **设置实时自动保存**：全部字段持久化，下次启动自动恢复并自动启动接收服务器。

### 9. 其他能力
- 托盘隐藏、开机自启、自动更新检查、命令行参数（`--page`/`--com`）、崩溃日志、多语言。
- **编译与发布（★ v5.23.0）**：`0_nuitka_build.bat`（Nuitka standalone）——自动 `cd` 到脚本目录、`chcp 65001`、**`call nuitka`**（本机 nuitka 是 `nuitka.cmd`，不加 `call` 会导致脚本在编译处被接管终止、后处理不执行）、写入 exe 版本信息、打包 `resource`/`device_protocol.json`/`LibreHardwareMonitor` + `LibreHardwareMonitor.NET.10`，编译后自动在发布目录创建 `hotfolder` 并同步 `config\*.json` 与 `.env`；`CONSOLE_MODE=attach` 可在 cmd 里看到 exe 日志（排查「编译后才有」的问题）；`APP_VER` 与 `PROGRAM_VERSION` 同步。
- **无控制台运行也不弹黑框（★ v5.23.0）**：`_NO_WINDOW_FLAGS`（`CREATE_NO_WINDOW`）——`ping_worker`（网络延迟页）与 `fetch_battery`（电池页）的子进程已带该标志，编译后的 exe 不再每秒闪黑框。
- **开机自启动兼容 Nuitka 打包（★ v5.23.0）**：`set_auto_start` 按 `sys.frozen` 或 `__compiled__` 判定「已打包」（Nuitka 不设 `sys.frozen`），注册表 Run 命令为 `"<exe>"` 而非 `"<exe>" "<exe路径>"`。
- **无控制台运行日志（★ v5.24.0）**：没有真实控制台时（编译后的 exe、pythonw），`stdout/stderr` 双写到程序目录 `MSU2_MINI_log.txt`（滚动上限 2MB，每次启动写分隔行）；有控制台时不写文件；`MSU2_MINI_NO_LOG=1` 关闭。
- **串口连不上原因可见（★ v5.24.0）**：串口打不开 / 设备无响应 / 设备校验失败的原因会汇总到信息框（限频 30 秒），如「COM4：串口打不开（可能被其他程序/另一个实例占用）」——不再只能看到“没连接”。
- **单实例保护（★ v5.25.0）**：命名互斥体 `MSU2_MINI_V2_single_instance`（本会话唯一，检查在 `__main__` 最前、串口初始化之前）；第二个实例启动时会把已运行实例的窗口切到前台（`EnumWindows` 按标题前缀找可见窗口 + `ShowWindow(SW_RESTORE)`/`SetForegroundWindow`），找不到则弹提示说明；例外：`--allow-multiple` 或 `MSU2_MINI_ALLOW_MULTIPLE=1` 可多开，`--quiet`/`MSU2_MINI_QUIET` 静默退出。彻底避免「两个实例各抢一块屏 → 一块屏像没连接」。
- UI 状态持久化（窗口几何/最大化/上次标签/信息框显隐 show_info/设为自动连接 auto_connect，程序级存 MSU2_MINI_ui.json）、**屏幕分辨率手动设置持久化（lcd_size，Detect_LCD_Size 优先应用，★ v5.16.0）**、**API/MQTT 字段即时自动保存（★ v5.16.0）**、**主控页控件按设备配置恢复（★ v5.17.0：`_apply_main_ui_to_config` 自动回填 RGB/填充/间隔/FPS/相机/镜像/分辨率）**、显示墙独立配置、所有设置退出时写盘完成再退出。
- 电视墙：显示墙（控件/画布两种渲染）+ 显示墙设置（快速布局/行列微调/「显示器标签内显示实时内容」开关，独立配置；布局在设备掉线/重连期间自动保留，不被重置为推荐值）。
- 静态页不重复刷新：照片/关于/纪念日/待办/农历等静态页内容无变化时跳过重绘与串口发送（内容指纹机制，配置修改/跨天自动重绘），长期挂机显著省资源。
- **硬件监控数据源可选（★ v5.12.0）**：设置 → 监控显示 → 硬件监控数据源 三选一——①**LibreHardwareMonitor**（默认，pythonnet/.NET 加载程序目录 `LibreHardwareMonitorLib.dll`，无后台程序）；②**AIDA64**（需 AIDA64 后台运行并开启「硬件监视→LCD→启用共享内存」，ctypes 读共享内存 `AIDA64_SensorValues`）；③**系统原生**（零后台：psutil/WMI/nvidia 程序内直读 CPU/内存/磁盘/电池/CPU温度/NVIDIA GPU，覆盖有限）。硬件详情/仪表盘/自定义显示/传感器选择框全部复用；未运行/未开启时提示。
- **DeepSeek 余额显示页（★ v5.13.0，v5.14.0 仅显示官方字段，v5.15.0 字体设置，v5.18.0 对齐方式，v5.19.0 自定义模板）**：第 28 个页面「DeepSeek余额」——查询官方 `GET https://api.deepseek.com/user/balance`，自定义勾选显示官方真实返回字段：总余额 / 赠送余额 / 充值余额 / 币种 / 账户是否可用（**不做任何估算**）；**每个显示项可自定义显示模板（%1=值 %2=币种，如「自定义 %1 元」，留空用默认格式）**；**字体自适应屏幕（默认，基于内容宽高：单行/少行上限 50 大号显示，长内容/多行自动缩小）或手动字号**；**对齐方式（垂直居中默认 / 向上对齐）**；**API Key 存程序目录 `.env` 的 `DEEPSEEK_API_KEY`**（设置页可填写并「保存到 .env」，密钥不入源码/配置 JSON）；后台线程抓取 + TTL 自动刷新（默认 300s，可配置/关闭）；背景/字体颜色可配（含配色方案）。

---

## 四、维护约定（每次更新必须遵守）

1. **本文档与代码同步**：每次软件更新（新增/修改/删除功能、修复、重构、改配置字段/API/页面）后，必须在本文档「二、详细迭代记录」追加对应版本条目，并在「三、当前版本完整功能清单」同步增删，避免原有功能遗漏/丢失。
2. **主程序为最终版**：`MSU2_MINI_V2.py` 是最终更新版与最终编译文件；新改动统一合入主程序，历史版本只读保存在 `version/`。若把新版本文件归档到 `version/`，建议文件名标注版本号与日期。
3. **版本号同步与备份（强制）**：每次发布新版本必须：①更新主程序 `PROGRAM_VERSION` 与 `PROGRAM_CHANGELOG`；②把主程序复制到 `version/` 子目录，命名 `MSU2_MINI_V2 - X.X.X 简短说明.py`；③同步更新本文档。三处必须一致，避免「改了功能忘了改版本号/备份」。
4. **PROGRAM_CHANGELOG 同步**：主程序顶部 `PROGRAM_CHANGELOG` 的版本说明应与本文档保持一致（新增版本时两者都更新）。
5. **设置实时自动保存（强制）**：所有「设置」项必须实时自动保存 + 持久保存——新增设置项时在 `sys_config.__init__` 加字段 → UI 控件变更信号回调里 `_lock()`（`_ui_set_active()` 锁定本屏）→ 改 `config_obj` → `save_config()`（5 秒防抖 → 快照 + 原子写盘到本设备配置文件），下次启动经 `load_config()` 恢复。**新增设置项必须保存/加载成对实现**，并在本文档功能清单与代码地图同步。
6. **API 维护约定**：新增/修改/删除 API 端点时，必须同步更新 `_build_openapi_doc()` 的 paths 与 schemas，并同步 `_API_DOC_HTML` 说明；启动时 `_check_openapi_sync()` 会校验，不一致会打印警告。
7. **编译/发布约定（★ v5.23.0，强制）**：①改版本号时同步 `0_nuitka_build.bat` 顶部的 `APP_VER`（exe 属性版本，与 `PROGRAM_VERSION` 保持一致）；②发布 = 拷贝**整个 `build_output\MSU2_MINI_V2.dist\` 目录**（单独拷 exe 无法运行）；③程序目录 = exe 所在目录，设置在同目录 `config\`、密钥在 `.env`，换机器要一起带（脚本已自动同步）；④新增第三方 DLL/数据文件必须加进 bat 的 `--include-data-*`（如硬件监控 DLL 目录）；⑤编译用脚本即可（已自动 cd 到项目目录），且**不要并发两个编译进程**。
7. **脚手架同步（⚠ 已过时，勿执行）**：`_scaffold/_qt_ui_page_body.py` 目前只含早期 UI_Page，**缺少** `_build_main_tab`/`_build_settings_tab`/`_build_scheme_settings` 等大量现有函数。**切勿运行 `_apply_qt_ui.py`**——它用过时 body 整体覆盖主文件 UI_Page，会摧毁配色方案/监控显示/懒加载等功能。主文件 `UI_Page` 已是唯一活跃源，改动直接改主文件即可；脚手架/测试脚本一律放 `_scaffold\` 子目录，不散落根目录。
8. **`.env` 存放密钥（强制）**：所有 **API Key / 令牌 / 数据库密码** 等敏感凭据**一律放在程序目录 `.env` 文件**（或环境变量）中，用 `os.environ.get(...)` 读取，**严禁把真实密钥直接写进 `MSU2_MINI_V2.py` 源码**（也不得写进 `version\` 备份、`config\` 配置 JSON 或本文档）。约定：①新增外部服务密钥（API Key / Token / 数据库 / Webhook 密码等）时在 `.env` 加一行 `键=值`，代码里用 `os.environ.get("键", "")` 读取（缺省空串），可附 `.env.example`（占位值模板）方便他人配置；②`.env` 不入版本库、不参与 Nuitka 打包（exe 运行时从同目录读取）；③默认无需鉴权/匿名的场景（如 MQTT 空用户名）保持为空即可，勿造假密钥。
9. **配色/颜色相关**：新增任何可改颜色的渲染位置时，需同步：渲染函数用可配置背景+字体色（`_hex2rgb`）、`sys_config.__init__` 加字段、`_API_CONFIG_WRITABLE` 加白名单、UI 加色块下拉/方案行/存为新方案（`_make_color_row`/`_make_scheme_row`），并刷新 `_scheme_combos`。
10. **本文档持续同步（强制）**：每次对 `MSU2_MINI_V2.py` 的任何更新（新增/修改/删除功能、修复、重构、改配置字段/API/页面）都必须同步更新本文档：①在「二、详细迭代记录」追加条目；②在「三、当前版本完整功能清单」增删对应项；③若涉及架构/模块/函数位置/配置字段变化，同步维护「五、架构分析（代码地图）」中的行号地图与函数清单，避免功能与设置丢失。
11. **构建脚本依赖同步**：`0_nuitka_build.bat` 的 `--include-package` 列表必须覆盖主程序**所有可选依赖**（含函数内延迟导入的：`pynvml`/`wmi` 系统原生监控 v5.12.0、`grpc`/`google.protobuf` gRPC v5.9.0、`paho` MQTT v5.10.0、`zmq` ZeroMQ）。新增可选依赖时同步补 `--include-package`（本机未安装则不会打包，exe 运行时自动跳过）；`requests` 仅出现在 API 文档字符串示例中非真实依赖，无需打包；gRPC 消息为动态构建（descriptor_pool+message_factory），`msu2_api_pb2*` 无需打包。

---

## 五、MSU2_MINI_V2.py 架构分析（代码地图）

> 目的：给后续维护一份「代码地图」——说明主文件整体架构、各模块位置、线程模型、配置体系与维护要点，改代码时先查这里，避免遗漏需要同步的位置。
> 行号以 **2026-09-13 v5.25.0（单实例保护）** 为准；改动代码后请同步更新本节的函数行号。
> ⚠ **行号偏移**：各版本累计插入 gRPC/MQTT/AIDA64/系统原生/DeepSeek（v5.13.0 新增 `.env` 密钥机制 + DeepSeek 余额页，约 260 行；v5.14.0 去除估算；v5.15.0 字体设置；v5.16.0 持久化修复；v5.17.0 主控页恢复；v5.18.0 对齐方式；v5.19.0 自定义模板；v5.20.0 字号上限；v5.21.0 字号自适应优化；v5.22.0 多屏修复，主程序 15824 行）；**下表关键行号已按 v5.22.0 重新核对**，后续改动后请以主程序为准更新本节。
> ★ **v5.22.0 改动点（行号）**：`_primary_device`（908，主设备槽位，不再由 UI 改写）/`_default_device`（910，默认活跃屏）/
> `COMM_FAIL_LIMIT`（912，通信失败阈值 3）/`get_current_device`（915）/`set_current_device`（923）/`set_default_device`（928，新增）；
> `ScreenDevice.comm_fail_count`（ScreenDevice.__init__ 内）；`set_device_state`（13461，改为连续失败累计判掉线）；
> `SER_Read` 成功收到响应即清零 `comm_fail_count`；`daemon_task` 端口扫描重写（15300，端口归属优先在 15389）；
> `manage_task`（15459，启动时 `set_current_device(dev)` 绑定本线程）；`_apply_show_info_state`（15629，新增）/
> `_show_info_widgets`·`_show_info_cbs`（15625）；`_ui_load_state`（13268，show_info 读取修复）；`_build_settings_tab`（10438，登记勾选框）。
> ★ **v5.23.0 改动点（行号，主程序 `__main__` 在 15725）**：`_NO_WINDOW_FLAGS`（44，新增，`CREATE_NO_WINDOW`，
> 供子进程不弹黑框）/`PROGRAM_VERSION`（108）/`set_auto_start`（8377，打包判定改 `sys.frozen or "__compiled__"`）/`ping_worker`（13910，子进程补 `creationflags`）/`fetch_battery`（14826，同上）。
> 其余行号相对 v5.22.0 基准 **+34**（顶部新增 `_NO_WINDOW_FLAGS` 5 行 + `PROGRAM_CHANGELOG` 新条目 29 行）。
> ★ **v5.24.0 改动点（行号，主程序 `__main__` 在 15869）**：`_TeeWriter`（8270）/`_setup_log_file`（8314，启动即调 8341）/
> `set_auto_start`（8473）/`set_device_state`（13589）/`_note_connect_failure`（13654）/`_report_connect_failures`（13664）/
> `Get_MSN_Device`（13686）/`ping_worker`（14050）/`fetch_battery`（14966）/`daemon_task`（15474，失败汇报 15603）/`manage_task`（15637）。
> 其余行号相对 v5.23.0 基准 **+144**（日志区约 140 行）。
> ★ **v5.25.0 改动点（行号，主程序 `__main__` 在 15992）**：`import ctypes`（41，新增模块级导入）/
> `_setup_log_file`（8332）/`_SINGLE_INSTANCE_NAME`（8367）/`acquire_single_instance`（8371）/
> `_find_running_main_window`（8409）/`notify_already_running`（8438）/`set_auto_start`（8596）/`daemon_task`（15597）；
> `__main__` 里单实例检查在最前面（`_init_single_device()` 之前）。其余行号相对 v5.24.0 基准 **+123**。

### 5.1 总体架构

- **单文件程序**（约 15400 行），`if __name__ == "__main__"`（15342）为唯一入口。
- **技术栈**：Python 3.10 + **PySide6 (Qt 6.11)** 界面层；`pyserial` 串口通信；`numpy`/`cv2`/`PIL` 图像处理与 RGB565 编码；`psutil` 系统监控；`LibreHardwareMonitor.NET`（外置进程）硬件传感器；**AIDA64（共享内存 `AIDA64_SensorValues`，ctypes 读取，★ v5.11.0）**；**系统原生（psutil/WMI/nvidia 程序内直读，零后台，★ v5.12.0）**；**DeepSeek 余额（官方 GET /user/balance，API Key 存程序目录 .env，★ v5.13.0）**；`pystray` 托盘；`http.server`/`websocket` 本地 API；**gRPC 接入（`grpcio`，`msu2.Msu2Api`）**；**MQTT 接入（`paho-mqtt`，连接外部 Broker）**；**Webhook 接收服务器（`WebhookReceiveHandler`，对接 Synology Chat 等聊天机器人）**。
- **分层**：业务逻辑层（串口寄存器/渲染/配置/API/烧写/数据抓取/Webhook/硬件监控）全部为模块级函数；**UI 层集中在 `UI_Page()`**（9054~13200，PySide6 主界面，含所有构建函数与后台线程启动）。
- **多设备**：`ScreenDevice` 每屏一个实例；全局 `all_devices` 字典 + `_primary_device`（UI 活跃屏）；`set_current_device()`/`get_current_device()` 线程本地活跃设备（daemon 渲染时指向当前渲染屏）。

### 5.2 文件行号地图（顶层区域）

> 行号为函数/类定义所在行；区域为大致范围。**表中历史行号为 2026-08-27 基准（随版本更新会漂移）**；
> 带 ★ 的条目为各该版本（v5.26.0 / v5.27.0 / v5.28.0 / v5.29.0）实测行号；
> **v5.29.0（2026-09-21）起整体行号再次上移（主程序共约 16650 行）**，最新基准见 ★v5.29.0 行与 5.9 节。

| 行号 | 内容 |
|---|---|
| 16~99 | PySide6 Qt 库导入、全局常量（SHOW_WIDTH=160/SHOW_HEIGHT=80、颜色、锁等） |
| 100~308 | 程序元数据：`PROGRAM_VERSION`（103，**每次发布需同步**）/`PROGRAM_CHANGELOG`（125）/`get_program_info`（302）/`get_about_lines`（313） |
| 309~506 | 内联 **MiniMark 模板引擎**（`MiniMark`（389）/`MiniMarkParser`（401），自定义显示渲染） |
| 507~806 | 内联 **ContinuousCapture**（528，PrintWindow/mss 窗口捕获） |
| 807~1071 | **多设备基础设施**：`ScreenDevice` 类（847，含 `_static_fp` 静态页指纹、`comm_fail_count` 通信失败计数 ★v5.22.0）、`_init_single_device`（989）、`get_current_device`（915）/`set_current_device`（923）/`set_default_device`（928，★v5.22.0 默认活跃屏）、`COMM_FAIL_LIMIT`（912）、`get_all_cameras`（997）/`get_all_windows`（1014） |
| 1072~1215 | `Win32_Image`（1091）、`_mss_fallback_screenshot`（1107）、`get_window_image`（1127，截图通道） |
| 1216~1284 | **UI 消息队列**（`insert_text_message`（1235）/`_process_ui_msg_queue`（1286），工作线程→主线程安全刷新） |
| 1285~1542 | 照片/动图烧写入口（`Get_Photo_Path`（1352）/`Start_Write_Photo_Path`（1370）/`Write_Photo_Path1~4`（1386/1402/1420/1438）） |
| 1543~1797 | 页面/方向切换（`state_change_set`（1562）/`Page_UP`（1591）/`Page_Down`（1620）/`do_key_action`（1648）/`LCD_Change`（1733）/`sync_page_combobox`（1685）/`apply_language`（1773）等）+ 每屏主控 ctx（`_cur_main_ctx`（1680）） |
| 1798~2130 | **串口寄存器读写**：`SER_Write`（1817）/`SER_Read`（1860）/`SER_rw`（1893）、`Read_M_u8`（1933）/`Write_M_u8`（1969）、`Read_ADC_CH`（2005，按键+心跳）、`Read_M_SFR_Data`（2038）、`Read_MSN_Data`（2106）/`Write_MSN_Data`（2130） |
| 2130~2335 | **Flash 烧写**：`Write_Flash_Page`（2149）/`_fast`（2176）/`Erase_Flash_page`（2202）/`Read_Flash_byte`（2220）/`Write_Flash_Photo_fast`（2252）/`Write_Flash_hex_fast`（2290）/`Write_Flash_ZK`（2317，字库） |
| 2336~2817 | **LCD 指令**：`LCD_Set_XY/Size/Color`（2355/2366/2377）、`LCD_State`（2455，方向）、`LCD_DATA`（2481）、`Write_LCD_Photo/Screen_fast(_1)`（2501/2536/2604/2669）、白底/混合字体（2717~2797）、`LCD_Color_set`（2817，清屏）、`update_burn_offset`（2413，防烧屏偏移） |
| 2831~2980 | `show_gif`（2837，动图）/`show_PC_state`（2871，系统监控）/`show_Photo`（2986，相册） |
| 2978~3115 | 预览软件渲染（`_update_preview_clock`（2997）/`_update_preview_state`（3022））、`show_PC_time`（3050）、`Screen_Date_Process`（3084） |
| 3116~3252 | **图像编码/缩放**：`rgb888_to_rgb565`（3135）/`rgb565_to_rgb888`（3164）、`shrink_image_block_average`（3168）、`set_select_hwnd`（3248） |
| 3266~3780 | **每屏截屏/处理/FPS 线程**：`screen_shot_task`（3272）/`screen_process_task`（3484）/`fps_control`（3461）、**能效等级辅助 `_power_level`（3399）`_power_factor`（3413）`_power_active`（3420）`_apply_process_priority`（3425，进程空闲优先级）`_power_wait`（3448）**、`show_PC_Screen`（3705，屏幕镜像/相机）、`_safe_send_rgb888`（3769）、`sizeof_fmt`（3747） |
| 3735~6666 | **本地 API 投屏服务器**（HTTP+WebSocket+TCP/UDP/管道/热文件夹/ZMQ/**gRPC**/**MQTT**/stdin/SSE；`api_execute_command`（4507）统一命令（含 webhook_send/webhook_status）；`ApiHandler`（4580）；`_build_openapi_doc`（4944）；**gRPC `_GRPC_PROTO`（6206）`_api_grpc_build_messages`（6248）`start_api_grpc`（6305）**；**MQTT `start_api_mqtt`（6428，连接外部 Broker，需 paho-mqtt）**；`stop_api_extra`（6561，含 gRPC/MQTT 停止）`_api_enabled_protocols`（6659，附加协议开关解析）`start_api_server`（6666）/stop；屏幕序号检测/强制投屏） |
| 6743~7097 | **Webhook 对接**（★ v5.8.0）：`_webhook_extract_text`（6743，消息文本提取）、`webhook_receive`（6797，接收核心）、`_api_try_webhook_overlay`（6826，屏幕叠加显示）、`webhook_send`（6867）/`_webhook_send_worker`（6883，后台发送）、`api_apply_webhook_send`（6914）/`api_get_webhook_status`（6925）、`WebhookReceiveHandler`（6946，接收服务器）、`start_webhook_server`（7059）/`stop_webhook_server`（7081）/`webhook_apply_server_state`（7097） |
| 6783~7920 | 网络速率渲染 `_render_two_line_bars`（7215）`/show_netspeed`（7249）、`load_hardware_monitor`（7290，LibreHardwareMonitor 加载）、**`load_aida64_monitor`（7420，AIDA64 共享内存读取器 ★ v5.11.0）**、**`load_system_monitor`（7607，系统原生零后台读取器 ★ v5.12.0）** |
| 7792~8015 | 自定义显示渲染（`get_draw_text`（7792）`/show_custom_two_rows`（7814）`/get_full_custom_im`（7895）`/show_full_custom`（7975），首次进入后台惰性加载硬件监控） |
| 8016~8310 | **配置体系**：`save_config`（8016，延迟写盘+快照防串屏）`save_config_thread`（8037）`/load_config`（8062，含 api_protocols 旧配置迁移）`device_config_path`（8112）`/load_device_config`（8137，含迁移）`set_active_device_config`（8194）`/_ui_set_active`（8202）`/set_auto_start`（8213）`/export_config`（8240）`/import_config`（8254）`/check_update`/窗口枚举辅助；**`.env` 密钥机制 `_load_env_file`（8098）`/_save_env_key`（8119，★ v5.13.0）** |
| 8266~8640 | **配色方案库** `BUILTIN_COLOR_SCHEMES`（8266，260+ 色系）、`get_all_color_schemes`（8628）`/parse_color_list`（8639） |
| 8715~8880 | **`sys_config` 配置类**（8715，所有可持久化字段，见 5.6；含 `power_mode` 能效等级 1~5、`api_protocols` 附加协议开关（tcp/udp/hotfolder/pipe/unix/zmq/grpc/**mqtt**）、**MQTT 7 字段 `mqtt_host/port/username/password/command_topic/response_topic/frame_topic`**、**Webhook 6 字段 `webhook_enable/port/urls/auto_display/display_seconds/receive_token`**、**硬件监控数据源 `hardware_source`（libre/aida64/system，★ v5.12.0）**、**屏幕分辨率 `lcd_size`（★ v5.16.0 持久化）**、**DeepSeek 8 字段 + `deepseek_custom_templates`（dict 自定义模板，★ v5.19.0）`（`deepseek_show_items/auto_refresh/refresh_interval/bg_color/text_color/font_auto/font_size/align`，★ v5.13.0；API Key 存 .env，仅显示官方返回字段，字体自适应+手动，对齐方式）**） |
| 7990~8157 | LCD 分辨率检测：`Detect_LCD_Size`（7990）/`ReDetect_LCD_Size`（8108）/`Set_LCD_Size_Manual`（8122）/`Cleanup_LCD_On_Exit`（8151） |
| 8985~13050 | **`UI_Page()`**（PySide6 三层界面，见 5.8；API接入页含附加协议开关（**含 gRPC/MQTT 勾选框**）与 **MQTT 接入配置分组**（Broker 地址/端口/用户名/密码/命令/响应/帧主题）；**设置页 Webhook 标签**：启用/端口/令牌/自动显示/时长/发送 URL/测试发送/应用重启；**监控显示页顶部「硬件监控数据源」下拉（LHM/AIDA64/系统原生，★ v5.12.0）**） |
| 12090~12145 | `MSN_Device`（12090）`MSN_Data`（12098）数据类、`set_device_state`（12107）、`_dump_usb_descriptor`（12129） |
| 12147~12274 | `Get_MSN_Device`（12147，设备握手/SN/连接初始化） |
| 13189~13416 | **`_static_page_loop`（13189，静态页内容指纹：内容不变跳过重绘/发送）**`MSN_Device_1_State_machine`（13210，每屏渲染状态机，按页面分发：屏幕序号检测 → 强制投屏 → **Webhook 叠加 `_api_try_webhook_overlay`（6830）** → 各页面；静态页接入指纹机制） |
| 13444~14854 | 各显示页面 `show_*` 函数（天气/行情/热搜/电池/音乐/硬件详情/仪表盘等，刷新点已接入能效 `_power_wait`；照片/关于/纪念日/待办/农历经 `_static_page_loop` 内容不变不重绘）→ **DeepSeek 余额（★ v5.13.0）：`_parse_deepseek_balance`（14727）`/_deepseek_build_lines`（14753）`/fetch_deepseek_balance`（14782）`/show_deepseek_balance`（14809）**；数据抓取 `fetch_*` 后台线程（`fetch_weather`（14326）`/fetch_battery`（14507）`/ping_worker`（13486）） |
| 14898~15340 | 全局变量区、**`_ensure_hardware_monitor_async`（14903，硬件监控惰性加载：按 hardware_source 选 LHM/AIDA64/系统原生）**`/load_task`（14951，PAGE_ID 标签）`daemon_task`（15300）`/manage_task`（15459，按键 ADC，能效降频） ＋**`_deepseek_cache`（15295，DeepSeek 余额缓存 ★ v5.13.0）**、**`_show_info_widgets`/`_show_info_cbs`/`_apply_show_info_state`（15625/15626/15629，★ v5.22.0 信息框显隐同步）** |
| 15342~15440 | `__main__`（15342）：初始化、线程启动、崩溃日志、退出清理（join 线程 + 保存配置 + 清 LCD + **stop_api_server/stop_webhook_server**） |
| ★v5.26.0 新增/改动 | **画面刷新健康度 + 每屏健康检查**（"屏黑着但显示已连接"的排查与自愈）：`_note_write_error`（2014，串口写失败登记）、`ScreenDevice` 健康字段（1093：`last_frame_ok_time`/`write_error_seq`/`last_write_error`/`render_error_count`/`_frame_stall_notified` 等）、`FRAME_STALL_SECONDS`（3929）/`_note_picture_ok`（3934）/`_mark_frame_sent`（3943）/`device_frame_status`（3956）、`_device_health_tick`（15730，daemon 渲染前调用：通信心跳/线程看护/画面停滞提示）、`daemon_task` **逐屏 try/except**（15788）、方向看门狗后 `state_change=1`（`MSN_Device_1_State_machine` 14091）、状态栏画面刷新（`_periodic_refresh` 13728）、设备信息页「画面刷新/渲染异常」行（`_build_hw_tab` 12622）、回归测试 `_scaffold/smoke_test_device_health.py`（30 项）、排查脚本 `_scaffold/run_dev_with_log.bat` |
| ★v5.27.0 新增 / ★v5.36.0 逐柱 / ★v5.37.0 单位色+门槛 / ★v5.38.0 默认马卡龙五色+色系 | **速度单位换色**（柱状图按单位/门槛换色）：`SPEED_UNIT_MB_BYTES`(8083)/`_render_cfg()`(8086)/`_speed_unit_is_mb()`(8094)；★v5.37.0 「逐柱优先级链」：常量 `SPEED_UNIT_KEYS`(8105)/`SPEED_UNIT_LABELS`(8106)/`SPEED_UNIT_SCALE`(8107)/`SPEED_RULE_OPS`(8108)/`SPEED_RULE_ROWS`(8110)/`SPEED_RULE_EQ_TOL`(8111)，函数 `_speed_unit_key()`(8175)/`_speed_unit_field()`(8192，MB 沿用老字段名 → 老配置继续生效)/`_speed_rule_hit()`(8199)/`_speed_rule_unit_scale()`(8222)/`_speed_rule_list()`(8227)/`_speed_parse_color()`(8239，空→None、支持 `#rgb`) 与 **`_speed_bar_color_resolver(row, base)`(8247)**（每帧每行构造一次，柱循环内逐柱调用；开关关=直返原色），v5.36.0 的 `_speed_unit_palette()`/`_speed_unit_bar_color()` 已删除；★v5.38.0 **色系**：色系表 **`SPEED_UNIT_SCHEMES`(8114)**（7 套，每套 = (名称, 上行五色, 下行五色)，顺序 = `<1K(B)`/`KB`/`MB`/`GB`/`TB`，首套 = 马卡龙）、`SPEED_UNIT_SCHEME_NAME`(8137)/`SPEED_UNIT_BAR1_DEFAULT`(8138)/`SPEED_UNIT_BAR2_DEFAULT`(8139)/`SPEED_UNIT_SCHEME_CUSTOM`(8140)、`_speed_scheme_colors()`(8143)/`_speed_current_scheme()`(8151，按 10 色反查)；接入点 `_render_two_line_bars`(8296，网络流量 + 磁盘读写经典2) 与 `_show_diskio_netspeed`(15609，磁盘读写网速样式)；`sys_config` 11 个字段（`speed_unit_color_enable`/`speed_unit_bar1_color`/`speed_unit_bar2_color` + `speed_unit_bar{1,2}_{b,kb,gb,tb}_color` + **`speed_color_rules`**，默认值 10104~10112 由 `SPEED_UNIT_SCHEMES[0]` 循环写入）；设置 UI 在「监控显示」（`_build_monitor_settings` 13112，开头套 `QScrollArea` 防小窗口裁切）：开关 + **色系下拉框(13186)**/`_refresh_scheme_combo`(13195)/`_apply_scheme`(13205) + 单位色网格 `_unit_color_cell`(13223) + 门槛编辑 `_make_rule_row`/`_add_rule`/`_rebuild_rules`；`_API_CONFIG_WRITABLE` 仍只有 3 个 int/str 键；测试 `_scaffold/smoke_test_speed_unit_color.py`（**v5.38.0 起 116 项**）+ 预览 `_scaffold/preview_speed_unit_color.py` + 界面截图 `_scaffold/shot_speed_unit_color_ui.py` |
| ★v5.38.0 实测行号（主程序共 17499 行 / 830800 字节 / 纯 LF，2026-09-22） | `PROGRAM_VERSION`(112)/`SPEED_UNIT_MB_BYTES`(8083)/`_render_cfg`(8086)/`_speed_unit_is_mb`(8094)/`SPEED_UNIT_KEYS`(8105)/**`SPEED_UNIT_SCHEMES`(8114)**/`SPEED_UNIT_SCHEME_NAME`(8137)/`SPEED_UNIT_BAR1_DEFAULT`(8138)/`SPEED_UNIT_BAR2_DEFAULT`(8139)/`SPEED_UNIT_SCHEME_CUSTOM`(8140)/`_speed_scheme_colors`(8143)/`_speed_current_scheme`(8151)/`_speed_unit_key`(8175)/`_speed_unit_field`(8192)/`_speed_rule_hit`(8199)/`_speed_rule_unit_scale`(8222)/`_speed_rule_list`(8227)/`_speed_parse_color`(8239)/`_speed_bar_color_resolver`(8247)/`_render_two_line_bars`(8296)/`sys_config`(9980，速度字段 10104~10112)/`_build_monitor_settings`(13112，开头的滚动容器)/`色系下拉框`(13186)/`_refresh_scheme_combo`(13195)/`_apply_scheme`(13205)/`_unit_color_cell`(13223)/`_show_diskio_netspeed`(15609) |
| ★v5.28.0 新增 | **通信有效性 / 设备复位自恢复**：`_log_throttle_time`/`_throttled_print`/`_throttled_dev_print`、`_looks_like_boot_banner`、`_force_device_reset`、`_note_comm_ok`/`_note_comm_fail`（均在 `SER_Write` 之前，约 2050~2150）；`SER_Read` 末尾改为「横幅 → `_force_device_reset`；其余保留 v5.22.0 容错」；`SER_rw` 加响应校验（回显=最后一个子命令头）；`ScreenDevice` 新字段 `comm_ok_time`/`last_comm_note`/`comm_note_time`/`reset_banner_count`/`connected_time`（`set_device_state(1)` 时记连接时刻）；各处串口/LCD/监控失败打印改限频；`device_frame_status` 文案改「串口通信异常」；回归测试 `_scaffold/smoke_test_comm_health.py`（33 项） |
| ★v5.29.0 新增/改动 | **整帧发送「窗口 + 数据」一次发出（画面倾斜/斜切根治）+ 空档观测**：`_lcd_window_bytes`（2880，只构造地址窗口字节，含防烧屏偏移）、`LCD_ADD`（2907，改为调用它并在成功后标记 `frame_window_time`）、`_close_frame_window_gap`（4212，统计空档时长）、`_send_frame_with_window`（4231，★核心：窗口 + 整帧拼成一条流、一次 `SER_rw`）、`_note_frame_gap_intrusion`（2122，空档期插入计数 + 限频日志，`SER_rw` 内调用）、`ScreenDevice` 新字段（`frame_window_time`/`frame_gap_count`/`frame_gap_max`/`frame_gap_intrusions`，约 1165）、接入点 `_safe_send_rgb888`（4351）与 `show_PC_Screen`（4284）、设备信息页「帧空档」行（`_build_hw_tab` 约 13100）；回归测试 `_scaffold/smoke_test_frame_gap.py`（26 项） |
| ★v5.29.0 实测行号（主程序约 16650 行，2026-09-21） | `PROGRAM_VERSION`(112)/`PROGRAM_CHANGELOG`(135)/`COMM_FAIL_LIMIT`(1075)/`ScreenDevice`(1102)/`_note_frame_gap_intrusion`(2122)/`_throttled_print`(2156)/`_looks_like_boot_banner`(2177)/`_force_device_reset`(2197)/`SER_Write`(2239)/`SER_Read`(2282)/`SER_rw`(2323)/`_lcd_window_bytes`(2880)/`LCD_ADD`(2907)/`LCD_State`(2927)/`_note_picture_ok`(4190)/`_mark_frame_sent`(4199)/`_close_frame_window_gap`(4212)/`_send_frame_with_window`(4231)/`show_PC_Screen`(4284)/`_safe_send_rgb888`(4351)/`sys_config`(9506)/`_render_two_line_bars`(7825)/`show_netspeed`(7862)/`Get_MSN_Device`(14326)/`MSN_Device_1_State_machine`(14481)/`_show_diskio_netspeed`(14879)/`_device_health_tick`(16122)/`daemon_task`(16188)/`manage_task`(16379)/`__main__`(16611) |
| ★v5.30.0 实测行号（主程序约 16922 行，2026-09-21） | `PROGRAM_VERSION`(112)/`COMM_FAIL_LIMIT`(1104)/`SERIAL_QUIET_AFTER_FRAME`(1128)/`LCD_WATCHDOG_SECONDS`(1137)/`_is_static_redraw_page`(1141)/`ScreenDevice`(1173)/`_lcd_window_bytes`(2956)/`LCD_ADD`(2983)/`LCD_State`(3003)/`_serial_begin`(4283)/`_serial_end`(4291)/`_serial_transaction_busy`(4301)/`_note_serial_quiet_violation`(4319)/`_send_frame_with_window`(4368)/`show_PC_Screen`(4436)/`_safe_send_rgb888`(4503)/`_device_health_tick`(16279)/`daemon_task`(16348)/`manage_task`(16546)/`__main__`(16783) |
| ★v5.30.0 新增/改动 | **串口事务模型 + 帧自愈 + 看门狗收敛（画面倾斜第二条成因）**：常量区 `_env_int`/`SERIAL_QUIET_AFTER_FRAME`/`SERIAL_QUIET_AFTER_DIRCHANGE`/`SERIAL_WRITE_PACE`/`SERIAL_WRITE_CHUNK`/`SERIAL_WRITE_RESUME_DEADLINE`/`KEY_POLL_DISABLED`/`SERIAL_BUSY_STALE`/`LCD_WATCHDOG_SECONDS`/`LCD_WATCHDOG_DISABLED`/`_is_static_redraw_page`（均在 `COMM_FAIL_LIMIT` 之后）；`ScreenDevice` 新字段 `serial_busy_since`/`serial_quiet_until`/`frame_resend_count`/`serial_quiet_violations`；事务辅助 `_serial_begin`/`_serial_end`/`_serial_transaction_busy`/`_note_serial_quiet_violation`（在 `_note_picture_ok` 之后）；`_send_frame_with_window` 加整帧重发；`SER_Write` 短写续写；`SER_rw` 加静默违规观测；`LCD_State` 设方向静默期；`show_PC_Screen`/daemon 渲染改用事务辅助；`manage_task`/`_device_health_tick` 避让；方向看门狗只对静态页补 `state_change`；测试 `_scaffold/smoke_test_serial_transaction.py`（40 项） |
| ★v5.35.0 新增/改动 | **行级地址重锚（倾斜根治）**：常量 `ROW_GUARD_ROWS`（1161，`MSU2_MINI_ROW_GUARD`；1=每行重锚、0=关闭）、`_lcd_row_anchor_bytes`（3668）、重写 `Screen_Date_Process`（3693，按行重锚 + 行对齐提交，参数 `row_width`）、调用点 `screen_process_task`（`row_width=LCD_MAX_X`）与 `_safe_send_rgb888`（4579，`row_width=SHOW_WIDTH`）；测试 `_scaffold/smoke_test_row_anchor.py`（19 项，含虚拟小屏损控量化断言） |
| ★v5.35.0 实测行号（主程序约 16922 行，2026-09-22） | `PROGRAM_VERSION`(112)/`COMM_FAIL_LIMIT`(1130)/`SERIAL_QUIET_AFTER_FRAME`(1154)/`ROW_GUARD_ROWS`(1161)/`LCD_WATCHDOG_SECONDS`(1167)/`ScreenDevice`(1203)/`_lcd_window_bytes`(2988)/`_lcd_row_anchor_bytes`(3668)/`Screen_Date_Process`(3693)/`_serial_begin`(4343)/`_serial_transaction_busy`(4361)/`_send_frame_with_window`(4443)/`show_PC_Screen`(4512)/`_safe_send_rgb888`(4579)/`daemon_task`(16424)/`manage_task`(16622)/`__main__`(16860) |

### 5.3 核心类

| 类 | 行号 | 职责 |
|---|---|---|
| `MiniMark` / `MiniMarkParser` | 389/401 | 自定义显示模板引擎（p/a/m/t/f/c/i/v 等命令） |
| `ContinuousCapture` | 528 | PrintWindow 窗口捕获（每线程独立实例） |
| `ScreenDevice` | 847 | **每屏完整上下文**：串口/状态/线程/截图流水线/独立配置/LCD 尺寸/预览/传感器数据 |
| `Win32_Image` | 1091 | 窗口/屏幕截图封装（含 mss 回退） |
| `sys_config` | 8653 | **配置类**（所有可持久化字段 + 默认值，含 `power_mode` 能效等级 1~5、**Webhook 6 字段**、`api_protocols` 含 grpc/mqtt、**MQTT 7 字段**、**`hardware_source` 硬件监控数据源 libre/aida64/system（★ v5.12.0）**） |
| `ApiHandler` | 4563 | HTTP API 处理（do_GET/do_POST/WebSocket，含 `/api/webhook/send`、`/api/webhook/status`） |
| `WebhookReceiveHandler` | 6788 | **Webhook 接收服务器**（`/webhook/incoming` 接收 / `/webhook/send` 转发 / `/webhook/health`） |
| `_GrpcApiHandler`（`start_api_grpc` 内嵌套） | 6309 | **gRPC 通用处理器**（路由 `/msu2.Msu2Api/Execute`、`/WatchFrame`） |
| `Aida64MonitorManager`（`load_aida64_monitor` 内嵌套） | 7420 | **AIDA64 共享内存监控管理器**（★ v5.11.0，与 `HardwareMonitorManager` 接口鸭子兼容：sensors/get_value/get_value_formatted/list_sensors/update_hardwares；从值字符串解析数值并推断传感器类型与硬件类别） |
| `SystemMonitorManager`（`load_system_monitor` 内嵌套） | 7607 | **系统原生监控管理器**（★ v5.12.0，与 `HardwareMonitorManager` 接口鸭子兼容；psutil/WMI/nvidia 程序内直读，零后台进程；WMI 5 秒缓存） |
| `MSN_Device` / `MSN_Data` | 13002/13010 | 设备/数据描述 |
| `_WallCanvas` | UI_Page 内 | 画布方式显示墙（QPainter 统一绘制） |

### 5.4 页面系统（26 页 + 相机/自定义）

- 页面 ID 常量（0~27，28 页）定义在 681~758：`API_PAGE_ID=26`、**`DEEPSEEK_PAGE_ID=27`（730，DeepSeek 余额 ★ v5.13.0）**；`PAGE_ID`/`PAGE_ID_CN`/`PAGE_ID_EN` 为中英文名映射。
- **渲染状态机 `MSN_Device_1_State_machine`（13383）**：开头 `config_obj.state_machine = device.state_machine`（用本屏运行时页面，防多屏串台）；依次处理 屏幕序号检测 → 强制投屏 → **Webhook 消息叠加 `_api_try_webhook_overlay`（6830）** → 各页面 `show_*`（含 **DeepSeek 余额 `show_deepseek_balance`（14809）**）；含 LCD 看门狗（15s 强制方向重置）。
- **新增页面**需同步：PAGE_ID 常量 → PAGE_ID_CN/EN → 状态机 `elif` 分支 → `show_*` 渲染函数 → 页面内容设置（如需）→ API `/api/config/set` 白名单（如需）。

### 5.5 线程模型

| 线程 | 目标函数（行号） | 职责 |
|---|---|---|
| `daemon_thread` | `daemon_task`（15788） | 自动扫描/连接 WCH 设备（VID=0x1A86 ）、掉线重连、遍历所有已连接屏运行渲染状态机；★v5.22.0：端口扫描按 COM 端口归属 复用设备对象（15389），新设备索引 `max(keys)+1`，每轮渲染完把全局 config_obj 切 回主设备槽位 `_primary_device`；★v5.26.0：循环体改为**逐屏 try/except**（单屏渲染异常只计 `render_error_count` 并写信息框，不再中断整轮），且每屏渲染前调 `_device_health_tick`（15730：通信心跳 / 线程看护 / 画面停滞提示） |
| `load_thread` | `load_task`（14661） | 重建 PAGE_ID 标签排序（硬件监控已改按需惰性加载 `_ensure_hardware_monitor_async`（14613），不再无条件加载 LHM/AIDA64/系统原生） |
| `ping_thread` | `ping_worker`（12505） | 网络延迟检测（后台，能效等级放大间隔） |
| `manager_thread` | `manage_task`（15459） | 按键 ADC 检测（单击=下一页/双击=上一页/长按=切换方向），含串口避让（`serial_busy`）；能效等级降频（1级≈5Hz）；★v5.22.0 启动时 `set_current_device(dev)` 绑定本线程，避免“检查 A 屏、读写 B 屏串口” |
| 每屏 ×2 | `screen_shot_task`/`screen_process_task`（3272/3484，`ScreenDevice.start_threads`（956）幂等启动） | 截屏 + 图像处理/RGB565 编码入队（能效等级 fps 缩放） |
| 数据抓取 | `fetch_weather`（14432）`/fetch_crypto`（14463）`/fetch_hotsearch`（14482）`/fetch_battery`（14507）`/fetch_music`（14535）`/fetch_deepseek_balance`（14782，★ v5.13.0） | 天气/行情/热搜/电池/音乐/DeepSeek 余额 后台抓取 + 缓存 |
| API 协议 | `start_api_tcp/udp/hotfolder/pipe/unix/zmq`（5828/5874/5905/5969/6054+） | 各接入协议监听线程 |
| **gRPC** | `grpc.server.serve`（`start_api_grpc`（6305）→ `server.wait_for_termination` 线程） | gRPC 服务（Execute 命令 + WatchFrame 流式帧），grpc 内部线程池处理 |
| **MQTT** | `start_api_mqtt`（6428）→ paho `loop_start` + 帧发布线程 `_api_mqtt_thread` | 连接外部 Broker：命令主题收命令→响应主题发结果，帧主题推送 RGB888 |
| **Webhook 接收** | `WebhookReceiveHandler.serve_forever`（`start_webhook_server`（7059）） | 接收聊天机器人回调（`/webhook/incoming`），消息显示到屏幕/信息框 |
| **Webhook 发送** | `_webhook_send_worker`（6883，daemon 线程） | 把文本 POST 到配置的 URL 列表（Synology Chat 等） |
| 保存 | `save_config_thread`（8037） | 配置延迟写盘（用修改时快照，防多屏写串文件） |

> 主线程（Qt）另有几个 `QTimer`：`_periodic_refresh`（11401，2s，刷新设备列表/主控标签/下拉框；1级 8s）、`_wall_tick`（11157，1s，显示墙；1级 4s）与 `_auto_cycle_tick`（11436，1s，自动翻页）。

### 5.6 配置体系

- **主配置 `sys_config`（8715）**：所有字段按批分类——基础（文字 RGB/页面/方向/动图/相机/FPS/防烧屏/预览/**能效等级 `power_mode`（0~5，1级最省）**/**屏幕分辨率 `lcd_size`（★ v5.16.0）**）、自动化（自动翻页/息屏）、按键、页面内容（跑马灯/天气/热搜/时间/纪念日待办）、监控显示（进程/硬件详情/仪表盘/磁盘读写/网络流量/**速度单位换色 `speed_unit_color_enable`/`speed_unit_bar1_color`/`speed_unit_bar2_color`（★ v5.27.0）+ 8 个单位色 `speed_unit_bar{1,2}_{b,kb,gb,tb}_color` + 换色门槛列表 `speed_color_rules`（★ v5.37.0）——★ v5.38.0 起 10 个单位色**默认取自 `SPEED_UNIT_SCHEMES[0]`（马卡龙）**、设置页另有「色系」下拉框可整套换**、**硬件监控数据源 `hardware_source`（libre/aida64/system，★ v5.12.0）**、**DeepSeek 余额（`deepseek_show_items/auto_refresh/refresh_interval/bg_color/text_color/font_auto/font_size/align` + `deepseek_custom_templates`（dict 自定义模板），★ v5.13.0；API Key 存 .env，仅显示官方返回字段，字体自适应+手动，对齐方式，自定义模板）**）、**背景+字体颜色（20 个 `*_bg_color`/`*_text_color`，可配配色方案）**、API 接入（`api_enable`/`api_port`/`api_token`/`api_overlay`/`screen_id_timeout`/**`api_protocols` 附加协议开关（tcp/udp/hotfolder/pipe/unix/zmq/grpc/mqtt）**）、**MQTT 接入（`mqtt_host/port/username/password/command_topic/response_topic/frame_topic`，连接外部 Broker）**、**Webhook 对接（`webhook_enable`/`webhook_port`/`webhook_urls`/`webhook_auto_display`/`webhook_display_seconds`/`webhook_receive_token`，全部实时自动保存）**、自定义方案 `custom_color_schemes`。
- **按设备独立保存**：`config/MSU2_MINI_<SN>.json`（SN 冲突/为空按 COM 端口，`device_config_path`（8112）/`load_device_config`（8137））；`set_active_device_config`（8194）切换全局 `config_obj`。
- **实时自动保存（强制）**：设置页/主控页控件变更信号回调 → `_lock()`（`_ui_set_active()`，8202 锁定本屏）→ 改 `config_obj` → `save_config()`（8016）5 秒防抖写盘；写盘用**本次修改时的目标文件+配置快照**（`_pending_config_file`/`_pending_config_snapshot`），防 daemon 切换全局 `config_file`/`config_obj` 把设置写到别的屏文件；退出 `finally` 强制写盘并 `join` 等待完成（`__main__` 15051 内）。Webhook 设置页字段变更同样即时 `save_config()`（发送 URL 用 600ms 防抖），下次启动自动恢复并自动启动接收服务器。
- **独立配置文件**（程序级，与设备无关）：`config/MSU2_MINI_wall.json`（电视墙布局/方式）、`config/MSU2_MINI_ui.json`（窗口几何/最大化/上次第一层标签）。
- **API 白名单** `_API_CONFIG_WRITABLE`（4130）：`/api/config/set` 可改字段清单，新增配置字段需同步加入（★ v5.13.0 已含 6 个 deepseek_* 字段）。

### 5.7 本地 API 服务器 与 gRPC / MQTT / Webhook 对接（3735~7097）

- 多协议：HTTP REST / WebSocket / SSE / TCP(端口+1) / UDP(端口+2) / ZeroMQ(端口+3) / gRPC(端口+4，需 grpcio) / MQTT(连接外部 Broker，需 paho-mqtt) / 命名管道 / Unix Socket / 热文件夹(`hotfolder/`) / stdin。
- **统一命令执行器 `api_execute_command`（4480）**：screen/text/clear/slideshow/stop/page/mirror/page_next/page_prev/key/orientation/marquee/config_set/device_select/device_refresh/notify/quit/screen_id/**webhook_send/webhook_status**，均支持 `device` 指定目标屏。
- **OpenAPI 3.0**：`_build_openapi_doc()`（4916）生成 paths/schemas（含 `/api/webhook/send`、`/api/webhook/status` 与 **Webhook tag**），`GET /api/openapi.json` 提供；启动 `_check_openapi_sync()`（5752）校验同步。
- 网页投屏控制台 `show_api`（6753）+ `_API_DOC_HTML`（含第 12 节 Webhook 对接说明）；强制投屏 `_api_try_overlay`（6731）/屏幕序号检测 `_api_try_screen_id`（6680）。
- **gRPC 接入（★ v5.9.0）**：`_GRPC_PROTO`（6206）定义 `msu2.Msu2Api`（Execute 命令 / WatchFrame 流式帧）；`_api_grpc_build_messages`（6248）用 descriptor_pool+message_factory 动态构建（仅需 grpcio）；`start_api_grpc`（6305）启动在 127.0.0.1:api_port+4，未装 grpcio 自动跳过，导出 `msu2_api.proto` 到程序目录；`stop_api_extra`（6561）停止。
- **MQTT 接入（★ v5.10.0）**：`start_api_mqtt`（6428）以 paho-mqtt 客户端连接外部 Broker（设置 → API接入 → MQTT 接入：`mqtt_host/port/username/password/command_topic/response_topic/frame_topic`）；订阅命令主题执行 `api_execute_command`（响应发回响应主题），帧发布线程把当前活跃屏外部投屏帧（RGB888，有变化才发、≥0.5s 节流）推到帧主题；未装 paho-mqtt 自动跳过；`stop_api_extra`（6561）停止。
- **Webhook 对接（★ v5.8.0）**：接收服务器 `WebhookReceiveHandler`（6518，`start_webhook_server`（6631）/`stop_webhook_server`（6653）/`webhook_apply_server_state`（6669），默认端口 8633）——`POST /webhook/incoming` 接收聊天服务回调（兼容 Synology Chat/Discord/钉钉/通用 JSON/表单/纯文本，可选令牌 `X-Webhook-Token`/`?token=`），消息经 `webhook_receive`（6369）显示到屏幕（`_api_try_webhook_overlay`（6398）叠加、超时自动恢复）与信息框；`webhook_send`（6439）/`_webhook_send_worker`（6455）后台发送到配置 URL（Synology Chat「传入的 Webhook」等）；`GET /api/webhook/status`（`api_get_webhook_status`（6497））查询状态与收发日志。

### 5.8 UI 三层结构（`UI_Page` 内）

- **第一层 `top_nb`**：中控 | 电视墙 | 关于。
- **第二层 `main_notebook`**：屏幕1/屏幕2…（每屏一套主控控件，ctx 存 `_main_ctxs[dev.index]`，`_cur_main_ctx()` 取当前屏）。
- **第三层 `inner_nb`**（每屏内）：主控 | 设置 | 设备信息（设置/设备信息**首次点开懒构建**）。
- 关键构建函数：`_build_main_tab`（9768）、`_apply_main_ui_to_config`（★ v5.17.0，UI_Page 内：设备配置就绪/变化时把配置刷回主控页控件）、`_build_settings_tab`（10318，含**能效模式（1~5级）**下拉与 **Webhook 标签**：启用/端口/令牌/自动显示/时长/发送 URL/测试发送/应用重启；**API 接入字段即时自动保存 `_autosave_api`（★ v5.16.0）**）、`_build_content_settings`（10840，含**「DeepSeek余额」tab：API Key 写入 .env / 显示项勾选（仅官方字段）+ 每项自定义模板 / 自动刷新 / 字体自适应+手动字号 / 对齐方式（垂直居中默认/向上） / 颜色，★ v5.13.0**）、`_build_mirror_settings`（11081）、`_build_scheme_settings`（11469）、`_build_monitor_settings`（11728，顶部含**「硬件监控数据源」下拉（LHM/AIDA64/系统原生，★ v5.12.0）**）、`_build_hw_tab`（11972）、`_show_custom_dialog`（9202）。
- 联动：`_activate_by_name`（12250）统一切活跃屏（主控/设置/设备信息标签 + 顶部下拉），`_rebuild_main_tabs`（12168）随设备集合变化重建标签。
- 配色共享组件：`_scheme_combos`/`_scheme_swatch_buttons`/`_current_scheme_colors`、`_make_scheme_row`（11323）`/_make_color_row`（11355）`/_scheme_dialog`（11069）。
- 电视墙：`_wall_apply_layout`（12489）`/_wall_refresh_options`（12525）`/_on_wall_quick`（12578）`/_on_wall_rc_changed`（12599）`/_wall_tick`（12679）`/_WallCanvas`，配置独立存 `MSU2_MINI_wall.json`。
- 托盘：`hide_to_tray`（12798）`/show_window`（12823）`/quit_window`（12828）；UI 状态持久化 `_ui_save_state`（12836）`/_ui_load_state`（12854）。
- **关键全局**：`config_obj`/`all_devices`/`_primary_device`/`_main_ctxs`/`Text1`（信息框）/`_ui_root`/`_keepalive`（跨线程桥防 GC）。

### 5.9 维护要点（改代码前必读）

1. **多设备隔离**：被 daemon 切换的全局 `config_obj`，独立线程一律改用 `dev.config`（循环内取最新）；UI 刷新前 `_ui_set_active()` 锁定当前屏；批量刷新用 `_ui_batch_sync` 防重复保存。
   ★v5.22.0 补充：**「默认活跃屏」与「主设备槽位」必须分开**——UI/API 切屏只用 `set_default_device()`，
   `_primary_device` 只由 daemon 端口扫描/连接维护（端口归属、断线重连依赖它）；新增多设备逻辑时不要再用
   `_primary_device = dev` 这种写法（否则两块屏同时掉线重连会端口串台，导致其中一块永久离线）。
   LCD/SFR 命令失败一律调 `set_device_state(0)`，由 `COMM_FAIL_LIMIT`(3) 连续失败机制决定是否真掉线，
   不要在命令失败处直接关串口。
2. **跨线程更新 UI**：后台线程用 **Signal 跨线程桥**（`_ComboBridge`/`_HwBridge`，实例加入 `_keepalive` 防 GC）或 `QTimer.singleShot`（仅主线程有效）；不要直接操作控件。
3. **新增设置项**：`sys_config.__init__` 加字段 → UI 控件 + trace/信号回调（自动走 save_config）→（如需 API）`_API_CONFIG_WRITABLE` →（如需配色）见维护约定 6。
4. **新增页面**：见 5.4 的同步清单。
5. **API 改动**：`_build_openapi_doc()` paths/schemas + `_API_DOC_HTML` + `export_api_json` 重新生成；`_check_openapi_sync` 启动校验。新增接入协议（gRPC/MQTT 等）时同步：设置 `_proto_items` 勾选框（MQTT 另含 `mqtt_*` 配置分组）→ `start_api_server` 分发 → `stop_api_extra` 停止 → `_api_enabled_protocols`/`api_get_protocols` 自动发现 → 服务定义（gRPC=`_GRPC_PROTO`/`_api_grpc_build_messages`，MQTT=`start_api_mqtt`）→ 文档三处。
6. **Webhook 改动**：新增接收/发送能力时——接收在 `WebhookReceiveHandler.do_POST`（`/webhook/incoming`）加解析分支并补 `_webhook_extract_text` 键；发送在 `webhook_send`/`_webhook_send_worker` 扩展 payload 或加触发点（API `/api/webhook/send`、`api_execute_command`、服务器 `/webhook/send`、设置测试按钮）；同步 `_build_openapi_doc()` 与 `_API_DOC_HTML` 第 12 节；新配置字段在 `sys_config` 加 + 设置页 Webhook 标签接自动保存 + 冒烟测试。
7. **⚠ 脚手架已过时**：不要运行 `_apply_qt_ui.py`，主文件 `UI_Page` 是唯一活跃源。
8. **硬件监控源改动**：新增/修改硬件监控数据源（LHM/AIDA64/系统原生）时——管理器接口必须与 `HardwareMonitorManager` 鸭子兼容（sensors/get_hardware/update_hardwares/get_value/get_value_formatted/list_sensors）；`_ensure_hardware_monitor_async`（14613）按 `config_obj.hardware_source` 分流加载；新配置字段在 `sys_config`（8653）加默认值；切换数据源时重置 `hardware_monitor_manager=None`、`_hw_monitor_loading=False` 下次进入硬件页面生效；AIDA64 读取共享内存 `AIDA64_SensorValues`（需 AIDA64 后台运行并开启「硬件监视→LCD→启用共享内存」，无独立 DLL）；系统原生 = psutil/WMI/nvidia 程序内直读（零后台，WMI 查询需 5 秒缓存防卡顿）。
9. **退出清理**：`__main__` 的 `finally` 会 join 全部线程、`stop_api_server`/`stop_webhook_server`、`Cleanup_LCD_On_Exit`、强制 `save_config(True)` 并 join——新线程/资源也应在退出时清理。
10. **编译（Nuitka）相关（★ v5.23.0，写代码前必看）**：以下四点在源码运行时都看不出来，只在编译后暴露——
    ①**Nuitka 不设 `sys.frozen`**（只注入 `__compiled__`）：判断“是否已打包”必须两者都认（如 `set_auto_start`）；
    ②无控制台 exe（`--windows-console-mode=disable`）下，**任何 `subprocess` 调外部命令都要带 `creationflags=_NO_WINDOW_FLAGS`**，否则子进程会新建控制台窗口（黑框一闪），源码运行时因继承父控制台而看不到；
    ③无控制台时 `sys.stdout/stderr` 是 `NUL:` 包装器（`print` 静默丢弃，日志全丢）→ 排查问题请把 bat 的 `CONSOLE_MODE` 改成 `attach` 重编；
    ④`__file__` 解析为 **exe 目录**下的模块相对路径，`sys.argv[0]` 所在目录 = 程序目录（`get_base_config_dir`）= 配置目录基准；新增外部文件/依赖必须加进 `0_nuitka_build.bat` 的 `--include-data-*`；
    ⑤**排查编译后的问题先看日志**：程序目录 `MSU2_MINI_log.txt`（无控制台时自动写）、信息框的「以下串口未连接」提示；需要实时看输出时把 bat 的 `CONSOLE_MODE` 改 `attach` 重编。
11. **单实例保护（★ v5.25.0）**：`acquire_single_instance()`（命名互斥体）必须在 `__main__` **最前面**调用
    （`_init_single_device()` 之前），否则第二个实例会先抢走某块屏的串口；改这里时注意：
    ①`ctypes` 必须模块级已导入；②读错误码必须用 `WinDLL("kernel32", use_last_error=True)` + `ctypes.get_last_error()`
    （用 `windll...GetLastError()` 会静默失效）；③不要去掉 `--allow-multiple`/`MSU2_MINI_ALLOW_MULTIPLE` 例外；
    ④回归测试 `_scaffold/smoke_test_single_instance.py`。
12. **「已连接」≠「真的在刷新」（★ v5.26.0，排查“屏黑着但显示已连接”必读）**：`device_state` 只表示
    「串口打开过 + 未被判定掉线」，而整帧页发送走 `SER_rw(read=False)` **不回读响应**、写失败被 `SER_rw` 吞掉
    （前 2 次还被 `COMM_FAIL_LIMIT` 忽略），加上按键/ADC 心跳 `manage_task` **只绑主设备一块屏** ——
    第二块屏彻底坏掉时旧逻辑完全无感。改代码时注意：
    ①发送画面后必须用 `_mark_frame_sent(dev, err_seq)`（写成功才记时间）/`_note_picture_ok(dev)`
      （LCD_ADD/图库照片）记账，**不要**直接写 `dev.last_frame_ok_time`；
    ②`LCD_Color_set` 是纯清屏，**不要**记成「画面刷新」（否则“被看门狗清屏后一直没重绘”会被掩盖）；
    ③daemon 渲染循环必须保持**逐屏 try/except**（历史版本一个 try 包住整个 for：一块屏抛异常会中断整轮循环，
      那块屏永久不刷新，而 UI 仍显示已连接、预览还停在最后一帧）——新增页面/渲染逻辑不要把它挪出 try；
    ④新增“画面类”发送路径时记得记账（否则那块屏会被误判为画面停滞）；
    ⑤回归测试 `_scaffold/smoke_test_device_health.py`。
13. **设备复位（上电横幅）与容错尺度（★ v5.28.0，改串口/容错逻辑前必读）**：
    ①**上电横幅 `\x00MSNxx` 是设备复位的铁证**：`_looks_like_boot_banner()` 识别后由
      `_force_device_reset()` **立即判掉线**（关串口→daemon 重连→重新初始化 LCD），不进 `COMM_FAIL_LIMIT` 计数；
      判定条件包含「已连接 + 非烧写中（`write_path_index==0`）+ 连接稳定 3 秒后（`connected_time`）」，
      不要去掉（否则握手残留横幅会让刚连接的屏反复重连）；
    ②**不要用“收到任何字节就清零 comm_fail_count”（SER_Read 旧逻辑）之外的方式收紧容错**——
      开发中曾把「只有回显正确才清零」当成修复，结果高负载下 3 次瞬时失败就误判掉线、
      屏幕反复初始化（正是 v5.22.0 修过的「一块屏老是自动断开」，被 `smoke_test_multiscreen` 抳出）；
      现在横幅靠 `_force_device_reset` 精准命中，其余保持宽容度；
    ③响应校验的**回显语义**：设备回的是「最后一个子命令」的头 2 字节（`LCD_ADD` 这种拼接命令回末尾的 `2,3`，
      不是整条命令头），别按“整条命令头”校验（会整屏误报通信异常）；
    ④新增串口/LCD 失败打印请用 `_throttled_print`/`_throttled_dev_print`（按“设备+类型”限频，5~10 秒一条），
      否则一台设备异常就能把控制台与日志刷爆；
    ⑤回归测试 `_scaffold/smoke_test_comm_health.py`（33 项）。
14. **整帧必须「窗口 + 数据」一次发出（★ v5.29.0，改发送/新增页面必读）**：设备按**字节流**解析命令，
    「LCD 地址窗口」（`LCD_Set_XY`+`LCD_Set_Size`+`[2,3,7]`，即 `_lcd_window_bytes`）之后必须**紧跟**像素数据。
    旧写法「`LCD_ADD(...)` → 渲染+编码（几十毫秒）→ `SER_rw(整帧, read=False)`」中间那段空档里，
    按键 ADC 轮询（`manage_task` 约 20Hz）、通信心跳（`_device_health_tick` 每 2 秒）、API 投屏都会插进命令，
    导致设备解析错位 → **画面倾斜/斜切**（预览却正常），下一次整屏重绘才恢复；v5.26.0 的方向看门狗
    每 15 秒强制 `state_change=1`，把这条路径变成周期性 → 倾斜变密集（v5.29.0 的修复背景）。
    改代码时注意：
    ①**整帧发送统一走 `_send_frame_with_window(dev, hexstream, w, h)`**（窗口命令与数据拼成一条流、一次 `SER_rw`；
      `SER_lock` 内是原子的，任何线程都插不进来）；**不要**再写「先 `LCD_ADD` 再单独发整帧」，
      也不要自己发裸的 `SER_rw(整帧, read=False)`（页面里原有的 `LCD_ADD` 调用可以保留，它只多花一条小命令）；
    ②新增整帧页面时，`LCD_ADD(0,0,SHOW_WIDTH,SHOW_HEIGHT)` 与 `_safe_send_rgb888(...)` 的写法照抄既有页面即可
      （窗口由发送函数自带），**但图像编码必须在发送前完成**（拼接时两者必须紧邻）；
    ③**不要在后加 `time.sleep`/取硬件数据/请求网络到「拼好窗口」与「发数据」之间**；
    ④空档观测：`ScreenDevice.frame_window_time/frame_gap_count/frame_gap_max/frame_gap_intrusions`——
      设备信息 → 连接 页的「帧空档 N 次（最长 x ms）· 空档内被插入命令 M 次」在涨，说明串口上还有东西
      在窗口与数据之间插队（排查提醒，v5.29.0 起已不影响画面）；
    ⑤回归测试 `_scaffold/smoke_test_frame_gap.py`（26 项，含「窗口 + 数据同一条字节流 / 0 字节空档」像素级断言）。
15. **串口事务：帧前后不许有别的命令（★ v5.30.0，用户实测「v5.29.0 之后倾斜依旧」后追加，排查倾斜必读）**：
    这块屏是**小缓冲的字节流解析器**，一帧就是一大串字节（160x80 ≈ 26KB）。v5.29.0 让每帧自带窗口后
    倾斜仍在，而日志里「整屏窗口与帧数据之间被插入命令」仍在计数 → 结论：**只要串口上还有别的命令贴近
    帧数据（帧前、帧后、方向重置前后），设备就可能把帧解析错位 → 画面倾斜**（预览正常），
    而且残缺帧会一直留在设备上，直到下一次整屏重绘才恢复。所以：
    ①**发帧前/后、以及整轮页面渲染期间，把该屏当作「占用中」**：用
      `_serial_begin(dev)`/`_serial_end(dev)`（帧后自动带 150ms 静默期）/`_serial_transaction_busy(dev)`；
      新增「会往串口发命令的线程/定时器」必须先问 `_serial_transaction_busy(dev)`（`manage_task` 与
      `_device_health_tick` 已接）——否则又会变成「倾斜查不出原因」；
    ②**不要**在帧发送前后手动加 ADC/SFR 读来“确认设备还好”（那正是倾斜的推手）；需要心跳就用
      `_device_health_tick`（它已在静默期内自动跳过）；
    ③方向重置（`LCD_State`）后必须尊重 200ms 静默期（`SERIAL_QUIET_AFTER_DIRCHANGE`）——
      「方向重置 → 清屏 → 立刻写整帧」是最易错位的时刻；
    ④方向看门狗清屏后**只对静态页**补 `state_change=1`（`_is_static_redraw_page`）；新增「内容不变就不重绘」
      的静态页要加进那个集合，否则那块屏会一直黑着；
    ⑤帧发送失败会**自动整帧重发**（`frame_resend_count`）；`SER_Write` 短写会续写剩余——
      新加的直接发送路径（不经 `_send_frame_with_window`）请保证写完整性，别用「短写就抛异常」的老写法；
    ⑥现场定位（环境变量，先设再启动）：`MSU2_MINI_NO_LCD_WATCHDOG=1`（关周期方向重置）、
      `MSU2_MINI_LCD_WATCHDOG_S=30`、`MSU2_MINI_NO_KEY_POLL=1`（关按键轮询，按键会失效）、
      `MSU2_MINI_FRAME_QUIET_MS=400`、`MSU2_MINI_WRITE_PACE_MS=2`（大块写限速）；
      看设备信息 → 连接 页的「帧空档 / 静默违规」计数与限频日志；
    ⑦回归测试 `_scaffold/smoke_test_serial_transaction.py`（40 项）。
16. **行级地址重锚：提交不得跨行（★ v5.35.0，改渲染/编码前必读）**：这是倾斜**后果的结构性根治**——
    每行开头都重下一遍「第 row 行、行宽 W」的地址窗口（`_lcd_row_anchor_bytes`），
    所以「丢一个包/杂散字节/命令被吞」最多毁**当前这一行**，下一行立即恢复；旧版整帧只一个窗口时，
    同样的故障会从出错点起**整幅斜切**（量测：4389 像素/40 行 vs 143 像素/1 行）。改代码时注意：
    ①**每个缓冲提交（`[2,3,8,hi,lo,0]`）必须落在同一行内**（不要写回「一个提交跨多行」的旧写法）；
    ②新增/修改画面编码必须保留「行宽 = 图像宽度」的窗口（不要用宽度≠行宽的窗口，否则步长错→画面斜）；
    ③新页面仍走 `_safe_send_rgb888`/`_send_frame_with_window`（自动带上行锚与帧窗口），不要自己拼裸串口流；
    ④对比旧行为用环境变量 `MSU2_MINI_ROW_GUARD=0`；
    ⑤回归测试 `_scaffold/smoke_test_row_anchor.py`（19 项，含虚拟小屏「丢包后错误 ≤ 1 行」的量化断言）。
